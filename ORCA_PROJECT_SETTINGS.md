# Orca 项目设置：junshi-app

> 适用项目：`junshi-app`
> Orca 项目路径：`/Users/caibiao/orca/projects/junshi-app`
> 配置入口：Orca -> 项目列表 -> `junshi-app` -> 项目操作 -> 项目设置
> 说明：本文件对应 Orca 项目设置页面的基础配置，不是 `orca.yaml`。`orca.yaml` 留待下一阶段处理。

## 一、基础目标

将 `junshi-app` 配置为 Orca 中可稳定识别、可快速定位、默认在本机运行的项目：

- 显示名称保持为 `junshi-app`。
- 项目类型保持为 `Git`。
- 默认可用主机保持为 `Local Mac`。
- 项目图标使用稳定、易识别的项目标识。
- 项目颜色与其他 Orca 项目区分开。
- 不在项目设置中保存账号、Cookie、API Key 或 SSH 私钥。

## 二、项目身份

### 推荐设置

| 设置项 | 推荐值 | 说明 |
| --- | --- | --- |
| 类型 | `Git` | 当前项目是 Git 仓库，不要改成普通文件夹项目。 |
| 显示名称 | `junshi-app` | 保持与仓库和 Orca 项目名称一致，便于 CLI、工作树和终端识别。 |
| 项目路径 | `/Users/caibiao/orca/projects/junshi-app` | 当前 Orca 使用的项目路径。 |

### 操作步骤

1. 打开 Orca 左侧项目列表。
2. 找到 `junshi-app`。
3. 打开项目操作菜单。
4. 选择“项目设置”。
5. 检查“显示名称”是否为 `junshi-app`。
6. 如果名称不是 `junshi-app`，改为该值。
7. 离开输入框，让 Orca 自动保存；返回项目列表确认名称已经更新。

## 三、项目图标

截图中的图标设置提供三个标签：

- **头像**：适合使用 GitHub avatar 或网站图标。
- **图标**：适合使用本地 PNG 图标。
- **表情符号**：适合快速区分多个项目。

### 推荐方案

优先使用一个稳定的本地项目图标，而不是依赖远程网站或会变化的 GitHub 头像：

1. 进入“图标”标签。
2. 上传项目 PNG 图标。
3. 使用清晰、简洁、正方形构图的图标。
4. 文件大小控制在 `256KB` 以内。
5. 上传后返回项目列表，确认图标在项目卡片和项目设置中都显示正常。

如果暂时没有正式项目图标，可以先使用以下临时方案：

- 继续使用当前 GitHub avatar；或
- 使用“表情符号”设置一个容易识别的符号。

### 图标选择原则

- 颜色和其他 Orca 项目明显区分。
- 在小尺寸项目卡片中仍能识别。
- 不使用包含密码、token、个人隐私或内部地址的图片。
- 不使用需要登录才能访问的远程图片。
- 不频繁更换，避免工作树列表中的项目识别混乱。

## 四、项目颜色

截图显示项目颜色可以选择：灰色、红色、橙色、黄色、绿色、青色、紫色、粉色，也可以使用 Custom 自定义颜色。

### 推荐设置

为 `junshi-app` 选择一个与其他项目不同的颜色。当前截图中已选择红色；如果红色没有和其他项目冲突，可以保持不变。

颜色只用于 Orca 的视觉识别，不应被当作项目权限、环境、发布状态或风险等级。

### 验收标准

- 左侧项目列表中可以快速找到 `junshi-app`。
- 多个 `junshi-app` 工作树同时存在时仍能识别项目归属。
- 颜色没有被误解为“生产环境”或“高风险”标记。

## 五、项目可用主机

截图中的“可用主机”区域显示：

- `Local Mac`：就绪、当前项目路径为 `/Users/caibiao/orca/projects/junshi-app`。
- “添加到其他主机”：用于把项目配置到已连接的其他主机。

### 当前阶段推荐

保留 `Local Mac` 作为默认主机，不添加远程主机。

原因：

- 当前项目已经在本机可用。
- 本阶段只做 Orca 项目基础身份和显示配置。
- 远程主机属于下一层运行环境配置，需要单独确认 SSH、目录、依赖和权限。

### 本阶段不配置

- SSH 主机。
- 远程 Orca Server。
- 云虚拟机。
- 远程浏览器网络出口。
- 远端项目路径。

这些配置不能只通过“添加主机”完成，还需要验证远端 Git、依赖、Agent、终端和权限，留到远程开发阶段处理。

## 六、基础配置验收清单

- [ ] Orca 项目类型显示为 `Git`。
- [ ] 显示名称为 `junshi-app`。
- [ ] 项目路径为 `/Users/caibiao/orca/projects/junshi-app`。
- [ ] 项目图标已选择一种稳定来源。
- [ ] 如果上传 PNG，文件不超过 `256KB`。
- [ ] 项目颜色与其他项目能够区分。
- [ ] 图标在项目列表和项目设置页面显示正常。
- [ ] `Local Mac` 显示为就绪并作为当前主机。
- [ ] 本阶段没有写入账号、Cookie、API Key 或 SSH 私钥。
- [ ] 本阶段没有创建或修改 `orca.yaml`。

## 七、后续阶段边界

完成本文件对应的项目设置后，再进入以下独立阶段：

1. `orca.yaml` 与 worktree 共享目录。
2. 默认 Agent 与项目级启动参数。
3. 项目级浏览器、Design Mode 和本地端口。
4. GitHub/GitLab/Linear/Jira 项目集成。
5. SSH/远程 Orca Server。
6. 自动化、Orchestration 和项目级验收流程。

本文件只描述截图中的项目身份、图标、颜色和主机配置，不把后续阶段的配置提前混入当前项目设置。
