# junshi-app interview 技能组测试报告

> 测试性质：只读审查、脚本化场景测试与门禁验证
>
> 测试结论：当前技能组尚未形成可验证、可稳定运行的完整访谈闭环
>
> 测试时间：2026-09-10
>
> 交付限制：本次未修改任何技能、规则、模板、脚本或项目文件

## 一、结论先行

当前 interview 技能组的优点是职责划分和原则写得较完整：已经定义了访谈约定、调查取证、现场记录、报告撰写和质检五个辅技能，也明确了证据分层、代拟边界、长论证保真和阶段门控。

但它目前更像一套“流程宪法”，还不是一套可以稳定执行和验收的“访谈系统”。最严重的问题不在于缺少几个追问技巧，而在于缺少可执行的状态模型：

1. 没有决策树、节点依赖和前沿的结构化契约，无法证明每一轮问题来自当前前沿，也无法可靠处理范围变化、越级提问和重复提问。
2. 没有 fork 子代理的异步协作协议。当前文件定义的是同步门控和阻塞式交接，无法保证支线取证运行时主代理继续推进不依赖支线的主线访谈。
3. 调查取证前没有一个强制、可验证的“具体范围确认门”。是否允许外查、查什么平台、什么时间和版本、产出给谁使用，主要依赖调用者自觉。
4. 没有真正的 interview 运行器或评测运行器。现有 evals 只有 3 个对话用例，仓库中未发现能执行完整多轮访谈并验证状态变化的入口。
5. 阶段门控主要检查文件、字段和用户确认，不能判断“产品想法是否已经变清楚”“当前阶段是否真正收敛”。
6. 面向非专业编程/工程用户的白话解释、术语澄清和理解复述没有成为硬规则。
7. 证据门禁和跨工件质检大量依赖关键词、全局字符串和启发式相似度，存在误放行和误阻塞。
8. 长论证保真原则写得较强，但没有把“问题依据、前沿变化、证据选择、状态变化和范围重算”作为结构化过程记录保存。
9. 批判性代拟的原则已经写出，但没有强制“先审查用户建议，再代拟，再等待确认”的状态转换和顺序校验。

因此，本技能组当前的总体判定是：

> **规范设计中等偏强，运行可验证性不足；不宜直接作为完整访谈闭环投入使用。应先补齐状态、证据、异步协作和阶段验收基础，再扩大访谈能力。**

## 二、测试范围与证据等级

### 2.1 审查范围

纳入了 interview 技能组的完整依赖闭包：

- 主技能：`.agents/skills/interview-workflow/SKILL.md`
- 辅技能：
  - `interview-brief`
  - `interview-due-diligence`
  - `interview-transcriber`
  - `interview-reporter`
  - `interview-qc`
- 直接关联的 references、assets/templates、scripts、examples 和 `evals/evals.json`
- `.agents/skills` 与 `.claude/skills` 两套副本
- 现有 `访谈资料/学业军师产品正式方案访谈/` 四个访谈工件

### 2.2 测试方式

本次采用了多代理并行的只读测试：

- 1 个依赖闭包和运行入口侦察代理；
- 5 个专项代理，分别测试非专业用户、决策树与前沿、证据取证、fork 与辅技能、压力场景与代拟；
- 主代理对脚本、现有工件和两套技能副本做定向验证。

### 2.3 证据标记

- `[静态事实]`：来自当前文件内容、路径或脚本实现。
- `[实测]`：已实际运行命令或函数并取得输出。
- `[场景预期]`：针对脚本化用户场景设定的验收行为，尚未由真实 interview runner 执行。
- `[推断]`：基于静态事实得出的风险判断。
- `[未验证]`：当前环境没有可执行入口或缺少必要外部条件。

本报告不把模型不可外显的隐藏思维作为交付物。这里的“保留长思维链”落实为保留可见的问答、论证、问题依据、证据定位、状态变化、修正链和阶段验收依据。

## 三、测试事实与直接证据

### 3.1 技能闭包和运行入口

[静态事实] 当前技能组为 1 个主技能加 5 个辅技能。主技能在 `interview-workflow/SKILL.md:18-25` 定义职责矩阵，在 `:89-124` 定义 Phase 0–5，在 `:127-155` 定义硬路由和降级，在 `:156-185` 定义保真验收。

[静态事实] 当前仓库没有独立的 interview runner、访谈状态机或 eval runner。可执行脚本主要是脚手架和几个启发式门禁脚本。`evals/evals.json` 只有 3 个对话型用例，且 `files=[]`，没有发现与之配套的执行器。

[实测] `.agents/skills` 与 `.claude/skills` 两套 interview 技能目录执行 `diff -rq` 无差异。当前内容一致，但没有同步机制；未来仍存在双份维护漂移风险。

[未验证] `STRUCTURE.md` 表明 Claude 开发侧优先加载 `.claude/skills`，但当前会话没有对宿主 loader 做实际黑盒验证，不能把加载优先级写成已验证事实。

### 3.2 现有工件和门禁结果

[实测] 辅技能示例门禁可以通过：

- `brief_validate.py`：示例通过，输出“5 项门控已确认，六维度齐备”；
- `evidence_gate.py`：示例通过；
- `report_gate.py`：报告示例通过。

这些结果只证明示例满足脚本的浅层规则，不证明访谈行为、语义状态或用户理解正确。

[实测] 对现有 `访谈资料/学业军师产品正式方案访谈/` 运行 `qc_run.py`：共 220 项，阻塞 28 项，严重 190 项。主要问题包括 02→03 长回答保留率只有 2%–9% 的疑似压缩、状态和定位问题。结论为“存在阻塞项，定稿暂停”。

[实测] 对现有 `02_访谈记录.md` 运行 `record_gate.py --full`：问题单元 2 缺少回应、确认状态、原始定位和记录授权。

[实测] 对现有 `01_调查取证.md` 运行 `evidence_gate.py`：脚本报缺少“证据概括”，并报有来源区块缺少材料状态。实际文件使用的是“调查概括”，且 `### 来源状态统一说明` 被脚本的 `^### 来源` 分割规则误当成来源区块。

[实测] 对现有 `03_访谈报告.md` 运行 `report_gate.py`：报告问题节缺少确认状态。

这些结果证明现有样例闭环没有通过；不能把已有工件当作技能组已经有效运行的证明。

## 四、问题清单与严重度

严重度含义：

- **阻断级**：会使访谈无法可靠推进、错误取证或错误放行，必须先处理。
- **严重**：会显著污染需求、证据、状态或报告，但可以在补充人工复核后暂时运行。
- **一般**：影响质量、可维护性或用户体验，暂不一定阻断主流程。

### P0-1：没有可执行的决策树、节点依赖和前沿机制

**证据**

- `interview-workflow/SKILL.md:107-111` 只要求“报道当前阶段/问题”、识别未决和判定状态。
- `artifact-schemas.md:32-64` 与 `assets/templates/02_访谈记录.md` 只有问题、长篇回应、确认状态、原始定位、记录授权和记录变更。
- `reasoning-methodology.md` 虽然提到因果、依赖、前提、分支、取舍和反例，但这些是论证表达要求，不是可执行的节点调度结构。
- 当前没有 `root_id`、`parent_id`、`depends_on`、`blocked_by`、`frontier`、`downstream_decisions`、`completion_evidence` 或 `scope_version`。

**场景测试**

- 用户第一轮说“想做一个 AI 学习产品”，下一轮改成“教师备课工具”；预期是保留原根节点，建立新范围分支，标记旧下游陈旧并重算前沿。
- Q1“目标用户”→Q2“价值假设”→Q3“功能”，Q4“预算”为独立问题；预期 Q3 未解锁前不能问，Q4 可以作为当前前沿候选，但不能无条件把所有问题一次问完。

当前没有字段或机制支撑这两个场景。[未验证] 因没有 runner，真实对话行为未执行；这里判定的是设计能力缺口，不是已观察到的模型运行错误。

**影响**

无法审计：

- 问题为什么在这一轮被提出；
- 问题依赖的前提是否满足；
- 哪些问题已被用户回答；
- 范围改变后哪些结论和下游问题失效；
- 当前还有哪些可以问、不能问、需要证据后才能问的问题。

这直接影响“逐步把模糊想法打磨清晰”的核心目标，属于阻断级设计缺口。

**优化方向**

建立独立的决策树与访谈状态契约。六种证据/确认状态不能被直接拿来代替节点生命周期；建议分开记录：

- 节点生命周期：`candidate / ready / active / blocked / done / stale / superseded`；
- 证据与确认状态：沿用现有六态；
- 取证状态：`unverified / partially_verified / verified / conflicting`。

每个节点至少增加：

```text
node_id
root_id
parent_id
branch_id
depends_on
blocked_by
independent_group
question_kind
assumptions/prerequisites
facts
tradeoffs
risks
downstream_decisions
completion_evidence
evidence_refs
status
supersedes
scope_version
recompute_reason
round
source_location
```

每轮输出 `frontier={ready, candidate, blocked, done, stale}`。用户改变目标或范围时递增 `scope_version`，沿依赖和下游关系标记 stale，保存修正链，再重新计算 ready 节点。

**必须增加**

- `references/decision-tree-contract.md`；
- 02 记录模板的节点、依赖、前沿和范围版本字段；
- `scripts/frontier_gate.py` 或同等状态校验脚本；
- 目标改变、依赖解锁、独立问题、重复问题和前沿重算 eval fixtures。

**必须减少或改变**

- 减少固定“只按阶段和问题推进”的隐式逻辑；
- 将六个 Phase 从不可变流程改为默认骨架，允许按具体问题增加、合并、跳过或回退阶段；
- 不再把“所有独立问题都有回应”作为前沿正确的替代验收。

**阶段验收**

- 每个当前问题都能指向一个节点和一条依赖链；
- 依赖未满足的问题不能进入 ready；
- 同一轮只提出当前前沿中相互独立且已满足前提的问题；
- 用户改范围后，旧下游被标记 stale，并记录重算原因；
- 每个完成节点都有确认/证据/完成依据，而不是只有一段结论。

### P0-2：调查取证前没有强制确认具体范围

**证据**

`interview-due-diligence/SKILL.md:39-47` 要求调用方提供调查对象、问题范围、允许来源、排除范围、输出位置和是否联网；`due-diligence-guide.md:15-45` 也要求来源定位、时间/版本、权限和外部授权。但 `interview-workflow/SKILL.md:89-104` 的 Phase 0/1 没有把这些字段列为正式门控。

[静态事实] 当前设计把“先确认范围”写成调用输入要求，未形成“缺任一关键范围字段就停下询问用户”的强制状态。

[实测] `evidence_gate.py` 只检查来源区块、定位、材料状态、证据概括和待核验清单，不检查调查范围授权、时间窗口、版本/提交点、允许平台、外部网络授权、排除范围、来源类型或产出用途。空定位、空状态、占位符、缺证据等级和缺冲突区块的脚本化负例仍可通过。

**影响**

主代理可能在用户没有指定具体范围时自主取证，把历史文件、候选方案、外部文章或代码现状误当成调查对象；这会污染后续问题和用户决策，属于阻断级风险。

**优化方向**

把“调查范围授权”前置到 00 约定或单独的调查计划节点。未满足时只询问范围，不派 due-diligence 子代理。

调查授权至少包括：

- 调查目标和核心问题；
- 产出给谁使用、用于什么决定；
- 时间窗口；
- 仓库分支、提交点或文档版本；
- 允许的来源类型和平台；
- GitHub、X、论文、文章等平台是否分别允许；
- 是否允许联网；
- 明确排除范围；
- 失败或不可访问来源的处理方式；
- 调查结束的验收条件。

**必须增加**

- 00 模板中的“调查授权与来源范围”区块；
- 01 模板中的“调查范围快照”和“来源登记表”；
- `evidence_scope_gate.py` 或扩展现有 `evidence_gate.py`。

每个来源至少记录：路径或 URL、来源类型、章节/行号/消息号、获取时间、版本/提交/发布日期、权限、访问状态、材料状态、证据等级、来源事实、来源观点、调查推断、缺口、问题候选。

**必须减少或改变**

- 减少“调用者自觉传入范围”的隐式依赖；
- 禁止 gate 以存在任意一段“待核验”文字代替真实的失败来源和缺口登记；
- 不能把 `调查概括` 与 `证据概括` 的命名差异误判成证据缺失，需统一 schema 或兼容明确别名。

**阶段验收**

- 范围字段缺任一关键项时，系统先提问，不开始外部取证；
- 每个来源都能回溯到路径/URL、版本和获取时间；
- 不可访问来源被记录为失败，不被静默跳过；
- 来源缺口自动变成待核验问题，不被写成结论；
- 用户确认问题清单前，不进入正式访谈。

### P0-3：fork 子代理没有异步非阻塞协议

**证据**

主技能 `SKILL.md:100-121` 和 `routing-contract.md:9-51` 定义了辅技能交接、合格字段、阻塞和降级，但搜索整个技能组没有发现 fork、异步、迟到结果、超时、取消、去重、冲突合并或前沿重算协议。

`interview-due-diligence` 只规定按来源或材料区间并行，并返回证据包；没有规定支线取证运行时主线可以继续问什么。

**影响**

当前契约有两种危险解释：

1. 主代理等待支线证据返回，主线访谈被阻塞；
2. 主代理不等结果继续推进，却可能问到依赖该证据的问题，或在旧证据上写入后续产物。

这直接违反“fork 子代理执行支线、主代理继续主线”的目标，属于阻断级缺口。

**优化方向**

增加异步路由协议，至少包括：

```text
dispatch_id
parent_phase
parent_checkpoint
input_snapshot
input_version
shard
status
attempt
partial/late
result_ref
evidence_state
failure_code
conflict_set
merge_policy
frontier_effect
fallback_auth
fallback_expiry
review_due
```

允许主线继续的条件：当前问题的 `depends_on` 不包含正在运行的支线结果，且不会越过阶段门控、写入依赖支线结果的工件。

支线结果返回后：

- 依据 `dispatch_id + input_version` 去重；
- 迟到结果标记为 `late` 或 `stale`；
- 冲突结果进入 `conflict_set`，不能静默覆盖；
- 更新证据状态和前沿；
- 影响下游节点时触发重算；
- 失败、超时、取消必须留账；
- 只有经过授权的降级才允许主技能代执行，并设置复核期限。

**必须增加**

- `references/async-routing-contract.md`，或扩展 `routing-contract.md`；
- 主线问题与支线结果的依赖字段；
- fork 延迟、失败、冲突、迟到、取消和降级的 eval fixtures。

**必须减少或改变**

- 减少“所有辅技能结果返回后才能继续”的全局阻塞；
- 改为“只等待当前问题真正依赖的结果”；
- 主技能负责编排、依赖判断和冲突裁决，QC 保持只读独立审计，不与主技能重复集成。

**阶段验收**

- fork 启动后，主代理能够继续处理不依赖 fork 的问题；
- 主代理不会因为支线未返回而停止整个访谈；
- 主代理不会在依赖证据未到达时推进相关节点或门控；
- 支线结果回传后能影响对应节点和 frontier；
- 迟到与冲突结果不会静默覆盖已有状态。

### P0-4：没有完整 interview runner，无法验证真实黑盒行为

**证据**

[静态事实] 仓库中未发现独立 interview runner、状态机或 eval runner。现有 `evals/evals.json` 只有 3 个用例，现有脚本只做工件门禁或脚手架。

[未验证] 本次无法直接运行完整的多轮访谈，因此不能声称当前模型一定会重复提问、越级提问、阻塞主线或错误代拟；只能确认技能文件没有提供足够的可执行约束和测试入口。

**影响**

设计意图无法转化为回归测试。每次修改技能后，只能靠人工阅读或偶发对话观察，无法确认：

- 每轮 frontier 是否正确；
- 用户改范围后是否重算；
- fork 是否非阻塞；
- 代拟状态是否正确转移；
- 记录和报告是否逐问题可回溯。

**优化方向**

增加最小可运行的 interview eval harness，至少能接收脚本化用户输入，保存每轮事件，并对状态断言。

**必须增加**

- `evals/fixtures/`：脚本化用户、证据、范围变化和支线延迟场景；
- `evals/runner` 或等价执行入口；
- `evals/assertions/`：frontier、状态转换、来源、授权、问题 ID、记录保真和异步合并断言；
- 每次运行的事件日志和结果摘要。

**必须减少或改变**

- 不再把 `evals.json` 中的自然语言期望当作可执行评测；
- 不再把通过几个示例门禁描述为整个技能组通过。

**阶段验收**

同一脚本化场景重复运行时，能够稳定检查并报告：问题序列、frontier、依赖关系、证据引用、状态变化、fork 事件、暂停原因、阶段门控和最终结果。

### P1-1：阶段门控检查工件，不检查产品想法是否收敛

**证据**

`interview-workflow/SKILL.md:90-124` 定义了六个固定 Phase 和文件门控；`artifact-schemas.md` 定义了四个工件的结构。但没有对“对象、用户、真实问题、约束、目标、证据和决定是否已经足够清楚”定义内容级验收。

**影响**

可能出现文件齐全、勾选项齐全，但用户仍没有明确说明：

- 谁在什么场景遇到什么问题；
- 想改变的可观察处境是什么；
- 当前判断依赖哪些前提；
- 哪些决定已经确认；
- 哪些仍需要证据。

**优化方向**

把阶段验收从“文件存在+状态勾选”升级为“节点完成条件+用户可复述+证据可定位”。

建议默认阶段只作为骨架，每个会话根据问题生成自己的阶段计划。阶段可以增加、合并、跳过或回退；每个阶段必须定义：目标、当前 frontier、问题集合、完成证据、用户确认方式、未决清单和下一阶段解锁条件。

**必须增加**

- `references/stage-acceptance.md`；
- 02 或独立状态记录中的阶段完成条件和验收证据；
- 0/1/2/3/4/5 阶段的内容级 eval。

**必须减少或改变**

- 取消 03 报告必须固定生成六个章的强制映射；无记录阶段不应生成空骨架章；
- 将六阶段改为默认阶段注册表，而非所有问题都必须经过相同顺序。

**阶段验收示例**

- 目标和范围阶段：用户能用自己的话复述对象、使用者、问题、约束和预期改变，并能纠正代理复述；
- 证据阶段：每个关键判断有来源、状态和缺口；
- 决策阶段：每个关键决定有前提、取舍、风险、下游影响和完成证据；
- 收尾阶段：未决项都有负责人、所需证据和下一步，不以摘要掩盖。

### P1-2：非专业用户适配没有成为硬契约

**证据**

主技能虽要求第一性、真实对象和受访者背景，`interview-brief` 的 `alignment-guide.md:9-45` 也提供六维度问题，但没有规定：

- 遇到专业术语先停下解释；
- 让用户用自己的话复述；
- 发现用户误用工程术语时不把它当作需求；
- 为“智能、自动、闭环、低延迟、可扩展、MVP、RAG、API”等词建立待定义记录。

`02_访谈记录.example.md` 有一条不使用产品术语的优秀示例，但它只是示例，不是流程硬规则。

[实测] `brief_validate.py` 对示例输出通过，但该脚本只检查关键词和 checkbox，不能验证白话表达或用户是否理解。

**影响**

面向非专业用户时，代理可能把模糊愿望直接拆成功能列表，或者把用户错误使用的工程词直接当成需求和架构前提，污染后续取证、决策树和开发规划。

**优化方向**

增加术语安全环：

```text
捕获用户原词
→ 询问用户想表达的具体事情
→ 要求给出真实例子
→ 提供一个或多个白话解释候选
→ 将候选标为专业建议/待确认
→ 用户用自己的话复述或纠正
→ 确认后才写入节点和阶段约定
```

**必须增加**

- `references/non-expert-language.md`；
- 00 模板中的“对象—用户—问题—约束—目标”字段；
- 00 或 02 中的术语账本：用户原话、当前解释、证据/未知、状态、定位；
- 模糊想法和工程术语误用 eval。

**必须减少或改变**

- 减少访谈早期的技术方案、架构选型和 API 讨论；
- 不把“智能/自动/闭环/低延迟”等词直接写入产品需求；
- 术语解释不应塞给 due-diligence 或 transcriber，保持辅技能专业边界。

**阶段验收**

- 访谈开场用非专业用户能理解的语言说明流程、可暂停、可纠正和记录边界；
- 用户能用自己的话复述当前定义；
- 代理能明确指出用户复述中的错误或缺口；
- 术语在未解释和未确认前保持 `[待确认]`；
- 对象、用户、问题、约束和目标未收敛前，不进入架构选型。

### P1-3：证据门禁过浅，存在误放行和误阻塞

**证据**

`evidence_gate.py:19-53` 主要检查区块名称、来源块中是否出现“定位/材料状态”和待核验清单，不检查来源类型、获取时间、权限、版本、访问状态、证据等级、事实/来源观点/调查推断的完整性，也不拒绝占位符。

[实测] 空定位、空状态、占位符来源、缺证据等级、缺冲突/限制区块的脚本化负例仍可通过。

[实测] 真实 `01_调查取证.md` 因使用“调查概括”而不是“证据概括”，并因 `来源状态统一说明` 被错误切分而失败。这是误阻塞，不是证据真的缺失。

`qc_run.py` 对报告的证据检查只看是否出现字符串“02_访谈记录”，可以被无关文本或伪造引用绕过。

**影响**

- 不完整的证据包可能被放行；
- 正常的证据包可能被错误阻塞；
- 外部来源不能稳定复现；
- 证据缺口不能可靠转化为访谈问题。

**优化方向**

将 01 改成可解析的来源登记结构，而不是靠标题和关键词识别。门禁逐来源校验必填字段，逐问题校验证据引用。

**必须增加**

- 来源 schema 和来源 ID；
- 来源类型、版本、获取时间、权限、访问状态和证据等级；
- 失败来源、冲突集合和待核验问题；
- `question_id → evidence_ref[]` 映射；
- 对 GitHub、论文、文章、X 的来源特征说明：GitHub 关注提交/版本，论文关注 DOI/版本/方法/样本，文章关注作者/出版物/发布日期，X 默认弱证据并记录上下文和可访问性。

**必须减少或改变**

- 减少标题正则和全局关键词作为放行依据；
- 统一“调查概括/证据概括” schema，或明确兼容别名；
- 不把“存在待核验清单”当作所有证据缺口已经登记。

**阶段验收**

- 每个来源字段完整且可复现；
- 每个关键问题能列出证据或明确标记缺证据；
- 失败来源被记录；
- 来源冲突不被自动仲裁成单一事实；
- 未获用户授权的外部平台不被访问。

### P1-4：长过程保真规则存在，但过程记录不够结构化

**证据**

主技能和 transcriber 已明确要求保留前提、条件/限定、反例、证据等级、修正链和未决项；`transcriber/SKILL.md:33-47` 规定增量写入前先全量核对。

但 02 模板只有问题、回应、状态、定位、授权和变更字段，没有保存：

- 为什么在这一轮问这个问题；
- 问题对应哪个节点和 frontier；
- 取证结果如何改变问题；
- 哪些决定被用户修正；
- 哪些下游问题因此失效；
- 当前阶段为什么算收敛。

[实测] 现有目录 `qc_run.py` 检测到大量 02→03 长答案保留率为 2%–9%，远低于 60% 阈值。当前报告不能作为长论证保真通过样例。

[静态事实] `qc_fidelity.py` 和 `qc_run.py` 使用整份报告的全局 SequenceMatcher，可能被跨问题重复文本、摘要或表格污染；它们不能逐问题确认论证零件是否保留。

**影响**

报告可能保留了“结论”，却丢失用户为什么这样判断、什么条件下成立、后来如何修正以及哪些仍未解决。对于非专业用户逐步打磨产品想法，这是实质性信息损失。

**优化方向**

保留两层记录：

1. 可见原始访谈层：逐轮问题、用户回答、代理复述、用户修正和授权；
2. 结构化决策层：节点、前提、证据、状态、取舍、风险、下游、frontier 和验收。

这不是保存隐藏思维，而是保存可复核的访谈过程和显性论证。

**必须增加**

- 02 的轮次和问题依据字段；
- 节点与证据引用；
- 前沿快照和范围版本；
- 用户修正链及其影响范围；
- 阶段验收记录；
- 逐问题、逐论证零件的保真检查。

**必须减少或改变**

- 不再把全局文本相似度当作长论证保真结论；
- 将启发式脚本降级为“提示人工复核”，不能单独放行定稿；
- 报告不能用结论总览代替问题单元中的长回应。

**阶段验收**

对每个问题单元能够逐项回溯：问题依据、用户原答、代理复述、用户修正、证据引用、状态、授权、下游影响和阶段确认。

### P1-5：批判性代拟没有可执行的先后顺序和状态迁移

**证据**

`state-rubric.md:5-29` 已规定“请为我作答”只授权代拟，代拟不等确认；主技能也要求代拟与用户结论分开。

但实际脚本压力测试显示：

- `record_gate` 接受“请直接替我写最终答案”并标记 `[确认采纳]` 的矛盾记录；
- 同一问题同时出现 `[用户明确判断] [待确认]` 也不会被阻塞；
- 规则没有要求先列客观证据、冲突和风险，再进入代拟。

**影响**

用户的非专业建议可能直接进入正式结论；代理也可能先顺着用户写稿，再补一个模糊的“待验证”，而没有真正检验建议是否符合事实。

**优化方向**

为代拟增加强制“批判卡片”：

```text
用户建议
→ 已知事实与证据
→ 不支持/冲突的证据
→ 隐含前提
→ 风险与反例
→ 可行解释或替代方案
→ 专业建议·代拟
→ 用户确认/修正/拒绝
```

代拟必须保持 `[专业建议·代拟]` 和 `[待确认]`，只有用户明确确认对应具体内容后，才允许转为 `[确认采纳]`。

**必须增加**

- 代拟前批判卡片字段；
- 状态迁移规则和冲突校验；
- 用户确认的对象范围和定位；
- 代拟顺序与事实冲突 eval。

**必须减少或改变**

- 禁止“用户说了所以直接写成结论”；
- 禁止用任何笼统的“确认”覆盖同段未被确认的扩展内容；
- 禁止同一问题同时拥有互相冲突的当前状态。

**阶段验收**

- 用户建议不符合证据时，代理先指出冲突，再给出替代方案；
- 代拟与用户原话视觉和语义分离；
- 用户未明确确认时，不能写成用户结论；
- 用户改变意见时，旧表述保留并标记已被修正，新表述单独定位。

### P1-6：门禁脚本不能可靠阻止错误放行

**证据**

压力测试实际发现：

- `qc.check_state` 对“当前没有待确认项，所有方案已确认”仍返回无发现；无关的 pending 标记也可以掩盖缺失；
- `qc_run.py` 不逐问题映射报告和记录 ID；虚构的“问题 9-9”不会被作为严重映射错误阻塞；
- `report_gate.py` 只检查报告中存在某个确认状态，不检查每个问题节都有状态；
- `record_gate.py` 的字段正则按行解析，多行回应可能被截成首行，不能正确判断回应和代拟语义；
- `qc_run.py` 只检查报告中出现“02_访谈记录”字符串，不能证明真实证据引用；
- `qc_run.py` 的长论证检测和权限检查主要是关键词提示；
- `scaffold_interviews.py` 在模板缺失时会写入“模板缺失，由 interview-workflow 填充”的占位文本。

**影响**

门禁可能出现两种相反错误：

- 不合格产物被放行；
- 合格产物被误阻塞。

错误放行比单纯漏报更危险，因为它会让主技能继续推进并把错误状态写入后续工件。

**优化方向**

所有门禁改为按结构化对象逐项校验：问题 ID、节点 ID、状态、定位、授权、证据引用、来源、阶段和当前状态都必须一一对应。脚本无法判断语义时必须输出“需人工复核”，不能输出“通过”。

**必须增加**

- 结构化解析器或稳定 schema；
- 逐问题/逐节点映射校验；
- 状态冲突和非法迁移校验；
- 否定语义和无关 pending 检测；
- 多行字段解析测试；
- 占位符、空字段和模板缺失的失败关闭测试。

**必须减少或改变**

- 移除全局字符串存在性作为证据和状态门禁；
- `scaffold_interviews.py` 模板缺失时应失败，不应写入可被误认为产物的占位文件；
- 把脚本名称和输出从“通过”改为更准确的“结构检查通过，语义仍需复核”。

**阶段验收**

构造以下负例时必须阻塞：

- 报告含有不存在的节点或问题 ID；
- 某一问题缺状态、定位、授权或证据引用；
- 同一问题同时存在互相冲突的当前状态；
- 仅有无关的待确认文字；
- 代拟未确认却标为确认采纳；
- 证据引用只是无关的文件名字符串；
- 模板缺失或占位符未被替换。

### P1-7：固定六阶段和固定报告章法限制因地制宜

**证据**

`artifact-schemas.md` 和 reporter 技能把报告映射为第一章至第六章，对应固定阶段 0–5。主技能也以六个 Phase 作为默认流程。

**影响**

不同产品问题可能需要完全不同的访谈路径：

- 有的问题应先验证现实场景，再谈产品价值；
- 有的问题需要先厘清责任边界，再调查工程方案；
- 有的问题需要中途回退到范围约定；
- 有的问题只有一个阶段就能完成；
- 有的问题需要新增安全、用户研究、合规或工程可行性阶段。

如果固定章法先于问题结构，访谈就会为了填阶段而提问，而不是根据当前前沿推进。

**优化方向**

保留 00/01/02/03 四类工件作为稳定的证据层，但将阶段改成“会话级动态阶段计划”：

- 默认阶段只是候选；
- 阶段可增加、合并、跳过和回退；
- 每个阶段有独立目标、节点集合、验收标准和退出条件；
- 报告按实际阶段和问题节点生成，不生成空章；
- 用户改变目标时可重新建立阶段计划，但保留历史版本。

**必须增加**

- 动态 stage registry；
- 阶段版本和回退记录；
- 阶段完成证据；
- 动态报告映射规则。

**必须减少或改变**

- 减少固定六章和空模板章节；
- 不把“完成六个阶段”本身作为访谈完成标准。

### P1-8：辅技能职责有文字边界，但缺少运行时责任协议

**证据**

静态职责边界整体清晰：

- brief 只做约定；
- due-diligence 只读取证；
- transcriber 负责 02；
- reporter 负责以 02 为唯一证据写 03；
- QC 负责跨工件只读审计。

但主技能也负责“集成、统一口径、去重”，QC 又负责“集成口核对”，transcriber 和 QC 都检查保真、状态和定位。静态文件没有明确冲突裁决权、输入快照、输出版本和谁可以改变 frontier。

**影响**

辅技能可能重复处理同一件事，或者都以为另一方会做冲突裁决；最终报告出现重复、状态覆盖和责任不清。

**优化方向**

给每个辅技能增加统一输入输出契约：

- 输入范围和版本；
- 只读/可写权限；
- 负责字段；
- 不负责字段；
- 失败和阻塞格式；
- 结果引用；
- 是否可以影响状态或 frontier。

责任原则建议固定为：

- 主技能：编排、阶段计划、冲突裁决、用户门控；
- brief：约定内容；
- due-diligence：证据包；
- transcriber：可见访谈层记录；
- reporter：由 02 生成正式阅读层；
- QC：独立只读审计，不改稿、不替主技能裁决产品判断。

**必须减少或改变**

- 减少主技能与 QC 的集成职责重叠；
- 减少 transcriber 与 QC 对同一语义问题的重复判定；
- 禁止辅技能直接改变用户决策和产品状态。

### P2-1：`.agents/skills` 与 `.claude/skills` 双份副本存在漂移风险

[实测] 当前两套副本没有差异。

[推断] 两套目录仍然意味着未来可能只修改其中一份，导致“测试的是一套、实际加载的是另一套”。

**优化方向**

保留一个权威源，另一份由同步脚本或构建流程生成；如果宿主明确只加载 `.claude/skills`，则应减少 `.agents/skills` 作为独立维护源的职责。

**阶段验收**

- 每次技能变更能检查两套副本一致；
- 测试明确记录实际 loader 选择的路径；
- 不允许一份更新、一份静默过期。

### P2-2：现有 eval 覆盖太窄，不能覆盖用户真正关心的行为

现有 3 个 eval 只覆盖：

1. 第一性追问；
2. 局部学校试用不等于产品有效；
3. 长回答报告保真。

缺少：

- 调查前范围确认；
- 非专业用户和错误术语；
- 决策树、前沿、依赖和重复问题；
- 用户改变范围后的重算；
- fork 延迟和主线继续；
- 来源冲突和失败来源；
- 拒答、含糊回答和沉默；
- 批判性代拟；
- 阶段内容验收；
- 逐问题记录和报告映射；
- 非法状态迁移和授权冲突。

**优化方向**

将 eval 从 3 个开放式对话题扩展为事件驱动的脚本化场景集。每个场景都要有输入、预期事件序列、状态断言、证据断言、暂停条件和最终验收。

## 五、必须增加、保留、减少的功能与文件

### 5.1 必须增加

| 优先级 | 功能/文件 | 目的 |
|---|---|---|
| 阻断级 | `references/decision-tree-contract.md` | 定义根节点、分支、依赖、前沿、下游影响、范围版本和重算规则 |
| 阻断级 | `references/async-routing-contract.md` 或扩展 `routing-contract.md` | 定义 fork 非阻塞、dispatch、快照、迟到、冲突、失败、降级和合并 |
| 阻断级 | interview eval runner 与 `evals/fixtures/` | 让多轮访谈、范围变化和 fork 结果可重复测试 |
| 严重 | 00/01/02 的结构化字段扩展 | 保存调查授权、来源、节点、轮次、前沿、问题依据和状态 |
| 严重 | `references/stage-acceptance.md` | 定义内容级阶段完成标准，支持动态阶段 |
| 严重 | `references/non-expert-language.md` | 定义白话解释、术语安全环、用户复述和理解验收 |
| 严重 | 代拟批判卡片与状态迁移规则 | 强制先证据审查，后代拟，最后等待用户确认 |
| 严重 | 逐节点/逐问题 gate | 防止状态、证据、定位、授权和问题 ID 错配 |
| 一般 | 副本同步检查 | 防止 `.agents` 与 `.claude` 未来漂移 |

### 5.2 必须保留

- 00/01/02/03 四类工件的职责分层；
- 六种证据/确认状态；
- due-diligence 的只读和事实/观点/推断/待核验分层；
- transcriber 的全量先于局部原则；
- reporter 以 02 为唯一内容证据源；
- QC 的独立只读审计角色；
- 长论证中的前提、限定、反例、证据等级、修正链和未决项。

这些部分是当前技能组中最有价值的基础，不应因为增加状态机而被删除。

### 5.3 必须减少或改变

1. **减少固定六阶段的强制性**：改为默认阶段骨架和会话级动态阶段计划。
2. **减少同步全局阻塞**：只等待当前节点真正依赖的结果。
3. **减少关键词门禁**：改成结构化逐节点校验。
4. **减少全局相似度作为保真证据**：改成逐问题、逐论证零件核对。
5. **减少空模板和占位回退**：模板缺失时失败关闭，不写可被误认的占位产物。
6. **减少主技能与 QC 的集成重叠**：主技能裁决和编排，QC 独立审计。
7. **减少早期技术化提问**：非专业用户的对象、问题、约束和目标未确认前，不进入 API、RAG、事件驱动、低延迟等技术词。
8. **减少双目录独立维护**：选择一个权威技能源，另一份同步或删除。

## 六、推荐的完整闭环

建议将 interview 技能组重构为以下可审计闭环：

```text
1. 范围约定
   ↓
2. 调查授权与来源计划
   ↓
3. 建立根节点、决策树和初始 frontier
   ↓
4. 只提出当前 frontier 中已满足前提的问题
   ↓
5. 用户回答、复述、纠正和确认
   ↓
6. 证据取证与来源登记
   ↓
7. 更新事实、前提、取舍、风险、下游决定和节点状态
   ↓
8. 记录可见问答、证据定位、修正链和授权
   ↓
9. 重算 frontier；必要时回退或重定范围
   ↓
10. 阶段内容验收
   ↓
11. 支线结果异步合并；冲突/迟到结果触发重算
   ↓
12. 批判性代拟与用户确认
   ↓
13. 逐节点 QC 和报告生成
   ↓
14. 输出已确认、专业建议、待确认、待验证和下一步
```

### 每轮最小输出

每一轮不应只输出问题文本，至少要保存：

- 当前 scope_version；
- 当前 frontier；
- 本轮提出的问题及其 node_id；
- 每个问题的依赖已满足依据；
- 为什么现在问；
- 用户回答和原始定位；
- 事实、推断、取舍、风险、未决项；
- 证据来源和访问状态；
- 用户确认/修正/拒绝/暂停状态；
- 对下游节点的影响；
- 下一轮 frontier 和阶段验收状态。

### 对非专业用户的最小交互要求

- 用日常语言说清当前要解决什么；
- 不把用户说出的技术名词自动当作需求；
- 发现含混词时要求真实例子；
- 给候选解释时标记为专业建议，而不是事实；
- 让用户用自己的话复述；
- 允许用户纠正代理；
- 在关键字段未确认前不进入架构选型；
- 每轮结束时告诉用户已确认、仍不确定、下一步为什么问。

## 七、最终验收标准

### 闭环级验收

- 用户从一句模糊想法开始，经过多轮后能说清对象、用户、真实问题、约束、目标和下一步；
- 每个关键决定都能回溯到用户回答、证据或明确标记的专业建议；
- 用户改变目标后，旧下游不会被静默沿用；
- 当前问题始终来自已重算的 frontier；
- fork 支线不阻塞不依赖它的主线；
- 外部调查前有范围和网络授权；
- 代拟不会被写成用户已确认结论；
- 记录保留可见长论证、修正链、证据和状态；
- 报告问题 ID 与记录节点一一对应；
- 阶段验收检查内容收敛，而不是只检查文件存在。

### 负例必须阻塞

1. 未确认调查范围就启动外部取证；
2. 用户尚未解释技术术语，代理直接选型；
3. 依赖节点未完成就提出下游问题；
4. 用户改变目标后继续使用旧 frontier；
5. fork 结果迟到后静默覆盖已有证据；
6. 代拟未确认却标记为确认采纳；
7. 报告引用不存在的问题或节点；
8. 只有无关待确认文字却放行；
9. 空定位、空状态或占位符被视为合格证据；
10. 长回答被压成结论但 QC 仍报告通过。

## 八、测试限制与未验证项

1. 当前仓库没有 interview runner，因此本次没有执行真实多轮黑盒访谈；涉及模型实际提问顺序、真实 fork 时序和动态回退的结论均标为静态设计缺口或未验证。
2. 脚本化用户场景已由专项代理设计，但没有通过真实技能运行入口执行。
3. 外部 GitHub、X、论文和文章没有作为本次主要证据来源。原因是本任务的核心对象是当前仓库内技能组的结构和门禁，仓库与现有工件已经提供了直接证据；外部平台的当前可访问性也未作为已验证事实写入报告。
4. `agent-reach doctor --json` 显示 GitHub 和 X 当前没有可直接宣称已验证的 active backend，因此本次没有用外部平台内容填补仓库证据缺口。
5. 现有访谈工件只能证明当前工件质量和门禁实现存在问题，不能单独证明所有未来访谈都会产生相同错误。

## 九、交付与修改边界

- 本次只读测试，未修改 `.agents/skills`、`.claude/skills`、项目文档、访谈工件或脚本。
- 本文件是测试报告，不是技能重写稿。
- 报告中的新增/删除/拆分建议尚未实施，属于待确认的优化方案。
- 当前优先顺序应为：先补状态/前沿和范围授权，再补异步 fork，再建立可执行 eval runner，随后改造证据与质检脚本，最后优化非专业用户交互和报告呈现。
