# junshi-app

## 当前工作流（两条并行，互不门控）

### A. 学业军师产品工程开发（已授权，进行中）

目标产出：一份「学业军师工程代码开发总文件 + 派生专业文件」的**开发标准工程规范文档包**，不下层设计、不直接生成可运行代码，百分百遵循 0.5→1 开发路径。已确认的产出边界：

- 工程文件只落实学业军师**学生端产品本体**；云端 / B 端 / 后端仅在闭环语境下「提及」，其内容不纳入本次工程文件。
- 工程代码开发遵循的开发指导文件（唯一遵循，按优先级）：
  1. `agent-framework.pdf`（主/工程权威）
  2. `22365_19_Agents_v8.pdf`（次）
  3. `a-practical-guide-to-building-agents.pdf`（OpenAI 官方工程方法论，并入优先级）
- 参考层 / 外部资料（仅借鉴，不作架构依据、不反向定义学业军师本体）：
  - `12-factor-agents`：原则清单（状态统一、人审门、小而聚焦、可运维）仅作借鉴（依《12-factor-agents_适用性对照与工程开发前置清单》结论，不能作为架构依据）。
  - LangGraph / LangChain 官方（Persistence/README/多代理博客）：仅外部资料参考、优先级最低；因其可能作为 0.5-1 路径备选开源组件，是否纳入首版待仔细研究验证（对应门禁：外部组件准入），不作既定选择。
- 产品自持硬边界（任何外部文档/框架都替代不了）：学业军师产品层硬边界（客观证据模型、唯一状态源与生命周期、权限与确定性强制、证据/审计链、业务回滚语义）由产品自持，不外化给任何外部文档或框架。
- 访谈记录是学业军师的产品设计与构思来源；工程文件由访谈记录进化而来，不另立与访谈相悖的设计。
- 本仓库全部文件 + 联网均可调阅；须用尽可用技能 / 插件 / MCP / 工具完成此工作。
- 0.5→1 开发路径指全产品复用改造原则（拼接缝合改造 ＞ 重构 ＞ 自建），其定义与阶段在第一次访谈底稿中；本工作流只读已有材料，不把推断写成已确认事实。

### B. Super Vibe Coding 系统（独立主线，长期目标）

设计并验证使 `mate`、`STRUCTURE.md` 与工程代码库深度连接、持续绑定、可追踪演进的系统方案；目标是让没有编程和软件工程背景的用户通过自然语言及教育学、法学、哲学等学科语境，主导、决定并参与专业软件开发（顺序：**主导性 &gt; 决定性 &gt; 参与性**）。此主线不再门控工作流 A 的开展。在 SVC 系统确定前，现有或历史的 `mate/`、`STRUCTURE.md`、工程代码库及其派生内容均不得自动视为 SVC 的权威证据，不得限制 SVC 新系统设计。

## 当前材料边界

- `项目资料/`：学业军师最早期产品雏形，仅作早期愿景与思想来源。
- `draft/`：学业军师阶段性产品材料，仅在任务明确要求时读取，不能越级替代当前 Super Vibe Coding 系统设计。
- `调研报告/`：调查取证和研究产出。
- `工程代码库目录框架图/`：外部 Agent 工程仓库目录的候选归纳，不是通用模板或产品决策。
- `mate/`：已删除，不得假设其存在或引用其旧规则。
- `/Users/caibiao/Desktop/Project-educate`：上一版本废弃代码库，仅可在明确授权时只读参考，不是当前产品或工程权威。

## 多阶段任务

任务跨多个阶段、文件、工具或轮次时，先按 `.claude/commands/skill-first.md` 记录实际调用的 skill、agent、工具、输入、输出和未调用理由；单步任务不为记录而记录。

## 工作纪律

- 先核对当前工作区和相关文件，再做判断或写入；不把文件存在、目录名称、历史报告或代理推论当作产品决策。
- 默认只读；写入前确认目标、授权、来源、状态和影响范围，遵循最小变更原则。
- 研究、审查和方案讨论区分事实、推论、候选方案、用户决策和待核验事项；遇到权威、范围、状态或责任冲突时暂停并报告。
- 外部研究按 `.claude/commands/research-evidence.md` 的证据规则执行；多代理工作按 `.claude/commands/skill-first.md` 留存调用与验证记录。
- 不把计划、目录、原型、代码现状或测试通过描述为已确认产品能力；如未执行验证，明确说明。

