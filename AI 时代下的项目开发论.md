# AI 时代下的项目开发论

——以 0.5-1 开发逻辑、哲学判断与 Software 2.0/3.0 为中心

作者：caibiao

## 引言：代码变得容易之后，什么反而更难了

我没有系统学过编程，专业背景是法学。过去，这样的人很难真正参与一个软件项目：即使有产品想法，也很难越过代码、部署、数据库和接口这些门槛。AI coding 改变了这一点。现在，一个人可以借助大语言模型、开源项目和现成工具，把一个想法做成能运行的产品。学业军师产品的开发，就是以 AI coding 为主要手段，借助 AI 辅助实现的。

但我很快发现，能生成代码，不等于知道怎样开发产品。LLM 在代码生成场景中本质上仍是基于上下文的统计补全：它在局部可以产出正确的代码，但随着规模扩大，全局的架构混乱与隐性矛盾会悄悄积累。对于没有技术背景的开发者而言，这些错误是不可见的——你无法发现你不懂的问题，它们只会在后续形成无法拆解的技术债务。

所以，AI 没有让"判断"退出开发。它只是让实现变得更快，也让错误更容易被快速放大。基于这一背景，我采用的是"0.5-1 开发逻辑"——以整合开源项目为基础，借助 AI coding 将产品组装成型，而非从零开始逐行构建代码。本文记录的是我在这一条件下逐渐形成的一套开发思路：以哲学思维先建立判断，以工程思维把判断落地，再借助 AI 完成大量执行工作。它不是一套已经证明普遍有效的方法，而是我为解决自身限制而正在验证的路径。

## 一、0.5-1 开发逻辑：为什么选择从已有结构开始

### 1. LLM 的本质是"补全"，而非"创造"

要回答"为什么选择这样的开发逻辑"，必须先回答一个更根本的问题：LLM 究竟是什么？

LLM 的工作机制，是将输入内容切分为若干语言片段（token）——并非完整词语，而是介于字符与词语之间的子词单元——再基于海量训练数据习得的统计规律，逐步预测下一个片段，反复迭代，最终生成完整输出。

理解这一点，至关重要的结论随之而来：LLM 的本质是"补全"，而非"创造"。它擅长在已有的上下文基础上延伸和填充，擅长在现有模式中寻找最合适的衔接。它的能力，与"给定一个起点，向后推进"高度适配。然而，当起点本身是虚空——即从零行代码起步，要求它构建出一套完整的、具备架构一致性的上万行代码——它所面临的问题规模，远超其核心能力的适配范围。

这不是说 LLM 无法生成大量代码；它可以。问题在于，随着生成规模的扩大，它需要在缺乏真实约束的情况下，依赖自身对"通用代码应该长什么样"的统计印象来填充细节。结果是：代码在局部看起来正确，在整体上却可能充满隐性矛盾、风格不一、架构混乱。对于有技术能力的开发者而言，这些问题可以被识别和修正；对于没有技术背景的开发者而言，这些问题是不可见的——你无法发现你不懂的问题，而正因为不可见，它们不会被修正，只会在产品后续积累成无法拆解的技术债务。

因此，"0.5-1 开发逻辑"是基于对 LLM 能力边界的准确判断而设计的：开发的起点是已有的开源组件和现成模块（0.5），在此基础上，AI 只需负责局部的定制、连结与衔接，而非全局的发明与构建。这极大压缩了 AI 必须"从虚空补全"的空间，将它的工作定格在它最擅长的区域——在已有结构之间找到合理的衔接路径，并将其完整实现。

需要补充的是，这一判断主要适用于代码生成这一具体场景，以及缺乏明确起点和约束时的大规模构建任务。具备链式推理能力的新一代模型在推理层面已有所突破，但在从空白起步构建复杂系统这一任务上，统计补全的根本局限仍然存在。

### 2. 幸存者偏差

至此，还有一个认知陷阱值得单独拆解：幸存者偏差。

当前媒体对 AI coding 的叙事，充斥着"非技术人员用 AI 一周内构建出完整产品"的案例。这些案例是真实的，但它们所呈现的，是一个严重失真的样本。我们看到的，是那些成功者；我们看不到的，是同期启动、中途放弃、产品崩溃或无法维护的大量失败案例——而这些失败，从未进入任何报道，因为失败没有传播价值。

更深层的问题在于：即便那些成功案例是真实的，它们的可复现性也极为有限。成功的 vibe coding 案例，往往隐含了若干前提——开发者能够快速识别 AI 错误输出，能够在出错时调整提示方向，且对产品目标有极度清晰的界定。这些前提条件，在报道中是隐形的。对于真正零技术背景的开发者而言，盲目模仿这一路径，意味着在没有安全网的情况下独自走钢丝——而你甚至无从知晓自己何时已经偏离。

这不是对 AI 能力的悲观，而是对现实条件的清醒认知。选择 0.5-1 的逻辑，正是站在这种清醒认知上做出的主动决策：不是因为做不到 0-1，而是因为 0.5-1 在当前条件下更可靠、更可控、更能真正落地。

## 二、关于这条路的寻找：哲学作为推演工具

### 1. 为什么选择哲学作为推演工具

确认了"走什么路"，下一个问题是"如何找到这条路"。答案并非来自技术文档，而是来自哲学推演。

对于没有技术词汇储备的人而言，直接阅读技术文档或架构设计指南，往往是无效的——语言本身构成了壁垒。哲学不同。哲学处理的是抽象结构，是事物与事物之间关系的本质，它不依赖专业词汇，却能精确描述复杂现实。因此，我选择以哲学逻辑为推演工具，构建一套不依赖技术背景、却能指导技术决策的思维框架。

选择哲学作为工具，还有一个更根本的理由：哲学与工程描述的是同一个现实，只是建立了不同的语言系统。任何学科的底层逻辑都在同一个现实基础上运作，只是表达方式不同——哲学说"透过现象看本质"，工程说"第一性原理"；哲学说"事物的条件"，工程说"触发规则与前置校验"；哲学说"矛盾与取舍"，工程说"优先级与降级方案"。两套语言描述的是同一类现实结构，本质一致，只是面向不同的使用者。

这意味着，哲学语言不只是对工程语言的"替代品"，而是一套**转换语言**：非技术背景的开发者用自己熟悉的哲学语境向 AI 表达意图，AI 将其映射到对应的工程语义上执行。当你说"不要参考表面，聚焦事物的本质"，AI 能识别出这是一个排除表面干扰、追溯核心机制的指令——与"第一性原理分析"触发的是同一种推理路径，只是表达方式不同。这条路径的有效性，依赖于哲学提问与工程规格之间存在可靠的语义对应关系：

| 哲学语境 | 工程映射 | AI 的执行方向 |
| --- | --- | --- |
| 透过现象看本质 | 第一性原理：聚焦核心机制，忽略表面状态 | 不参考表面 UI 描述，聚焦数据结构与业务逻辑 |
| 事物的条件 | 触发规则与前置校验 | 在信息不足时不执行，而是返回缺失条件 |
| 矛盾与取舍 | 优先级与降级方案 | 在目标冲突时作出选择，而不是同时满足所有要求 |
| 事物的关系 | 数据流与依赖图 | 识别模块间的影响关系，而不是孤立地实现单个功能 |

因此，哲学在这里不只是"我能用的语言"，更是"有效的沟通路径"：它不依赖技术词汇，却能精确触达工程所需的判断结构。

### 2. 产品如人：从结构到意识

这套框架的核心类比是：将一个产品看作一个人。

人由细胞构成器官，器官协作形成系统，系统整合为完整的躯体。这是一条从微观到宏观的构建逻辑。映射到产品开发中：代码是细胞，功能模块是器官，各模块的协作关系是系统，整合运转的产品是躯体。

这个类比的力量，不只在于它的形象性，而在于它直接指明了操作路径。生物学中，器官移植优于从细胞培育——因为从细胞培育一个成熟器官，需要投入远超最终价值的时间与精力，且充满不可控的失败风险。产品开发中同理：整合已有的成熟开源模块，远比从代码层逐行构建更高效、更可靠。这正是 0.5-1 逻辑的哲学基础——它不只是一个实用技巧，而是对"如何有效构建复杂系统"这一普遍规律的具体应用。两套逻辑，在不同层次上，指向同一个结论。

那么，产品这个"人"区别于其他事物的关键是什么？是意识。

意识赋予了人感知外部世界的能力，使人能够理解自身与他人的关系，并据此调整自己的外在呈现。人因为有意识，才懂得根据情境穿衣遮蔽，将内在的自我以适合外部环境的方式呈现出来。这对应的，是产品的前端——它不是装饰，而是产品感知用户、并将自身内部功能以可理解的形式呈现给外部世界的层次。没有前端的产品，如同意识缺席的躯体：内部机能或许完整，但无法与外部世界发生有效联结，也就无从产生真实的价值。

### 3. 人与 AI 的分工：AI 负责实现，人负责定义

从这条逻辑链继续推演，就能自然地抵达"人与 AI 如何分工"的答案。

AI 的实质能力是：在已有训练数据的空间内，找到最合理的补全路径。它的"创造"，本质上是对已见内容的重新排列与组合；它的"想象"，受限于训练分布的边界。这意味着，在训练数据充分覆盖的领域，AI 的执行精度极高；但在需要真正超越已知模式、生成前所未有的概念或方向时，AI 所能做的，只是以统计概率逼近一个它从未真正见过的目标。这种逼近，在执行层足够有效，在创造层却是根本性的不足。

人的创造力则不同。人可以在没有先例的情况下，凭借对问题本质的理解，生成全新的概念框架。人知道自己想解决什么问题，知道什么样的体验是有价值的，知道在面对矛盾时应当取舍哪一侧。这些判断，无法从统计规律中推导，只能来自对现实世界的直接体验与思考。AI 的创造力是训练出来的，人的创造力是活出来的——这是两者最本质的差异，也是分工不可颠倒的根本原因。

这两种能力的差异，决定了唯一合理的分工：AI 负责实现，人负责定义。AI 将已知的模式组合为可运行的代码，人决定要运行什么、为谁运行、以及运行背后的价值判断。两者独立，却相互依存——脱离人的定向，AI 的执行是无目标的漂移；脱离 AI 的实现，人的创意是无法落地的构想。

综合而言，这套方法论的内核是：以对 LLM 本质的准确认知为基础，以哲学类比为思维工具，以人机能力的真实差异为分工依据，构建出一条非技术背景开发者在 AI 时代能够真正走通的产品开发路径。这条路不依赖幸运，不依赖天赋，而依赖于清醒的认知、诚实的自我评估，以及对工具性质的深刻理解。不跟风，不自欺，各取所长，协作落地——这，是我在 AI 时代找到的开发之道。

## 三、关于未来软件的判断：从 Software 1.0 到 AI 软件

这里我以 Andrej Karpathy 在 2017 年 11 月 12 日所发表的文章《Software 2.0》所提出的观点与概念进行阐述。

源地址：[Software 2.0](https://karpathy.medium.com/software-2-0-a64152b37c35)

### 1. 从 Software 1.0 到 Software 2.0

Karpathy 对 Software 1.0 与 Software 2.0 的区分，首先改变的是我们对"程序由谁编写"的理解。

Software 1.0 是人直接写出的程序。开发者使用 Python、C++ 等编程语言，把条件、规则和执行路径逐条交代给计算机。用户进行某个操作，程序沿着预先写好的路径运行，再返回一个确定结果。无论软件多么复杂，其能力边界都已经被开发者写进代码之中。

Software 2.0 则不同。人不再负责写出全部规则，而是提供数据、目标和基本结构，再由优化过程寻找实现目标的程序。Karpathy 将训练过程类比为编译：数据集和神经网络架构相当于源代码，训练完成后的模型权重相当于最终生成的程序。人定义什么结果是好的，机器负责从大量可能性中寻找一条可行路径。

需要说明的是，Karpathy 在原文中所说的 Software 2.0，主要指由数据训练出来的神经网络程序。我在此基础上进一步借用这个概念，将其延伸到未来的软件产品形态。这里所说的"AI 软件"，不是指使用 AI coding 开发出来的软件，而是指 AI 已经进入产品内部，成为软件理解需求、作出判断和组织功能的运行中枢。

### 2. 判断 AI 软件的标准：拿掉 AI 之后

这两者有本质区别。假设一个记账软件的全部页面、按钮和执行流程都由 Claude 编写，但软件运行之后仍然只能按照预设规则记录和统计账目，那么它依然属于 Software 1.0。AI 只是参与了生产过程，并没有改变产品本身的运行方式。反过来，如果一个软件能够理解用户提出的目标，根据用户的具体情况制定方案，自主调用工具，并依据执行结果继续调整，那么即使它的前端仍然是熟悉的对话框、表格和按钮，它的内核也已经发生变化。

因此，判断一个产品是不是 AI 软件，不能看它是否由 AI 开发，也不能只看它有没有接入一个聊天窗口。真正的判断标准是：拿掉 AI 之后，这个产品原本的核心能力是否仍然存在。如果拿掉 AI，产品依旧可以依靠原来的固定流程继续运行，那么 AI 只是附加功能；如果拿掉 AI，产品就失去了理解、决策和组织能力，不再是原来的产品，那么 AI 才构成了它的内核。

### 3. 功能手机到智能手机：能力组织方式的两次变化

功能手机到智能手机的迭代，可以帮助理解这种变化。

功能手机提供的是一组由厂商预先安装的固定功能。用户可以打电话、发短信、设置闹钟，但只能在既定菜单中选择，很难改变手机原有的能力边界。智能手机到来之后，手机变成了一个可以安装应用、组合服务和持续扩展的通用平台。同一台设备，在不同使用者手中可以呈现出完全不同的内容和用途。

这一类比并不是说智能手机本身就是 Software 2.0，而是说明两次变化具有相似的方向：功能手机把能力固化在出厂设置中，传统软件把能力固化在开发者预先写好的功能与流程中；智能手机允许用户自由扩展能力，AI 软件则更进一步，它开始根据用户的目标主动选择、组合和执行这些能力。

### 4. 传统软件与 AI 软件的运行方式

传统软件的基本运行方式是：

> 用户选择功能，软件执行预设流程，返回固定类型的结果。

AI 软件的运行方式则会变成：

> 用户表达目标，AI 理解当前情境，将目标拆解为任务，调用相应工具执行，再根据结果继续修正下一步行动。

两者面对用户的界面或许没有明显区别，内部逻辑却完全不同。Software 1.0 要求开发者尽可能提前预测用户会遇到什么问题，并为每一种情况写好处理路径。AI 软件不再要求开发者穷尽所有路径。开发者需要提供可靠的能力模块、数据、权限和边界，AI 则在实际运行时根据具体情境组织它们。

如果继续沿用前文"产品如人"的类比，那么代码和功能模块仍然是细胞与器官，数据库承担记忆的作用，前端是产品接触外部世界的感官和表达层，而 AI 开始成为负责理解、判断和调度的认知中枢。

这并不意味着传统代码会消失。人的意识不能代替骨骼、器官和神经，AI 同样不能代替数据库、身份认证、权限控制和确定性的业务规则。对于支付、数据安全、隐私权限等不能容忍模糊判断的环节，仍然需要 Software 1.0 提供稳定边界。AI 所改变的，是这些模块的组织方式：过去由开发者提前写死模块之间的调用顺序，未来则由 AI 在规则允许的范围内，根据用户目标动态决定调用什么、何时调用以及如何处理结果。

### 5. AI 软件的定义与基本单位

由此，我对 AI 软件作出如下定义：

> AI 软件，是以 AI 模型作为运行时的理解、决策和调度中枢，以传统软件模块作为可调用能力，以用户数据和执行反馈修正行为，并由确定性规则约束安全边界的软件系统。

在这个定义下，软件的基本单位也会发生变化。过去，一个软件由若干固定功能组成，产品经理通过增加页面、按钮和流程扩展产品。未来，一个 AI 软件更像是由若干可被调用的能力组成：读取信息、搜索资料、修改文件、生成内容、安排日程、执行代码。用户不必知道每项能力位于哪个菜单，也不必亲自规定它们的调用顺序。用户只需要描述目标，AI 负责把分散的能力组织成一条可以执行的路径。

### 6. Agent 产品：形态的雏形

Codex、Claude Code 等 Agent 产品已经呈现出这种形态。它们表面上仍然保留对话框、终端或代码编辑器，真正的变化却发生在内部。用户不再逐条命令软件修改哪一行代码，而是提出"修复这个问题""理解这个项目"或"完成这个功能"。Agent 随后读取上下文、拆解任务、调用工具、检查结果，并在发现问题后调整行动。界面仍然存在，但它已经不再是功能菜单，而是用户表达目标、监督过程和确认结果的入口。

### 7. "0.5-1 开发逻辑"的第二层含义

这也使我此前提出的"0.5-1 开发逻辑"获得了另一层含义。

在当前的产品开发中，0.5 是已有的开源项目和成熟模块，AI 帮助我完成连接与定制，最终形成一个完整产品。到了 AI 软件阶段，这种逻辑不只存在于开发过程，也会进入产品的运行过程。每一个成熟模块都是一个已经完成的"器官"，AI 不必重新制造它，只需要理解它的能力和边界，在具体任务中完成调度。过去是开发者在产品发布之前把 0.5 组装成 1；未来则是 AI 根据不同用户的目标，在每一次运行中重新把若干个 0.5 组织成当下所需要的 1。

### 8. 学业军师的两种形态

以"学业军师"为例，Software 1.0 形态的学习软件可以提供课程表、任务清单、成绩统计和复习提醒。开发者提前设计好页面与规则，学生按照规定的步骤录入信息，再从固定模块中获取结果。它能够管理信息，却无法真正理解每个学生面对的处境。

AI 软件形态的学业军师则应当围绕目标运行。学生可以直接说明："我两周后参加考试，白天需要上课，数学基础较差，但希望本学期不挂科。"系统需要结合课程安排、剩余时间、历史完成情况和学习材料，判断问题的优先级，制定可以执行的计划。当学生没有按时完成任务时，系统也不只是机械地弹出提醒，而是重新评估时间和目标，调整后续安排。

课程表、任务列表和统计页面可能仍然存在，甚至在外观上与传统软件没有太大区别。但这些页面中的内容不再完全由开发者预设，而是由 AI 根据用户的真实状态动态生成。前端从"功能入口"逐渐变成"行动结果与控制边界的呈现层"，软件也从一个等待操作的工具，转变为能够持续参与目标实现的协作者。

### 9. 未来软件的竞争与变革

未来软件之间的竞争，也将不再只是功能数量的竞争。两个产品可能调用相同的模型，拥有相似的页面和工具，真正拉开差距的会是：谁更理解自己的用户，谁能够获得更准确的上下文，谁能把现有能力组织成更可靠的行动，以及谁能在执行错误之前守住权限与安全边界。

因此，我所判断的软件变革，并不是所有界面都会消失，也不是每一行传统代码都会被神经网络取代。变化发生在软件的控制中心。Software 1.0 由开发者预先写好的流程驱动，AI 软件则由用户目标和实际情境驱动。前端仍然负责连接人，传统模块仍然负责稳定执行，但理解目标、选择路径和调整行动的职责，会逐步转移到 AI。

从功能手机到智能手机，改变的是设备承载功能的方式；从 Software 1.0 到 AI 软件，改变的则是软件组织和执行功能的方式。过去，人必须学习软件的菜单和规则，才能把自己的目标翻译成一连串操作。未来，软件将反过来理解人的目标，再主动组织内部能力完成任务。

这就是我对未来软件形态的判断：AI 不会只作为一项新功能被安装在传统软件之上，它会逐渐进入软件的内核，成为组织数据、工具与行动的中心。届时，软件留给人的可能仍是熟悉的窗口、按钮和对话框，但运行在这些界面背后的，已经不再是一条固定的程序路径，而是一个能够理解、判断并采取行动的智能系统。

### 10. 与 Software 3.0 的印证

值得注意的是，Karpathy 在 2025 年 6 月于 Y Combinator AI Startup School 的演讲《Software Is Changing (Again)》中，正式提出了 **Software 3.0** 的概念：以自然语言作为新的编程接口，LLM 成为一种新型计算机，软件开发进入第三个范式。他将 Software 1.0 定义为传统逻辑代码，Software 2.0 定义为由数据训练出的神经网络程序，Software 3.0 则是由 LLM 驱动、以自然语言作为编程层的新一代软件。

这一框架与本文在 Software 2.0 基础上独立推演出的"AI 软件"判断高度一致：AI 成为软件的理解与调度中枢，传统代码模块退为可调用的能力单元，用户通过目标而非操作与软件交互。本文的判断早于该演讲独立形成，这种印证说明判断方向具有一定的可靠性，并非孤立的个人推测。

## 四、从全局判断到局部实现

### 1. 什么是全局，什么是局部

将开发方式、软件形态这类全局性问题说明之后，目光还应当进入产品内部。一个产品并不只由宏观方向决定，它最终能否稳定运行，取决于无数局部如何被设计、连接、约束与验证。

我所理解的产品全局，主要包括产品方向、核心功能与产品形态。产品方向回答的是"为谁解决什么问题"；核心功能回答的是"产品依靠什么能力完成这件事"；产品形态则回答"用户如何接触、理解和使用这些能力"。它们并非永远不能改变，但在一个产品阶段内，应当保持相对稳定。否则，局部开发就会失去参照：今天补一个功能，明天换一个方向，最后只会得到一组彼此无关的页面和模块，而不是一个完整产品。

产品局部，则是为了实现全局目标而存在的具体部分，例如一个功能模块、一段工作流、一项权限设计、一个数据结构，或一次用户交互。局部还应区分为关键局部与普通局部。

关键局部不只是"删掉后软件是否还能打开"，还包括它是否影响产品的核心循环、数据准确性、用户安全与整体体验。对于学业军师而言，学习计划的生成与调整、用户学习状态的记录、任务执行后的反馈机制，都属于关键局部；页面颜色、提示文案或非核心展示组件，则更接近普通局部。

前文提出过一个判断 AI 是否构成产品内核的方法：拿掉 AI 后，产品是否仍能维持原来的核心能力。这个判断同样可以用于识别关键局部。若移除某个局部后，产品只损失一项边缘体验，它通常是普通局部；若移除后，产品失去理解用户、形成方案、维持数据可信度或执行核心任务的能力，它就是关键局部。局部的重要性，不取决于它写了多少代码，而取决于它是否支撑产品作为一个整体继续成立。

### 2. 如何实现局部：让 AI 基于证据与约束工作

局部界定清楚之后，真正的问题才出现：如何实现局部？

我仍然沿用前文"将产品比作人"的思路，但这一次不再停留在结构层面。代码、模块、数据和界面，可以分别对应细胞、器官、记忆与感官；而真正决定产品能否根据外部变化作出反应的，是"意识"这一层。对于 AI 软件而言，AI 模型承担了理解、判断和推理的职责。但模型的判断不能只依赖自身的概率补全，否则它很容易在信息不足时生成看似合理、实际错误的内容。

这正是 LLM 幻觉的来源。它并不是因为模型"故意说错"，也不能简单归结为唯心论。LLM 的基本工作方式，是基于已有上下文预测最可能出现的内容。当上下文缺少事实、规则、项目状态或验证结果时，模型仍然会继续生成；它给出的往往是统计上合理的答案，却未必是现实中成立的答案。

如果借用哲学语言来理解，问题不在于模型拥有意识，而在于它的意识缺少足够的"物质基础"。这里的物质，不是抽象概念，而是能够被检查的证据：真实的项目代码、产品需求、用户数据、数据库状态、工具执行结果、测试报告、权限边界和验收标准。模型必须先接触这些事实，再进行判断和行动。这样做不能消除所有错误，却能把模型从"凭空补全"拉回"基于证据推理"。

这也是各种 Engineering 范式逐渐出现的原因。它们并不是彼此完全替代的线性阶段，而是在解决同一个问题的不同层面。以下几个方向是行业在不同层次上探索的路径，命名并非完全统一，但它们共同指向同一个目标：让 AI 不再凭空生成，而是在有证据和约束的条件下推理与执行。

Prompt Engineering 解决的是任务如何被明确表达；Context Engineering 解决的是模型在当前任务中应当看到哪些事实；Harness Engineering 则为模型配置工具、权限、规则、测试和反馈，使其不只是回答问题，而是在受约束的环境中完成任务。Loop Engineering 要求模型在执行后检查结果、发现错误并重新尝试；Graph Engineering 则将复杂任务拆分为有顺序、有状态、有分支条件的工作流。

对于 coding agent 而言，harness 的作用尤其直接：它要求模型先阅读项目规则和已有代码，再修改最小必要范围；要求它理解依赖关系和权限边界；要求它在修改后执行测试、检查报错，并以验收标准判断任务是否完成。模型不再只是"写代码的人"，而是在项目环境中接受事实约束的执行者。

因此，产品局部的开发不应只是给 coding agent 一句模糊指令，例如"帮我实现一个学习计划功能"。更可靠的做法是先明确该局部服务于什么全局目标，哪些输入是真实可信的，哪些操作不能越界，完成后以什么结果作为验收依据。需求、上下文、约束、工具权限与测试结果，共同构成局部开发的现实基础。没有这些基础，AI 只能猜测；有了这些基础，它才有条件作出可验证的实现。

### 3. 哲学思维：工程开始之前的判断

但对我而言，工程约束仍然只是实现层。更早的一步，是哲学判断。

对没有技术背景的开发者而言，引用哲学思维开发产品，核心不在于为产品套上一层抽象话语，而在于先建立判断，再进入实现。

工程思维通常从"如何实现"开始：选择什么技术、设计什么接口、建立什么数据结构、怎样把功能连接起来。这当然不可缺少，但它默认开发者已经知道自己要实现什么，以及为什么要这样实现。对非技术背景者而言，真正困难的往往恰恰在此：不是不会写某一段代码，而是不知道一个功能是否必要，不知道不同功能之间应当如何取舍，也不知道 AI 给出的"看起来能用"的方案，是否真的解决了用户的问题。

哲学思维提供的正是这一层判断坐标。它要求我不急于进入"怎么做"，而是先从事物的目的、结构、关系、条件和矛盾出发，建立自己对产品的理解。

产品的目的，回答的是它究竟要解决什么问题。学业军师不是为了生成更多学习计划，而是帮助一个处于具体学习处境中的人，把模糊、焦虑甚至相互冲突的目标，转化为当下可以执行的安排。如果脱离这个目的，功能再多，也只是把待办事项、课程表和数据图表堆在一起。

产品的结构，回答的是哪些部分构成核心，哪些部分可以后置。用户状态、目标、任务计划、执行反馈与调整机制，构成学业军师的主干；主题颜色、动画效果或边缘页面则不应挤占同等的开发优先级。结构判断帮助我把有限的时间和 AI 的执行能力放在真正决定产品价值的部分。

产品的关系，回答的是模块、用户与信息之间如何相互影响。一个学习计划不能孤立存在，它必须和考试时间、课程难度、用户基础、可支配时间以及历史完成情况发生关系。AI 软件的价值，也不在于某一个模型回答得多漂亮，而在于它能否正确处理这些关系，并在信息变化后重新作出判断。

产品的条件，回答的是一个功能在什么前提下才应当运行。系统不能只因为用户说"帮我制定计划"，就立刻给出完整方案；它还需要知道用户有哪些课程、距离考试还有多久、每天能够投入多少时间，以及哪些数据可靠、哪些数据缺失。条件意识能够防止 AI 在信息不足时用流畅的语言掩盖不确定性。

产品的矛盾，回答的是发生冲突时应当如何选择。用户既想提高成绩，又没有足够时间；既想把所有内容学完，又需要避免计划过载；既希望系统主动，又不愿意失去对结果的控制。这些不是产品设计中的例外，而是产品必须处理的现实。哲学思维要求开发者先识别矛盾，再确定优先级和边界；工程实现只是把这种取舍写进规则、工作流和交互之中。

因此，哲学在这里不是工程的替代品，而是工程开始之前的判断能力。它先把"应当做什么"说清楚，再把问题翻译为工程语言。这一翻译过程，可以整理为如下对照：

| 先问什么 | 在产品中意味着什么 | 最后要翻译成什么 |
| --- | --- | --- |
| 目的 | 这个产品到底替谁解决什么问题？ | 产品目标、用户任务、验收结果 |
| 结构 | 哪些能力是主干，哪些可以晚点做？ | 模块边界、优先级、最小范围 |
| 关系 | 用户、数据、模块和行为怎样互相影响？ | 数据流、接口、状态与工作流 |
| 条件 | 缺什么信息时，系统不该擅自行动？ | 输入要求、触发规则、权限与异常处理 |
| 矛盾 | 目标冲突时，产品该牺牲什么、保留什么？ | 决策规则、降级方案、人工确认点 |

这张表不是在装点门面，而是开发前需要走过的顺序：目的被翻译为产品目标，结构被翻译为模块边界，关系被翻译为数据流与工作流，条件被翻译为输入要求和触发规则，矛盾被翻译为优先级、异常处理与人工确认机制。它把"我觉得应该做一个功能"变成一组可以回答的问题。

只有完成这一步，AI 与工具才知道该执行什么。否则，AI 能够写出代码，却无法替开发者决定哪些代码值得被写。

### 4. 辩证法与方法论：把判断翻译为规则

局部开发还有另一层问题：功能本身如何被设计。这里可以引入哲学中的辩证法与方法论，但不能把哲学术语直接贴在产品上。真正有价值的做法，是把辩证思维转化为软件可以执行的判断规则。

所谓辩证，不是让 AI 输出更多复杂分析，而是要求它在作出决定前看到事物之间的条件、关系和矛盾。例如，学业军师不能只根据"距离考试还有两周"生成计划，还需要同时判断学生现有基础、可支配时间、课程难度、已完成任务和目标优先级。目标之间发生冲突时，系统也不能把所有任务都排满，而应当作出取舍：在"覆盖更多内容"和"保证计划可执行"之间，找到当前条件下更合理的方案。

这种方法论可以进入 AI 软件的各个层次。Skill 用来固化稳定的方法与工具边界；Workflow 用来规定任务的顺序、依赖关系和结束条件；Memory 用来保存真正影响后续判断的用户状态、历史决策和长期目标；框架则负责划分规划、执行、验证与人工确认的职责。无论它们是核心能力还是脚手架，都不应只追求"让 AI 更聪明"，而应追求"让 AI 在具体情境中作出更有依据的判断"。

### 5. 回到"产品如人"：哲学与工程的分层

回到"产品如人"的类比：工程解决的是器官如何被制造、连接和检验；哲学解决的是这个人应当为何而存在、各个器官之间应当如何协作，以及在冲突中应当优先保全什么。

代码可以由 AI 帮助生成，组件可以从开源项目中复用，测试也可以由工具执行，但"这个局部是否必要""它服务于哪个目标""它与其他局部发生矛盾时应当如何取舍"，这些问题不会因为 AI 能写代码而自动消失。

这正是我选择以哲学思维参与产品开发的原因。对于具备工程背景的人来说，工程知识可以帮助他们从架构、接口、数据结构和性能约束出发拆解问题；而对于没有技术背景的人，若一开始就试图用工程术语理解一切，往往会被陌生概念淹没，最后只能依赖 AI 给出的表面答案。哲学提供了另一条可以进入产品开发的路径：先从事物的目的、结构、关系、条件和矛盾出发，建立自己对产品的判断，再将这种判断翻译为工程语言，交给 AI 与工具执行。

在我的方法中，哲学思维是第一层，工程思维是第二层。

哲学思维负责定义产品要解决的问题，区分全局与局部，判断哪些能力是核心、哪些只是附属，识别用户目标之间的冲突，并确定人、AI 和确定性规则各自应当承担的责任。工程思维负责将这些判断落实为需求文档、模块边界、数据结构、工作流、权限设计、测试和验收标准。前者回答"为什么这样做、应该做什么、做到什么程度"；后者回答"如何把它做出来，并确认它确实做对了"。

这并不是用哲学替代工程。没有工程约束，哲学判断只能停留在概念层；没有哲学判断，工程实现则容易沦为功能的无序堆叠。二者的关系不是并列的，而是前后关系：哲学为工程提供方向、边界和判断标准，工程把哲学中的判断变成可以运行、可以观察、可以修正的产品。

## 结语：各归其位

引言提出了一个问题：AI 没有让判断退出开发，只是让实现变得更快，也让错误更容易被快速放大——对于非技术背景者而言，这些错误是不可见的，只会悄悄积累成技术债务。

这套方法试图回答的，正是这个问题：如何让错误在没有技术知识的情况下也能被看见、被控制。

三个部分构成一条依赖链，而不是三条并行建议。

哲学判断是起点。它先从目的、结构、关系、条件和矛盾出发，建立对产品的理解。哲学判断的输出是清晰的方向——知道要建什么、为谁建、哪些不能错。有了这个方向，错误是否存在才变得可以判断，因为你已经知道"应当是什么"。

0.5-1 的整合逻辑是第二步，它依赖哲学层已确认的方向。在方向明确之后，选择从已有的开源模块出发，而不是从空白处构建。这样做让 AI 的工作从"凭空发明"变成"在有边界的结构之间完成衔接"——错误发生在整合边界，范围有限，而不是弥漫在整个未知的架构中。

工程规范和 harness 约束是第三步，它依赖前两步提供的目标和结构。有了方向、有了起点，AI 才能在真实代码、上下文、权限和验收标准的约束下执行，而不是只凭一句模糊指令生成结果。有了这些约束，执行结果才能被验证——你知道它应当做什么，它也必须证明自己做到了。

三层之间是输入输出关系：哲学提供目标，0.5-1 提供结构，harness 提供约束——缺少任何一层，后面的层就失去了参照。

这套方法真正改变的，是非技术背景者在开发中的位置。他们不必假装自己是工程师，也不必完全依赖 AI 的判断——他们可以做自己实际上最擅长的事：对问题保持诚实，对价值作出取舍，对边界保持克制。这三件事不需要代码知识，却是任何软件产品能否真正成立的前提条件，也是 AI 无法替代的部分。

因此，我正在实践的并非简单地"让 AI 替我写代码"。我尝试验证的是：

> 先用哲学理解产品，再用工程拆解产品，最后借助 AI 将经过拆解和约束的判断实现为产品。

这条路径是串行依赖的：哲学判断决定了工程应当拆解什么，工程规格决定了 AI 应当在哪个边界内执行。哲学的位是工程开始之前，不是贴在工程之上的装饰语言；工程的位是 AI 执行之前，把判断翻译为可以约束行动的规格；AI 的位是在规格和约束之内，承担大量实现工作，而不是替代前两层。

只有三者各归其位，AI 才会成为开发的杠杆，而不是制造更多表面功能的工具。

## 参考

- Andrej Karpathy, [Software 2.0](https://karpathy.medium.com/software-2-0-a64152b37c35), 2017-11-11。
- Andrej Karpathy, [Software Is Changing (Again)](https://www.ycombinator.com/library/MW-andrej-karpathy-software-is-changing-again), Y Combinator AI Startup School, 2025-06-18。
