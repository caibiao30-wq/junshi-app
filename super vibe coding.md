# Super Vibe Coding 系统：设计白皮书

> **性质：** 官方正式设计方案与进度白皮书，基于本项目"Super Vibe Coding"系统设计讨论的实际决定与取证。本文件是把讨论结论系统性整合为可执行、可复核的设计依据，**不是最终冻结的系统规格**。
> **状态：** 设计推进中（2026-09-06）。
> **置顶目标：** 本系统的核心目标是让没有编程和软件工程背景的用户，通过产品自然语言（含教育学、法学、哲学等学科语境），主导、决定并参与专业软件开发。**在本系统完成并具备打包为 skills 的条件之前，不进入具体学业军师产品的科学研发工作。**

---

## 目录

- 一、定位与目标
- 二、核心原则：主导 > 决定 > 参与
- 三、第一性原理：产品源逻辑与跨域同源
- 四、三大核心资产及其关系
- 五、三类核心资产
- 六、结构真源文件：工程代码库骨架与 mate 对位
- 七、通用 Agent 工程代码库基础模板
- 八、基于调研的工程能力域
- 九、DeepTutor 底座评估
- 十、学业军师产品背景（作为系统首例验证对象）
- 十一、交付形态：打包为面向非技术用户的 skills
- 十二、当前阶段清单与进度
- 十三、未决问题与前沿
- 十四、重要防误用边界

---

## 一、定位与目标

### 1.1 一句话定位

> 设计一个使 `mate`、`STRUCTURE.md` 与工程代码库**深度连接、持续绑定、可追踪演进**的 Vibe Coding 系统方案，使没有编程和软件工程背景的用户能够通过产品自然语言主导、决定和参与专业软件开发。

### 1.2 系统要解决的根本问题

传统 Vibe Coding 依赖即时对话，需求、决策和上下文容易分散，用户仍需不断向 Agent 解释技术细节。本系统把非专业表达沉淀为**持续有效、可追踪、可演进**的产品元文档，再经由连接层映射到工程代码库，由 Coding Agent 完成专业转换与工程落地。

### 1.3 重要澄清："Vibe Coding 的 Super 版"是内部比喻

- 它是**内部讨论**的抽象比喻，不是对外宣传语，也不是已经由实验验证的产品优势。
- 正式目标不是"提升 Vibe Coding 的酷炫程度"，而是让非工程背景用户有能力、有依据地主导复杂软件研发。
- 系统的通用性（可复用于各 coding 领域）和相对传统 Vibe Coding 的优势，需通过后续垂直切片与对照实验验证，当前不视为已证实事实。

### 1.4 交付形态（最终目标）

系统设计完成、验证跑通后，**打包成一组面向非编程背景开发者的 skills**，使其他非技术背景的用户也能通过本系统主导软件开发。

---

## 二、核心原则：主导 > 决定 > 参与

系统有三个目标，且有**严格优先级顺序**：

| 优先级 | 维度 | 含义 |
|---|---|---|
| **第一要义** | **主导性** | 用户握着指挥棒，决定系统往哪走、边界在哪、何时验收、何时判定完成。 |
| **第二要义** | **决定性** | 用户拍板关键产品规则、取舍、风险接受度、对外承诺与边界。 |
| **第三要义** | **参与性** | 用户以合适粒度介入工程相关决策（是前两者的自然结果，不是独立主目标）。 |

**评判标准：** 系统是否让非编程用户从始至终**主导和决定**产品的方向与边界，而非仅仅参与某几个环节。任何"参与性"设计（如让用户填表单、勾选项）如果削弱了主导权，就是倒退，应当否决。

---

## 三、第一性原理：产品源逻辑与跨域同源

### 3.1 核心命题

> 工程代码库所沿用的语言体系、概念体系、逻辑关系和行为规则，与哲学、法学、教育学以及日常自然语言中的表达，**具有同一个更底层的逻辑根源**。不同学科和工程只是面对不同对象、目的和语境时，对同一底层产品源逻辑进行不同形式的表达。

工程代码不是自然语言之外另一种完全异质的东西，而是同一产品逻辑在工程语境中的一种表达形态。

### 3.2 "底层源代码"的两种含义

- **解释层含义**：帮助非技术用户理解"法律、哲学、教育、产品、架构和代码是在表达同一个产品逻辑的不同外形"。
- **治理层含义**：只有被拆解、编号、映射、审查并验证的部分，才可以被当作工程上可追踪的源逻辑。

### 3.3 重要边界（防止被误用）

"同一种底逻辑"**不意味着**：

- 不同领域文本内容逐字相同；
- 每个工程概念都能在普通语言中找到一一对应；
- 每个哲学判断都能直接转为代码；
- 代码一定完整表达了产品意图；
- Agent 自动转换不会遗漏、误解或引入新决定。

同源要求的是：**在明确范围内，关键的对象、关系、前提、行为、边界和结果被保持**。验证的对象不是"两个文件逐字相同"，而是该范围内核心语义是否保持。

### 3.4 "底层源代码"不是

- 隐藏在自然语言背后的实际程序；
- 已写好的、可直接编译的抽象需求文件；
- 由 Agent 自动提取的中间表示；
- 仅用于建立追踪 ID 的管理对象。

### 3.5 三种工程表达的区分

```text
工程代码表达
├── 对产品源逻辑的忠实承载
├── 为可执行性补充的工程具体化
└── 工程自身独立产生的技术选择（不得冒充产品源逻辑，需单独标记为工程决策）
```

### 3.6 正式名词建议

- "底层源代码"与"Vibe Coding Super 版"保留为**内部解释性比喻**；
- 正式设计概念使用 **"产品底层源逻辑" / "Super Vibe Coding 系统"**。

---

## 四、三大核心资产及其关系

### 4.1 三个角色（不局限于字面目录名）

```text
产品底层源逻辑（跨域同源）
       ↕
mate：用户以多学科自然语言对源逻辑的正式表达（用户确认的定稿层）
       ↕
STRUCTURE.md：记录不同表达和工程承载之间的同源、边界与演进（连接治理层）
       ↕
工程代码库：把同一源逻辑变成可运行系统的工程表达与现实层
```

### 4.2 三者的权威范围

- `mate` 对**产品意图、产品规则、产品承诺**拥有权威；
- 工程代码库对**实际实现与运行事实**拥有权威；
- `STRUCTURE.md` 对**这些内容之间如何被连接、以什么版本连接、是否经过验证**拥有权威；
- **没有任何一个文件可以独占整个底层源逻辑的全部内容。**

### 4.3 核心注意（伪证据源）

> **在系统设计确定之前，现有或历史的 `mate/`、`STRUCTURE.md`、工程代码库及其派生内容，均不得自动视为权威证据，不得限制新系统设计方向。**

本项目当前把所有既有文件当作完成系统的假象，因此现有 `mate/`、`STRUCTURE.md`、历史工程代码库上下文是**参考输入**，不是天生的权威源。它们的权威必须由新系统建立后，据新关系重新认定。

---

## 五、三类核心资产

| 资产 | 主要职责 | 不承担的职责 |
|---|---|---|
| `mate` | 保存用户已确认的产品源逻辑、规则、边界、理由、验收目标（多学科正式表达） | 不执行代码，不替代工程设计 |
| `STRUCTURE.md` | 记录产品语义、派生文件、代码对象、版本、责任、审批和验证之间的映射 | 不替代源文档、代码、测试或决策记录 |
| 工程代码库、运行平台与证据资产 | 实现、运行、测试、部署、观测并记录实际事实 | 不未经批准改写产品意图 |

---

## 六、结构真源文件：工程代码库骨架与 mate 对位

### 6.1 需求来源

必须依据产品性质，先广义上初步确认工程代码库的目录布局，反推 `mate` 需要存在哪些内容；再依据产品研发深入，不断迭代增加工程代码库与 `mate`。

因此必须存在一个专门文件，用于解决工程代码库和 `mate` 的目录布局、结构关系。**它不是一次确定，而是随产品进度和研发深入持续升级。**

### 6.2 结构真源文件的性质

- 是系统的 **"结构契约层"**（structural contract），独立于 `mate` 正文、独立于工程代码文件；
- 重点回答：工程代码库有哪些能力域 → 每个工程域需要从 mate 获得哪些产品定义 → mate 应存在哪些编章模块；
- 它管 **结构与对位**；语义级映射（每条规则 ↔ 具体代码对象）由连接层单独承载，两者分层，不分先后失序。

### 6.3 设计要点

| 要点 | 说明 |
|---|---|
| 身份 | 结构契约层，可视为"前置的 STRUCTURE.md 角色"，但内容从零确立 |
| 迭代 | 维护"当前结构基线 + 变更记录 + 待演进项"，随垂直切片、测试、运行事实更新 |
| 与语义映射关系 | 结构文件管骨架/对位；语义级映射是运行期追踪层，由连接层承接 |

### 6.4 关键校验（防止照搬成熟框架）

结构真源文件里每条工程域都必须能回答：

> 这个工程域，是为了承载产品性质的哪一条？它需要 mate 提供什么产品定义？

答不上来的工程域 = 搁置，不进骨架。**这才是"产品性质驱动"的落地校验，而不是把成熟框架目录抄一遍。**

---

## 七、通用 Agent 工程代码库基础模板

### 7.1 顶层结论

> 必须先存在一份或多份**通用工程代码库基础模板**（仅指 Agent 产品的工程代码库模板），并把产品性质实例化到模板之上，才能应对不同产品、不同情况下的多样化需求。

### 7.2 模板与"照搬成熟框架"的区别

- 模板是**职责驱动的归纳**（由"做 Agent 需要哪些能力域"推得）；
- 照搬框架是**结构套用**（由"某项目用了什么"推得）；
- 前者是合法的通用表征来源，后者是要避免的陷阱。

### 7.3 至今已完成的目录调研

已对 17 个 Agent 产品仓库做只读目录框架图调研，产物在：

```text
工程代码库目录框架图/
├── agno/ AutoGPT/ awesome-claude-code-subagents/
├── claude-code/ codex/ crewAI/ DeepTutor/ dify/
├── hermes-agent/ langgraph/ letta/ llama_index/
├── openclaw/ OpenHands/ OpenMAIC/ pi/
└── 各生成一份"目录框架图.md"
```

每个仓库一份"工程代码库目录框架图.md"，统一采用"顶层 + 职责相关第二层"深度，标注"候选归纳"。所有目录框架图**只是候选归纳，不是通用模板，也不是产品决策**。

### 7.4 调研中的特殊发现

- `letta-main` 是 landing page 而非实现仓库（实现为 letta-ai/letta-code）；
- `OpenHands` 该仓库实为 Web+Electron 前端（agent-canvas），非 Python 核心后端；
- `hermes-agent` 是巨型平铺单仓；
- 各仓库的工程能力分层目录不同——**不能从几个项目都存在某目录，推导出本项目必须使用同名目录**。

---

## 八、基于调研的工程能力域

综合外部对照与本地调研，一个成熟 Agent 产品的通用工程能力域候选集合如下（**候选，待按产品性质筛选**）：

1. **入口与代理编排**：agent/runner/角色、运行循环、状态与会话；
2. **模型与供应商**：模型调用、路由、供应商适配、降级；
3. **工具与外部服务**：schema、输入输出校验、最小权限、超时、沙箱、网络边界；
4. **记忆、知识库、检索与上下文**：长期/短期存储、RAG、retrieval；
5. **状态与持久化**：会话、记忆、缓存、恢复；
6. **角色、权限、安全边界**：guardrails、HITL、安全；
7. **数据模型、存储、迁移**；
8. **交互与多端适配**；
9. **测试、评估、质量**；
10. **观测、tracing、日志、监控**；
11. **构建、发布、部署**；
12. **配置、密钥、环境差异**；
13. **运维、回滚、灾备、供应商退出**。

**注意：** 这些是候选能力域，不是最终骨架。要让它们成为通用模板的骨架，每条都要能答"它是承载哪一类 Agent 产品的通用职责"，并经过基于产品性质的筛选、增删。

---

## 九、DeepTutor 底座评估

### 9.1 结论

经官方官网取证与本地仓库调研，**DeepTutor 适合作学业军师工程代码库的底座镜像**——不仅"分层骨架"贴合，"证据/记忆/可靠性"机制也贴合（贴合度中高）。

### 9.2 DeepTutor 是什么（官方）

- 官方定位："Agent-native 的开源个性化学习伴侣"；
- 八个界面（Home/Partners/My Agents/Co-Writer/Book/Learning Space/Memory/Knowledge Center）共享同一运行时上下文；
- 面向个人学习者为主，多用户模式有 Learner/Guardian/Standard/Custom 角色。

### 9.3 贴合度对照表

| 学业军师硬内核 | DeepTutor 官网对应证据 | 贴合度 |
|---|---|---|
| 证据/来源追溯 | Research 带引用报告；Co-Writer 可背书；字幕绑定时间戳；知识库锚定答案 | **高** |
| 记忆可审计 | 记忆=可审计三层管线（L1 工作区/L2 摘要/L3 综合），非隐藏向量库 | **高** |
| 判断逻辑/可靠性 | 先规划工具调用→观测→继续；Solve 带验证；Mastery 知识点类型决定"什么算掌握" | **中高** |
| 掌握度而非时长 | Mastery Path 以"是否真正掌握"衡量，硬门槛解锁 | **中高** |
| 持久化学习资产 | 学习空间组织聊天/笔记/题库/人设/Skill/MCP 为可复用资产 | **高** |

### 9.4 关键差异（须学业军师自行强化）

1. **判断引擎哲学深度**：DeepTutor 有 agent loop，但没有学业军师"领域判断引擎 + 三层降级 + 8 类异常契约 + 硬底线"的显式架构；
2. **学生主体/边界/隐私**：DeepTutor 有角色授权，但没有学业军师"家长无裁决写权 / 拒绝、沉默不自动等于失败 / 不承诺提分"等产品伦理与最高边界；
3. **垂直深度**：DeepTutor 是广谱学习空间（8 界面），学业军师是垂直学业闭环——可砍 co-writer/book/video 等广谱内容域。

### 9.5 加减法方向

- **保留/强化**：agent 原生运行时分层、learning/knowledge/evidence 域、记忆分层、Quiz/Solve 学习闭环；
- **砍减**：co-writer/book/video（immersive watching）/多智能体 Research 等广谱内容域（首版不做或降级）；
- **新增/强化**：领域判断引擎、产品伦理与最高边界、家长权限、安全兜底、四轴验证治理。

---

## 十、学业军师产品背景（作为系统首例验证对象）

系统设计会以"学业军师"为首个实例验证对象。其核心与特点（据三来源取证，见 `调研报告/学业军师Agent核心与特点调查取证报告.md`）概览：

- **为什么存在**：基础教育结构性矛盾——学业规划是"全链路三不管地带"，外部施教完备而学生内部成长体系空心；
- **是什么**：面向中国初高中学生的 1 对 1 专属学业军师，主动指挥官而非被动工具；教的是"理解与实践的条件"，不承诺替学生制造能力/成绩/结果；
- **关键策略定位**：唯一确定的新赛道（结构性需求 100% 存在、0% 被填补）、AI 让规划从"预制"走向"动态"。

### 10.1 核心机制（共识主体）

**学业军师分层结构：**

```text
通用 Agent 核心（可替换底座）
        ↓
学业规划壳层（价值创造发生的层，做加/减法的地方）
        ↓
APP 封装（1对1 学军师配置）
```

产品关系可概括为"军师定策，学生（君主）执行"；Agent 的主动性必须受产品边界、证据和用户确认机制约束。

### 10.2 关键概念（共识度高）

- **义务环（九步循环 + 五步最小循环）**：看清→裁决→规划→行动→调整→总结→复盘→回写→突破；
- **判断引擎**：在通用推理之上叠加领域专用思考过滤器，化繁为简、20% 核心配置撬动 80% 价值；
- **证据驱动**：判断可指向证据或显式"证据不足"；证据/资产/记忆三类分域；
- **学业资产**：面向单一学生、只属于该学生；原始材料不直写资产，须学生显式确认晋级，沉默≠确认；
- **优先级**：主导性（军师定策） > 决定性（用户拍板） > 参与性；
- **边界与安全**：不承诺分数、不代执行、退出权；家长只可读、不可写学生裁决；安全判断分域。

### 10.3 差异点（须自身构建，非靠底座）

学业军师与 DeepTutor 共构同域，但更重三点：**更垂直**（专注初高中校内学业）、**更重证据**、**更重产品判断逻辑（判断引擎）**。

---

## 十一、交付形态：打包为面向非技术用户的 skills

系统设计完成、验证跑通后，最终会打包为**一组面向非编程背景开发者的 skills**。这组 skills 的作用：

- 帮助非技术用户在具体项目上建立自己的 `mate`；
- 驱动连接层生成 `STRUCTURE.md` 的映射结构；
- 理解"产品源逻辑 → 工程代码库"的转换，让用户在关键决策点拍板。

打包前必须通过可复现实验测量（不视为已实现）：产品规则覆盖率、mate 到工程对象可追踪率、悬空映射数量、返工率、轨迹/响应/规则通过率、安全隐私与人工升级触发率、成本延迟与人工审查时间，以及与无注、无映射治理对照后的差异。

---

## 十二、当前阶段清单与进度

| 阶段 | 进程 | 状态 |
|---|---|---|
| 1. **定位与目标确认** | 目标优先级（主导>决定>参与）、第一性原理、交付形态 | 已确认 |
| 2. **现有资产重估** | 确立伪证据源认知、撤销旧权威 | 已确认 |
| 3. **通用工程代码库模板调研** | 17 个 Agent 仓库目录框架图已产出 | 已完成（候选归纳） |
| 4. **学业军师首个实例取证** | 三来源调查取证报告已产出（项目资料/draft/废弃库） | 已完成 |
| 5. **底座评估更新** | DeepTutor 官网证据已核实、底座评估已更新 | 已完成 |
| 6. **Super Vibe Coding 系统正式设计** | 结构真源文件、能力域、与代码库的映射 | 进行中 |
| 7. **设计验证与落盘 skills** | 垂直切片、对照实验、打包为 skills | 未开始（待系统设计完成）|

---

## 十三、未决问题与前沿

当前设计的前沿如下，需在后续讨论中逐项收束：

1. **结构真源文件的粒度**：停在"能力域/目录骨架"，还是深入到"语义单元↔工程对象"映射，还是分层（结构文件管骨架对位 + 独立追踪层管语义映射）——**推荐分层**；
2. **学业军师首版的垂直刻度**：只保留学业闭环主线（A）、保留学业闭环与基础学习工具（B）、保留较完整的学习工作空间（C），或暂不锁定（D）——直接决定加减法幅度；
3. **通用模板的能力域清单**：候选 13 项中哪些是 Agent 产品通域、如何增删；
4. **「围绕哪个仓库做首例实例化」**：DeepTutor 已评估为底座，仍需确认作为首版实例化的底座；
5. **skills 打包形态**：一组 skills 如何组织、曝光、触发、验证。

---

## 十四、重要防误用边界

1. **Pseudofact 边界**：现有 `mate/`、`STRUCTURE.md`、历史代码库上下文是伪证据源，不自动视为权威。
2. **"Super 版"比喻边界**：不是对外宣传语，不是已验证的产品优势。
3. **"底层源代码"边界**：不是隐藏的程序，不能证明可逆/自动/无损转换。
4. **结构真源文件的边界**：管骨架/对位，不是语义级映射的记录处；不能一次定死，须持续迭代。
5. **模板 vs 照搬边界**：通用模板是职责归纳，不是抄成熟仓库目录。
6. **底座的边界**：DeepTutor 只作骨架镜像，"证据与判断治理"层须学业军师自行强化。
7. **主导权边界**：任何"参与性"设计若削弱用户主导权即应否决。

---

> **本文件状态：** Super Vibe Coding 系统设计白皮书（进行稿）。它是把讨论结论整合为可执行依据的入口；下一步将进入结构真源文件与首例垂直切片的正式设计。本文件不构成最终系统规格，未验证能力均如实标注为未验证。