# `mate`、`STRUCTURE.md` 与工程代码库的关系说明

> **文档性质：外部证据审查与修订版**
>
> 本文是“学业军师” Agent 项目的拟议治理模型和论证说明，不是行业标准、任何 Agent SDK 的官方架构，也不是对当前代码库现状的自动认证。本文把外部资料能够支持的事实、根据事实作出的项目推论、项目内部规则和仍待核验的事项分开记录。

## 一、审查结论

本轮由多名子代理分别审查第一性原理、跨领域转换、Agent 工程架构、需求追踪、文档治理、安全、隐私、非功能需求、运行风险和全文结构，并使用 `research` 方法追到公开的一手资料，再以 `playwright-cli` 核对官方文档、源码入口和仓库目录。审查结论如下。

1. **核心方向成立，但应被定义为项目工作模型。** 对同一个已经确认的产品，产品意图可以在哲学、法律、教育、产品、架构、代码和测试等领域形成不同的表达或派生物。模型驱动架构、模型转换和 provenance 资料支持“分层表示、显式转换、来源记录和派生追踪”的工程方向，但不证明不同文本天然共享一套客观存在的“源代码”。[R1][R2][R3][R4]
2. **“同一组源代码”必须保留为受限定的第一性原理比喻。** 本文保留用户提出的核心论点：对同一个产品，不同领域文本所表达的意图、动机、论点、行为、证据、规则、边界和结果，在底层可以追溯到同一组产品源逻辑；变的是领域适配后的显性形式。这里的“源代码”是本项目定义的产品源逻辑，不是被外部科学证明的隐藏程序。只有在确定语义范围、稳定 ID、版本、映射关系和验证证据后，才能声称某个不变量被保持。
3. **人类与其他类人猿的类比只能说明“共同来源与显性分化”的直觉。** Smithsonian、NHGRI 和 UC Berkeley 的资料支持人类属于灵长类、与其他非洲大猿拥有共同祖先，以及演化涉及跨世代种群遗传变化；它们同时提醒，部分相似不等于完全相同，演化也不等于线性进步或有目的的完美适配。[R40][R41][R42] 这些生物事实不能证明文档之间存在语义同源，也不能证明 Agent 能自动正确转换。
4. **Coding Agent 不能被当作天然正确的编译器。** 形式化编译器的语义保持依赖源语言、目标语言、语义定义、转换阶段和正确性证明；CompCert 的官方资料正是这种严格条件的例子。[R8] Coding Agent 更准确的定位是：在人工确认、结构化映射、权限控制、测试和评估约束下，执行需求到工程实现的受控转译。
5. **“先完整确定工程结构，再决定 `mate`”不能作为不可颠倒的硬顺序。** IEEE 29148 的公开摘要强调需求过程的迭代和递归；NASA 的技术管理资料强调需求、接口、风险、配置、技术数据、评估和决策管理贯穿生命周期，并持续更新。[R6][R7] 正确做法是能力研究先行、垂直切片验证、阶段性基线和双向反馈，而不是一次性假定存在唯一的完整 Agent 目录。
6. **`STRUCTURE.md` 是本项目的治理设计，不是行业通用协议。** 它可以作为 `mate` 到工程代码库的连接入口，但必须从“桥梁”落实为带稳定 ID、来源、目标、版本、责任、审批、冲突和验证证据的映射索引。W3C PROV 只提供来源和派生关系的通用模型，不规定本项目的文件名或字段。[R3][R4][R5]
7. **原说明缺少足以支撑生产 Agent 的安全和运行闭环。** 外部资料显示，模型、工具、状态、记忆、guardrails、人工介入、观测、评估、数据治理、权限、限流、部署和回滚不能只作为目录名称出现；它们必须落到工程契约和上线门槛中。[R11][R12][R14][R15][R16][R19][R21][R22][R23][R24][R25][R26][R27][R28][R29]

所以，修订后的核心结论是：**`mate` 保存已经确认的产品源逻辑，`STRUCTURE.md` 记录源逻辑如何映射到工程资产，Coding Agent 在受控条件下完成跨领域转译，工程代码和运行平台实现并产生事实，测试、评估、安全审查和人工决策决定是否可以发布或反向更新上游。**

## 二、证据与判断的分级

为了避免“看起来通过、实际没有证据”的幻觉，本文采用四级标记：

- **外部事实**：标准组织、政府机构、科研机构、官方文档或官方仓库明确写出的内容，并用 `[R#]` 标注。
- **项目推论**：根据外部事实和“学业军师”项目目标作出的判断。它有依据，但不是外部标准。
- **项目规则/工作模型**：本项目拟定的 `mate`、`STRUCTURE.md`、审批、派生和反馈机制，必须由项目负责人确认后才成为正式规则。
- **待核验/待决策**：当前代码库事实、技术取舍、产品细节或责任归属尚未有足够证据，Coding Agent 不得自行填补。

外部资料只能证明资料本身的内容和适用范围，不能证明本项目已经实现了相应能力。一个框架有 sessions、tracing 或 guardrails，也不等于本项目已经完成会话隔离、敏感数据控制或安全验证。

## 三、第一性原理：不同显性文本可以来自同一组产品源逻辑

### 3.1 核心命题：变的是外部形态，不变的是源逻辑

用户提出的核心意思可以严谨地表述为：

> **不管是哪个领域、哪门学科的文本，只要它们讨论的是同一个产品或同一条产品行为链，那么文本中表达出来的意图、动机、论点、对象、关系、行为、规则、证据和预期结果，在底层都可以追溯到同一组产品源逻辑。不同学科所做的事情，是依据本学科的语法、概念、证据要求和使用场景，对这组源逻辑进行适配、编码和呈现。改变的是显性的语言、格式、术语、工具和外部形态；不应被任意改变的是已经确认的核心关系和语义不变量。**

这里的“同一组源代码”不是指一段藏在自然语言背后的程序，也不是说任何两份主题相似的文本天然共享同一套代码。它是一个用于理解产品开发的第一性原理工作模型。所谓“源代码”，指的是在所有专业表达之前，真正决定产品身份和行为的那组底层源逻辑。

这组源逻辑至少包括：

- **意图与动机**：为什么要做产品，要解决什么问题，要改变什么现状；
- **目标与论点**：希望达到什么结果，为什么采用某种方案，哪些前提支撑这一选择；
- **主体与对象**：涉及哪些用户、角色、资源、数据和外部对象；
- **关系与状态**：对象之间如何关联，当前处于什么状态，状态如何变化；
- **行为与流程**：谁在什么前提下可以执行什么行为，行为会产生什么结果；
- **规则与边界**：哪些行为必须、允许、限制或禁止，哪些情形构成例外；
- **证据与责任**：结论由什么事实、观察、标准或决策支持，谁负责确认；
- **结果与评价**：如何判断产品是否按预期工作，如何识别失败、偏差和风险。

这些内容是产品逻辑的共同来源。法律文本可以把它们表达为权利、义务、许可、禁止、责任、条件和例外；哲学文本可以把它们表达为概念、前提、论证、推论和价值判断；教育文本可以把它们表达为学习目标、学习状态、干预方式和评价标准；工程文本则把它们表达为需求、领域模型、状态转换、接口、数据结构、权限控制和测试条件。

从外部工程资料看，OMG 的 MDA 将业务功能和行为与平台特定技术分开，QVT 则提供模型转换相关的形式化语言和操作映射。它们支持“一个较稳定的业务/产品模型可以拥有多个技术表示”这一工程方向，但不支持把任意自然语言自动、完整、无损地变成代码。[R1][R2]

因此，本项目的“源代码”概念应同时具备两层含义：

1. **解释层含义**：帮助非技术用户理解，法律、哲学、教育、产品、架构和代码是在表达同一个产品逻辑的不同外形；
2. **治理层含义**：只有被拆解、编号、映射、审查并验证的部分，才可以被当作工程上可追踪的源逻辑。

第一层保留用户的根本洞察，第二层防止这个洞察被误解为“Agent 可以不经判断地自动生成正确代码”。

### 3.2 “同一组源代码”不等于所有内容逐字保留

不同领域的文本不是简单的同义词替换。跨领域转换可能出现三种情况：

| 层次 | 典型内容 | 转换要求 |
| --- | --- | --- |
| 规范语义层 | 对象、前提、可观测行为、规则、约束、例外和验收标准 | 在明确范围内应保持，是候选语义不变量 |
| 理由与来源层 | 动机、论点、价值判断、事实证据、责任人和决策背景 | 通过 provenance、决策记录和引用保留；不一定全部进入代码 |
| 领域实现层 | 类型、接口、协议、数据库、并发、部署、日志和供应商配置 | 由目标领域补充；不能未经确认改变产品规范 |

例如，产品规则“在学生明确同意前不得保存可识别的学习轨迹”，可以分别表达为：

- 伦理语言中的信息自主和控制权；
- 规则语言中的授权前提和撤回权利；
- 产品语言中的学习记录使用条件；
- 工程语言中的授权状态、写入条件和状态转换；
- 测试语言中的同意、拒绝、撤回和并发场景。

这些表达可以共同指向同一组源逻辑，但它们不会拥有完全相同的内容。工程实现必须增加技术细节，法律或哲学论证可能包含不会进入代码的理由，某些原始证据可能只进入来源记录。改变外部形式是适配，擅自改变规范性规则则是语义漂移。

W3C PROV-DM/PROV-O 将实体、活动、代理、使用、生成、派生、修订和替代视图区分开来。PROV-O 中的 `wasDerivedFrom`、`wasRevisionOf` 和 `alternateOf` 不是同一个关系，这说明“同源”“修订”“不同视图”不能被一个模糊箭头代替，必须明确记录。[R3][R4][R5]

因此，只有满足以下条件，才可以说两份文件在指定范围内来自同一组源逻辑：

1. 两份文件指向同一个已确认的产品范围；
2. 每个关键语义单元有稳定 ID 和源文件版本；
3. 已明确两份文件之间是引用、细化、实现、修订、替代视图还是其他关系；
4. 已声明本次映射保持哪些内容、忽略哪些内容、增加哪些目标领域细节；
5. 已记录负责人、审批、冲突处理和验证证据；
6. 发现未知、矛盾或未决内容时，系统会停止派生，而不是自动猜测。

### 3.3 人类与其他类人猿：有限的解释性类比

人类与其他类人猿的例子可以帮助读者直观理解“共同来源经过适配后呈现不同外形”，但必须把生物事实和软件工程类比分开。

Smithsonian 人类起源项目说明，人类属于灵长类，现代人类与黑猩猩、倭黑猩猩和大猩猩等非洲大猿拥有共同祖先；它还说明，演化涉及遗传物质及种群中基因比例跨世代的变化，并不是单个个体一生中的变化。[R40]

NHGRI 的比较基因组资料说明，比较完整基因组的目的在于识别相似与差异，保守序列只说明某些部分在不同生物中被保留，并不意味着不同物种拥有完全相同的基因组。[R41] UC Berkeley 的演化教学资料进一步指出，演化不等于必然进步，不是有目的地朝更高级方向发展，也不保证完美适配。[R42]

把这个例子用于本文，只能得到以下有限启发：

- 共同祖先，可以启发对共同来源进行记录；
- 分化，可以启发同一源逻辑在不同领域中的适配；
- 外形不同，可以启发文件形态和专业术语可以不同；
- 不能只看表面相似判断是否同源，应当查看结构、历史和来源证据。

以下边界必须写在类比旁边：

- 文档之间没有生物学意义上的遗传和繁殖机制；
- 人类不是由今天仍然存在的黑猩猩直接演化而来，而是与其拥有共同祖先的不同后代；
- 生物共同祖先不等于文档语义等价；
- 部分遗传保守不等于整套“源代码”完全相同；
- 生物演化的环境适配不等于 Agent 有目的地自动改写产品逻辑；
- 这个类比不能证明文本可逆、代码正确、需求完整或 Agent 可靠。

所以，生物例子适合放在解释性部分，不适合作为 `mate` 到工程文件转换成立的证据。真正的工程证据仍然是语义范围、映射关系、来源记录、测试结果和人工批准。

### 3.4 Coding Agent 是受约束的语义转译者，不是天然正确的编译器

Coding Agent 的工作可以借用“编译”来帮助理解，但不能把它描述成已经拥有形式语义保持证明的编译器。CompCert 的官方文档说明，真正的验证编译器需要定义源语言、中间语言和目标语言的语法与语义，为每个编译阶段定义转换，并对每个阶段提供正确性证明。[R8]

通用 Coding Agent 通常受到模型版本、上下文、提示词、工具结果、权限、外部服务和运行环境影响。它可以提出工程方案、生成代码和执行测试，但不能仅凭“文档已经写得很专业”就保证产品意图未被改变。

因此，Agent 应执行以下受控转译流程：

1. 从已批准的 `mate` 中识别意图、规则、对象、行为、证据和未决项；
2. 将叙述整理为前提、主体、状态、事件、动作、结果、例外和验收条件；
3. 根据 `STRUCTURE.md` 找到目标文件、模块、接口、数据模型、测试或运行对象；
4. 把工程领域新增的技术决策单独记录为 RFC、ADR 或其他决策资产；
5. 在受控环境中生成候选变更，并限制工具权限、网络出口、预算和副作用；
6. 对功能、语义、安全、隐私、性能和运行行为进行验证；
7. 遇到歧义、冲突、过期版本、缺失审批或高风险副作用时停止并升级；
8. 只有在验证和授权完成后，才允许发布工程变更或更新上游映射。

“编译”在这里最多是内部解释性比喻；正式治理文件应使用“受约束的需求到实现转译”这一表述。

## 四、`mate` 的职责和准入边界

### 4.1 `mate` 是已确认的非工程形式专业产品文档

**项目规则**：`mate` 目录只承载用户已经打磨、论证、检验和确认的非编程、非工程类专业产品文档。

它可以使用日常语言，也可以使用哲学、法律、教育学等领域的专业概念。是否使用技术术语不是判断标准；判断标准是内容是否已经形成清晰、稳定、可追踪的产品定义。

一份具备进入 `mate` 资格的文件，至少应当说明与自身范围相关的以下内容：

- 产品为什么存在，要解决什么问题；
- 服务哪些用户和主体，角色、责任和边界是什么；
- 产品应当具备哪些能力，不应当做什么；
- 产品中的对象、概念、关系、状态和行为；
- 规则成立的前提、例外、限制和禁止事项；
- 重要判断的理由、证据、来源和责任人；
- 成功、失败、拒答、升级和降级应如何被用户理解；
- 哪些内容已经确认，哪些内容仍然需要决策。

### 4.2 `mate` 不等于普通自然语言草稿

普通自然语言草稿可以是不完整的，可以保留多个方向，也可以等待讨论；`mate` 不能把这种状态伪装成正式产品定义。

可以用写文章来类比：

- 草稿可能观点含混、结构松散、论证不足、概念反复变化；
- `mate` 应当像经过多轮修改、核对、推敲、检验并正式确认的定稿。

两者都可以使用中文，但质量、用途和约束不同。**未经用户或授权负责人确认的草稿，不属于活动中的 `mate`。** 如果工作流需要保存草稿，应在 `mate` 之外或以明确隔离的非活动状态保存，不能让 Coding Agent 把它当作可派生的产品规范。

建议采用以下状态：

```text
草稿/提案（不可作为活动 mate 上游）
        ↓
已审阅（仍可能修改）
        ↓
已批准 approved（可作为产品规范基线）
        ↓
已替代 superseded（保留历史关系，不再作为当前规范）
```

修改已批准文件时，应生成新版本并重新确认，不能静默覆盖。`mate` 的“非工程”只描述表达领域，不降低逻辑、论证、边界和确认要求。

### 4.3 `mate` 不负责替代代码和工程设计

`mate` 负责保存产品语义和规范性要求，不负责执行代码，也不负责决定所有技术实现细节。它不应直接存放：

- 应用代码、数据库迁移、部署脚本或运行凭据；
- 只有工程师才能解释的临时调试输出；
- 未经确认的技术假设；
- 没有来源、状态、负责人和验收标准的“看起来完整”的文章；
- 仅凭 Agent 推测而生成的产品规则。

工程设计可以补充类型、接口、协议、并发、性能、供应商和部署信息，但这些新增信息必须有来源、有决策记录，并且不能未经确认改变 `mate` 中的产品意图。

## 五、三类核心资产的关系

### 5.1 三类资产

| 资产 | 主要职责 | 不承担的职责 |
| --- | --- | --- |
| `mate` | 保存已批准的产品源逻辑、规则、边界、理由和验收目标 | 不执行代码，不替代工程设计 |
| `STRUCTURE.md` | 记录产品语义、派生文件、代码对象、版本、责任、审批和验证之间的映射 | 不替代源文档、代码、测试或决策记录 |
| 工程代码库、运行平台和证据资产 | 实现、运行、测试、部署、观测并记录实际事实 | 不未经批准改写产品意图 |

工程代码库之外还包括模型供应商、数据库、身份系统、网络、部署平台、日志系统和评估平台。不能把所有职责都归于某个文件夹，也不能把 SDK 的能力误写成代码库已经具备的能力。

### 5.2 `STRUCTURE.md` 是本项目的连接入口

**项目规则**：`STRUCTURE.md` 是 `mate` 与实际代码库之间的结构化桥梁，也是未来派生文件的连接入口。

它至少要能够回答：

- `mate` 中哪份文件定义了什么语义或规则；
- 这份语义对应哪些需求、模型、接口、配置、代码、测试或运行对象；
- 该关系是引用、视图、细化、实现、修订、替代还是其他关系；
- 派生过程中增加了哪些技术决策；
- 当前映射对应哪个源版本、目标提交或部署基线；
- 谁负责这条映射，谁批准了变更；
- 如何验证映射仍然成立；
- 工程事实、测试结果或运行变化需要如何反馈上游。

外部资料支持模型分层、来源追踪、版本化和变更治理的方向，但没有任何本轮核验的 Agent SDK 规定必须存在一个名为 `STRUCTURE.md` 的文件。因此，`STRUCTURE.md` 是本项目的治理约定，不应冒充行业标准。[R1][R2][R3][R4][R6][R7]

### 5.3 权威关系与双向反馈

推荐把三类内容分开：

| 内容 | 权威位置 | 典型证据 |
| --- | --- | --- |
| 产品应该是什么 | 已批准的 `mate` | 用户确认、产品决策、来源和验收目标 |
| 产品定义如何进入工程 | `STRUCTURE.md` 和相关决策记录 | 映射记录、关系类型、版本、负责人和审批 |
| 产品目前如何实现和运行 | 工程代码库、运行平台和配置 | 提交、部署、测试、评估、日志、trace 和运行事实 |

这不是一个可以简单套用的绝对优先级。发生冲突时，不能静默覆盖，而应当：

1. 标记冲突，暂停受影响的派生或发布；
2. 区分是实现缺陷、技术限制、产品变更还是文档过期；
3. 分析影响范围，包括数据、权限、接口、测试、部署和用户行为；
4. 由有权限的负责人作出决策并记录理由；
5. 需要改变产品意图时，生成新的 `mate` 版本；
6. 需要改变映射时，更新 `STRUCTURE.md`；
7. 需要修正实现时，修改工程文件并重新验证。

工程事实可以反馈到 `mate`，但工程代码不能未经批准自动升级为产品规范。需求工程公开摘要强调全生命周期的迭代和递归；NIST SSDF 强调跟踪安全需求、风险、设计决策以及发布组件的来源。[R6][R14] GitHub 的分支保护和 Code Owners 机制提供了审查、状态检查、责任人和批准门的实现参考，但这些流程门禁本身不能证明语义正确。[R9][R10]

## 六、`STRUCTURE.md` 的可执行映射模型

### 6.1 最低字段

为了让“桥梁”真正可检查，每条映射至少应具有以下字段：

| 字段 | 用途 |
| --- | --- |
| `mapping_id` | 映射关系的稳定 ID |
| `source_id` | `mate` 中的语义、规则、目标或验收项 ID |
| `source_locator` | 源文件、章节、段落或锚点 |
| `source_version` | 源文档版本和批准状态 |
| `semantic_scope` | 本次映射实际覆盖的语义范围 |
| `relation_type` | `view`、`refinement`、`implementation`、`revision`、`alternate` 等 |
| `target_locator` | 目标目录、文件、模块、符号、测试或运行对象 |
| `target_revision` | 目标提交、版本、镜像或部署基线 |
| `derived_decisions` | 工程转换中新增的技术决策及其决策 ID |
| `owner` | 对内容或映射负责的人或团队 |
| `approval` | 批准人、批准状态和批准时间 |
| `verification_method` | 测试、评估、审查、形式分析或运行检查方法 |
| `evidence_locator` | 可重新取得的证据位置 |
| `conflict_status` | 无冲突、待裁决、已裁决或阻断 |
| `updated_at` | 映射的最后更新时间 |

这些字段借鉴了 PROV 对实体、活动、代理和派生的区分，也借鉴了需求工程对来源、分解、分配、验证和变更的要求；具体字段名、格式和自动检查方式仍然是本项目的设计选择。[R3][R4][R5][R6][R7]

### 6.2 映射关系不是一条无解释的箭头

以下关系应当区分：

- **引用 view**：目标文件呈现源语义的一部分，不承担全部源语义；
- **细化 refinement**：目标文件把源语义进一步拆分为可实现的约束；
- **实现 implementation**：目标文件负责执行源语义；
- **修订 revision**：新文件保留并修改旧文件的部分内容；
- **替代 alternate**：不同文件从不同角度或格式表达同一个对象；
- **自派生 self-derived**：工程或需求中出现源文档没有的新增内容，必须说明理由和责任人。

没有目标对象、关系类型和语义范围的“已连接”不具有工程意义。仅仅把两个文件路径写在同一张表里，不能证明它们表达的是同一组逻辑。

### 6.3 失败关闭条件

以下任一情况发生时，映射状态应当是 `blocked`、`proposal` 或其他非发布状态：

- `source_id` 不存在、指向草稿或指向过期版本；
- 源文档存在相互冲突的定义，但没有裁决记录；
- 目标文件、目标版本或责任人缺失；
- 派生过程增加了未批准的产品决定；
- 涉及删除、保留、授权、敏感数据或外部写操作，但处置方式尚未确认；
- 验证方法或证据无法重新取得；
- Agent 试图越过人工决策门直接修改批准的产品规范。

## 七、Agent 工程能力的外部对照

外部 Agent 项目不能直接决定本项目的目录，但可以用来检查能力是否遗漏。对照重点不是复制名称，而是识别职责。

### 7.1 能力矩阵

| 能力 | 外部资料中的具体证据 | 对本项目的判断 |
| --- | --- | --- |
| Agent 定义与运行循环 | OpenAI Agents SDK 将 Agent、Runner、tools、handoffs、guardrails、context 和 sessions 分开；官方源码还展示了 Agent 定义和 Runner 循环入口。[R16][R17][R18] | 不能只准备提示词文件；需要运行循环、状态、工具调用、最大轮次和错误处理 |
| 工具与外部服务 | OpenAI Tools 文档和 MCP 架构区分 hosted/local/function/MCP 工具，以及 Host、Client、Server 的权限和会话边界。[R16][R26] | 每个工具需要 schema、输入校验、执行位置、最小权限、超时、输出处理和信任边界 |
| 工作流与动态 Agent | Anthropic 区分预定义 workflow 和由模型动态决定路径的 Agent；LangGraph 支持在同一图中混合确定性和模型驱动步骤。[R15][R24] | 应明确哪些步骤必须确定性执行，哪些步骤允许模型决策，不应无理由增加自主性 |
| 状态与记忆 | OpenAI Sessions 和 LangGraph persistence 分别说明会话历史、线程状态、长期存储、恢复和持久化后端。[R20][R25] | 必须区分短期上下文、长期记忆、知识库、缓存、备份和删除策略 |
| Guardrails 与人工介入 | OpenAI 文档提供输入、输出、工具 guardrails 以及敏感工具调用的暂停、审批、拒绝、序列化和恢复。[R21][R22] | 高风险动作必须有独立控制、人工门、取消、幂等和回滚，不应只写在 prompt 中 |
| 测试与评估 | OpenAI testing 文档区分应用编排测试与外部模型/协议/provider 的集成测试；Google ADK 资料同时评估轨迹、工具使用和最终响应。[R23][R27] | 不能只测最终文本，要测工具轨迹、状态变化、异常、真实 provider、隐私和安全场景 |
| 观测与敏感数据 | OpenAI tracing 文档列出 runner、turn、agent、generation、function、guardrail 和 handoff spans，并说明 trace 可能包含敏感数据。[R19] | 需要关联 ID、版本、脱敏、访问控制、留存期和导出策略 |
| 部署与运行平台 | Google ADK 列出 Agent Runtime、Cloud Run、GKE 和任意容器；LangGraph 将运行时与生态平台区分开。[R24][R28] | SDK、应用、运行时、云平台、身份、网络、数据库和运维责任必须分开 |
| 安全和网络边界 | Google ADK safety、MCP 架构以及 OWASP Agent/LLM 风险资料分别强调身份、授权、沙箱、网络和外部内容边界。[R26][R29][R32][R34][R35][R36][R37] | 安全不能被当作部署完成后的附加目录，必须进入产品、工程和运行治理 |

### 7.2 目录名称不是架构标准

本项目可以使用 `docs`、`apps`、`runtime` 等名称，但这些名称只能在代码库核验和项目决策后生效。它们不是 Agent 产品的通用规范。

OpenAI Agents SDK 的官方仓库树显示，源码下包含 `extensions`、`handoffs`、`mcp`、`memory`、`models`、`sandbox`、`testing`、`tracing` 等目录，根部还包含 `docs`、`examples`、`integration_tests`、`SECURITY.md`、`Makefile`、`mkdocs.yml` 和 `pyproject.toml`。[R30]

Microsoft AutoGen 的官方 README 和 Core 源码树则采用 Core、AgentChat、Extensions、Bench 以及 telemetry、code execution、memory、model context、models、tools、runtime、serialization 和 config 等分层。[R31]

这些事实说明：成熟 Agent 的工程能力确实需要分层，但不同项目的目录边界不同。不能从几个项目都存在某个目录，推导出本项目必须使用同名目录；应当根据职责、依赖、部署方式、所有权和验证边界决定目录。

## 八、受控的 `mate` 到工程实现闭环

### 8.1 推荐流程

```text
用户初步想法
        ↓
用户整理、论证、检验和确认
        ↓
mate：approved 的产品源逻辑与规范
        ↓
STRUCTURE.md：来源、版本、范围、关系、责任、审批和验证映射
        ↓
工程设计：补充技术决策并记录 RFC/ADR
        ↓
代码、配置、数据模型、测试、部署和运行资源
        ↓
功能验证、安全审查、隐私检查、轨迹评估、最终结果评估和运行观测
        ↓
人工/产品负责人审批
        ↓
修正实现、更新映射，或生成新的 mate 版本
```

这不是一条只能从左到右执行一次的流水线，而是一个可暂停、可回滚、可重新基线的闭环。

```text
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  澄清语义 → 建立映射 → 生成实现 → 验证评估 → 审批变更       │
│      ↑                                      ↓               │
│      └──────────── 反馈、冲突处理与重新基线 ────┘            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 确定 `mate` 内容的正确顺序

`mate` 的内容不能脱离 Agent 的实际工程结构凭空设计。`mate` 不是独立存在的文集，它最终要通过 `STRUCTURE.md` 与具体的需求、架构、代码、配置、测试、运行和维护资产建立关系。如果连下游工程体系需要哪些能力、目录和文件都没有确定，就无法判断 `mate` 应该保存什么、保存到什么详细程度，也无法判断哪些内容需要由 Coding Agent 转换为工程文件。

因此，本项目确定 `mate` 内容的正确起点是：**先研究并确定成熟 Agent 产品所需的工程能力、文件体系和职责边界，再反向确定 `mate` 的内容范围。** 这不是让某个成熟仓库成为唯一模板，也不是要求一次性冻结最终技术方案，而是先回答一个更基础的问题：一个能够开发、运行、部署和长期维护的 Agent 产品，工程上究竟必须有哪些承接对象。

本轮对 OpenAI Agents SDK、Anthropic Agent 指南、LangGraph、Google ADK 和 AutoGen 的官方资料对照表明，成熟 Agent 的能力通常分布在多个层次，目录布局也不相同。模型调用、工具、任务编排、状态与记忆、权限与人工介入、评估、观测、部署和安全，可能分别由 SDK、应用、运行时、云平台和运维系统承担。[R15][R16][R17][R18][R19][R20][R21][R22][R23][R24][R25][R26][R27][R28][R29][R30][R31] 这说明研究的对象应当是**工程能力和责任边界**，而不是简单复制某个项目的文件夹名称。

在确定 `mate` 文件体系之前，至少要研究并明确以下工程体系：

- Agent 核心逻辑；
- 模型调用、模型路由和供应商适配；
- 工具和外部服务；
- 任务编排、工作流、handoff 和状态转换；
- 记忆、知识库、检索和上下文管理；
- 数据模型、持久化、数据处理和迁移；
- API、事件协议以及前端与后端的边界；
- 配置管理、环境差异、密钥和敏感信息边界；
- 测试、评估、回归和质量保障；
- 构建、发布和部署；
- 运行时资源、会话、队列、缓存和恢复机制；
- 日志、指标、追踪、监控、告警和错误处理；
- 运维、备份、回滚、灾备、供应商替换和长期维护。

同时，还要明确每类目录和文件分别承担什么职责，彼此如何协作，以及产品如何启动、运行、测试、部署和维护。只有把这些工程承接对象和生命周期边界梳理清楚，才能进一步判断：

1. `mate` 必须保存哪些类型的产品信息；
2. 每类信息需要达到什么详细程度；
3. 哪些内容应继续保留在 `mate` 中，作为用户确认的产品源逻辑；
4. 哪些内容需要由 Coding Agent 转换为需求、模型、接口、配置、代码、测试或运行规则；
5. 每类派生文件应进入代码库中的哪个目录或工程边界；
6. 哪些工程结果、技术限制和新决策需要反馈到 `mate`；
7. `STRUCTURE.md` 应如何记录文档、文件、目录、版本、责任、审批和验证之间的关系。

这一步的本质，是用工程体系反向检验 `mate` 的完整性。每一个工程承接对象都应当能够提出问题：它需要从 `mate` 获得什么产品定义、行为规则、数据边界、权限约束和验收目标？如果某个工程目录没有对应的产品语义来源，可能说明它是未经论证的技术堆砌；如果某项产品规则没有对应的工程承接对象，说明 `mate` 仍然没有形成可落地的派生关系。

因此，合理的工作顺序应当是：

```text
第一步：确定成熟 Agent 所需的工程目录与文件体系
        ↓
第二步：根据工程体系确定 mate 的内容范围和文件类型
        ↓
第三步：明确 mate 文件与派生工程文件之间的关系
        ↓
第四步：通过 STRUCTURE.md 建立文档与代码库的映射
        ↓
第五步：由 Coding Agent 执行非工程语言到工程语言的转换
        ↓
第六步：根据开发、运行和维护结果，反向更新并确认 mate
```

这里的“先确定工程目录与文件体系”，指的是先建立成熟 Agent 的能力清单、责任边界和初版承接结构，不是要求在产品尚未验证前把所有实现细节一次性冻结。产品语义、工程结构和映射关系仍然需要通过垂直切片、测试、安全审查、运行结果和阶段性基线不断校正。换句话说，**初始设计顺序必须从工程体系出发，后续演进过程则通过反馈循环收敛。**

主线仍然是 `mate` 的内容和职责设计；代码库目录重构是支线。支线只有在目标目录、文件职责、迁移影响和 `STRUCTURE.md` 更新方案达成共识并获得明确授权后，才能创建目录、文件或修改结构。支线完成后，实际形成的工程承接对象还必须重新反馈到 `mate`，以确认文档与代码库没有脱节。

### 8.3 歧义和未决内容的阻断协议

“Agent 不应猜测”必须落实为运行协议，而不是一句口号。以下情形应视为阻断级歧义：

- 角色、权限或数据主体不清楚；
- 关键术语没有定义，或同一术语在不同文件中含义冲突；
- 规则缺少前提、例外、时序或终止条件；
- 需要删除、保留、匿名化、外发或修改数据，但处置方式未确认；
- 需求无法写成可观察、可测试的验收条件；
- 工程方案会产生不可逆副作用，但没有审批、取消或回滚；
- 源版本、映射目标、责任人或验证证据缺失。

遇到阻断级歧义时，Agent 可以提出候选解释、生成只读分析或创建待决策记录，但不得：

- 把候选解释写入批准的 `mate`；
- 执行有副作用的工具调用；
- 用默认值掩盖缺失的产品决策；
- 把“代码能够运行”当作需求已经满足。

OpenAI 的 guardrails 文档说明防护需要绑定到输入、输出或工具调用的具体边界；HITL 文档说明敏感工具调用需要暂停、审批、序列化和恢复。这些资料说明，等待确认必须是可审计的运行状态，而不是自然语言中的愿望。[R21][R22]

## 九、必须补足的 Agent 产品工程闭环

### 9.1 产品语义层

应在 `mate` 或与其相连的产品治理文件中明确：

- 用户、学生、家长、教师、管理员和 Agent 的角色与责任；
- 学习目标、学习状态、干预规则和评价标准；
- 事实、建议、推断、不确定性和来源的区别；
- 何时必须询问、拒答、降级或转人工；
- 非目标、例外、失败路径和允许的降级行为；
- 每条关键规则的稳定 ID、版本、状态、负责人和确认记录。

### 9.2 Agent 运行层

应在工程和运行治理中明确：

- 模型、提示词、工具和 Agent 的版本注册；
- 模型路由、供应商替换、故障切换和兼容性；
- 工具 schema、输入校验、输出校验、最小权限、网络出口和沙箱；
- 任务编排、handoff、状态、会话、短期记忆和长期记忆；
- 最大轮次、时间预算、token/成本预算、取消和停止条件；
- 超时、重试、退避、幂等、并发、队列、补偿和断点恢复；
- 人工审批、人工升级、审批 TTL、拒绝和回滚。

Anthropic 的官方指南强调从环境取得 ground truth、在阻塞点接受人工反馈、设置停止条件，并提醒 Agent 的自主性会带来成本和复合错误。[R15]

### 9.3 数据、安全和隐私层

应明确：

- 身份认证、租户隔离、细粒度授权和服务身份；
- 学习数据分类、处理目的、最小化、同意、撤回、保留、删除和匿名化；
- RAG 文档、向量库、缓存、备份、日志和供应商副本的生命周期；
- 检索时的 ACL、跨租户隔离、来源认证、许可证和撤回传播；
- 提示词、工具输入输出、trace 和日志中的敏感数据脱敏；
- 代码执行、插件、MCP Server、网络出口和供应链信任边界；
- 安全事件、错误升级、漏洞修复、事故响应和审计留痕。

NIST AI RMF 将可信性纳入 AI 的预设计、设计开发、部署、使用和测试评估阶段，并明确说明单独处理某一项可信性特征不能保证整体可信，且不同目标之间可能存在权衡。[R11][R12]

NIST Privacy Framework 将隐私风险识别和管理定义为组织层面的风险管理工作。[R13] NIST SSDF 则强调安全开发实践应整合进不同的 SDLC，并关注安全需求、风险、设计决策、组件和来源。[R14]

OWASP 的资料进一步指出：提示注入可能来自用户输入、网站或文件，RAG 和微调不能完全消除风险；模型输出进入下游前必须验证、净化并按上下文编码；过度功能、权限和自主性会放大 Agent 的副作用；system prompt 不是秘密或安全控制；向量库需要权限感知和严格隔离；无界推理需要输入限制、限流、配额、超时和资源管理。[R32][R33][R34][R35][R36][R37] OWASP 还把第三方模型、依赖、数据和许可证的供应链风险，以及训练、嵌入和外部知识数据被投毒的风险单独列出，支持在发布前建立来源、版本、许可证和数据完整性检查。[R38][R39]

原文的同意示例必须补全。学生撤回同意后，历史记录究竟删除、匿名化、冻结、导出还是按某种保留规则处理，属于产品和合规决策，不能让 Agent 自行选择。该决策还要派生为数据模型、状态转换、接口规则、迁移方案和测试场景。

### 9.4 质量、评估和运行层

应明确：

- 单元、集成、端到端和真实外部服务集成测试；
- 评估数据集版本、正常/异常/边界/反例样本、阈值和复现方式；
- 工具调用轨迹、最终响应、状态转移、拒答和人工介入的评估；
- 事实准确性、规则遵守、引用质量、偏差、公平性和安全指标；
- trace、span、日志、指标、关联 ID、脱敏、留存和访问控制；
- SLO、延迟、成本、配额、限流、容量、告警和错误预算；
- 发布、灰度、feature flag、数据库迁移、备份、回滚和灾备；
- 模型、工具、数据、依赖和供应商变化的回归与退出方案。

OpenAI testing 文档明确区分应用自身编排的确定性测试与外部模型、协议和 sandbox provider 的真实集成测试；Google ADK 的评估资料同时关注 Agent 轨迹、工具使用和最终响应。[R23][R27] 这意味着“代码测试通过”不能单独证明 Agent 的产品行为、模型行为或外部工具行为已经达标。

OpenAI tracing 文档还说明，模型生成和工具调用的 trace/span 可能包含敏感数据，需要显式控制采集和导出。[R19] “有观测”与“可以安全记录”是两个不同问题。

## 十、代码库目录重构支线的边界

### 10.1 与主线的关系

`mate` 是主线：继续明确产品源逻辑、文档准入、语义分层和映射治理。

代码库目录重构是支线：根据已经审阅的目录布局方案，核对实际磁盘结构、构建入口、运行方式、部署边界和文件职责，并在达成共识后落实目录和 `STRUCTURE.md` 更新。

两条线可以并行讨论，但支线不能在方案尚未确认时擅自修改代码库。

### 10.2 支线启动条件

只有以下内容都确认后，才能执行目录或文件变更：

- 目标目录和文件范围；
- 每个目录和文件的职责边界；
- 现有目录的迁移、兼容和删除影响；
- 代码、文档、运行资源和元文档的边界；
- `STRUCTURE.md` 的字段、映射和更新方案；
- 负责人、审批人和验证方式；
- 明确的执行授权。

支线只负责落实已经批准的目录和文档方案，不扩展为业务代码开发。完成后，应把实际目录、文件职责、映射结果和验证证据重新反馈到 `mate` 的治理体系。

### 10.3 当前 `STRUCTURE.md` 状态的证据边界

原说明中“当前 `STRUCTURE.md` 尚未形成确切效益”的判断，若没有附带实际文件内容、核验命令、目录快照、时间点和对照结果，只能记录为**项目观察/待核验事项**，不能写成已经证实的代码库事实。

代码库目录布局重构研讨报告可作为支线输入：

```text
/Users/caibiao/orca/projects/junshi-app/调研报告/junshi-app代码库目录布局重构研讨报告.md
```

但该研讨报告不能代替对当前磁盘、代码库和运行状态的实际核验。

## 十一、对原说明的结构和论证审查

### 11.1 原说明可以保留的内容

- `mate` 与代码库不应混为一谈；
- 用户可以使用非工程语言表达产品；
- `STRUCTURE.md` 需要连接产品文档和工程对象；
- 工程结果应当反馈产品文档，但不能未经确认自动改写；
- Agent 产品不能遗漏模型、工具、状态、记忆、编排、测试、部署和维护；
- 需要从第一性原理解释不同专业表达之间的底层关系。

这些方向与模型分层、来源追踪、需求生命周期和 Agent 工程资料存在对应关系，但仍然需要项目自身的字段、状态、责任和验收设计。[R1][R3][R6][R7][R15][R24]

### 11.2 原说明需要修正的跳步

| 原跳步 | 为什么不严谨 | 修订方式 |
| --- | --- | --- |
| “不同领域文本共享一组源代码” | 没有定义源逻辑、范围、映射和验证，容易被理解为客观事实或完整可逆转换 | 改为本项目的源逻辑工作模型，并要求稳定 ID、版本、映射和证据 |
| “Coding Agent 是编译器” | 没有形式化源/目标语义和正确性证明，LLM 输出还受上下文和工具影响 | 改为受约束的需求到实现转译流程 |
| “逻辑不变量一定保持” | 没有中间表示、判定标准和测试覆盖 | 改为待验收命题，只对明确范围内的规则声称保持 |
| “先完整工程结构，再决定 mate” | 需求和架构实际是迭代、递归和相互暴露缺口的过程 | 改为能力研究先行、垂直切片验证、阶段性基线和反馈循环 |
| “docs/apps/runtime 是 Agent 工程目录” | 官方项目的目录、SDK、运行时和部署边界不同 | 改为本项目的候选命名，必须以实际职责和核验结果决定 |
| “工程反馈即可更新 mate” | 运行事实、技术决策和产品变更不是同一类内容 | 增加审批、版本、影响分析、冲突裁决和新版本机制 |
| “文档打磨后就能可靠转换” | 文档质量是必要条件，不是充分条件 | 增加结构化中间表示、阻断协议、权限、安全、测试和人工门 |
| “Vibe Coding Super 版”已经成立 | 没有基线、指标、样本、阈值和对照实验 | 只作为内部愿景或暂定名称，必须用数据验证 |

### 11.3 原说明的重复区域

- 开头结论、第 5 节链路和结尾定义反复解释三类资产职责；应集中在资产模型和闭环章节；
- `mate` 的正式性、非工程性和非草稿边界反复出现；应集中在准入章节；
- “底层逻辑”“非逐字翻译”“不变量保持”反复出现；应集中在第一性原理和判定标准章节；
- `STRUCTURE.md` 的桥梁作用在多个章节重复；应集中定义字段、关系类型和失败关闭条件；
- “先工程后 `mate`”被重复写成硬顺序；应统一改成迭代收敛；
- “Vibe Coding Super 版”不应在没有指标的情况下出现在结论中。

## 十二、最终定义

### 12.1 `mate` 的一句话定义

> **`mate` 是用户经过反复思考、打磨、论证、检验和确认后形成的非工程形式专业产品文档；它保存产品的源逻辑、规范、边界、理由和验收目标，不是普通自然语言草稿，也不是代码仓库。**

### 12.2 第一性原理的最终表述

> **同一个产品在不同领域中的文本，可以呈现完全不同的显性形态，但它们所表达的意图、动机、论点、对象、关系、行为、证据、规则和结果，在底层可以追溯到同一组产品源逻辑。哲学、法律、教育、产品、架构、代码和测试只是这组源逻辑在不同领域中的适配表达。变的是语言、格式、术语、工具和技术约束；能够被称为“不变”的，只能是已经明确、划定范围并通过映射与验证证明保持的语义不变量。本文中的“源代码”是本项目的工作模型，不是隐藏的计算机程序，也不能替代工程设计、测试、安全审查和人工决策。**

### 12.3 三者关系图

```text
用户初步想法
        ↓
用户打磨、论证、检验和确认
        ↓
mate（approved 的产品源逻辑与规范）
        ↓
STRUCTURE.md（来源、映射、版本、责任、审批、冲突和验证入口）
        ↓
Coding Agent 受约束转译 + 工程设计决策
        ↓
工程代码、配置、测试、部署与运行资源
        ↓
测试、评估、安全审查、trace 和运行事实
        ↓
人工/授权负责人裁决
        ↓
新版本 mate、STRUCTURE.md 或工程实现
```

### 12.4 关于“Vibe Coding Super 版”

“Vibe Coding Super 版”可以作为项目内部名称或愿景，但当前不能作为外部事实或已经证明的产品优势。要正式使用这个名称，至少需要通过可复现实验测量：

- 产品规则覆盖率；
- `mate` 到工程对象的可追踪率；
- 悬空映射和未经批准变更数量；
- 需求返工率和缺陷率；
- 工具轨迹、最终响应和规则遵守通过率；
- 安全、隐私和人工升级触发率；
- 成本、延迟和人工审查时间；
- 与没有 `mate`/映射治理的流程进行对照后的差异。

在完成这些验证前，准确的说法是：

> 本项目拟建立一种让非技术背景用户通过高质量产品语义参与专业 Agent 工程的工作方式；它是否优于传统 Vibe Coding，需要由指标和对照实验决定。

## 十三、交付前核验清单

### 13.1 `mate` 和语义质量

- [ ] 每份活动中的 `mate` 文档具有稳定 ID、版本、状态、负责人和批准记录。
- [ ] 未批准草稿与活动中的 `mate` 隔离。
- [ ] 目标、非目标、主体、对象、状态、行为、前提、结果、例外和验收标准可定位。
- [ ] 规范语义、理由/来源和工程实现细节没有混写。
- [ ] “源代码”“同源”“编译器”等词明确标为工作模型、类比或外部事实。
- [ ] 未决问题、冲突和假设不会被 Agent 静默填补。

### 13.2 `STRUCTURE.md` 和变更治理

- [ ] 每条映射有 `mapping_id`、`source_id`、源版本、语义范围、关系类型和目标对象。
- [ ] 映射有 owner、approval、decision、verification 和 evidence 字段。
- [ ] 双向链接可解析，没有悬空、过期或无负责人映射。
- [ ] 变更前有影响分析，变更后有重新验证和审批。
- [ ] 冲突有明确状态、裁决人和升级路径。
- [ ] 工程事实不会未经授权直接改写 `mate`。
- [ ] 目录名称没有被误写成 Agent 工程的通用标准。

### 13.3 Agent 运行和安全

- [ ] 工具具有输入校验、输出校验、最小权限、超时、网络和沙箱边界。
- [ ] 高风险工具动作具有人工确认、取消、幂等、审计和回滚路径。
- [ ] Agent 具有最大轮次、时间/成本预算、停止条件和人工升级路径。
- [ ] 用户、租户、会话、长期记忆、RAG 和备份数据得到隔离。
- [ ] 提示词不承载凭据，真正的授权由模型之外的确定性系统执行。
- [ ] 提示注入、恶意文档、输出处理、跨租户检索和供应链风险有对抗性测试。

### 13.4 评估和运维

- [ ] 评估集有版本、正常/异常/反例样本、阈值、环境和复现方式。
- [ ] 同时评估最终响应、工具轨迹、状态转移、规则遵守和安全行为。
- [ ] 真实模型、外部工具、网络协议和 sandbox provider 有集成测试。
- [ ] trace、span、日志和指标具有可关联的 run ID，并完成脱敏、留存和访问控制。
- [ ] 发布、迁移、灰度、回滚、备份恢复和供应商退出路径已定义。
- [ ] 运行平台的 SLO、成本、延迟、容量、告警和事故响应已定义。

## 十四、外部证据索引

> 下列索引优先采用标准组织、政府机构、科研机构、官方文档或官方仓库。每条索引同时说明它能支持什么、不能支持什么，防止把证据范围扩大。

### 14.1 跨表示、模型转换与来源追踪

- **[R1] OMG，Model Driven Architecture (MDA)**  
  <https://www.omg.org/mda/>  
  官方页面说明 MDA 使用模型表达软件规格，并把平台无关模型中的业务功能与行为同平台特定技术分开。支持“产品/业务语义与技术实现可以分层”的工程方向；不证明自然语言可以自动生成正确代码，也不证明本项目存在 `mate`。

- **[R2] OMG，MOF Query/View/Transformation (QVT) 1.3**  
  <https://www.omg.org/spec/QVT/1.3/>  
  官方规范页列出 Relations、Operational Mappings 和 Core 等模型转换语言及机器可读形式。支持“模型转换需要明确的模型、元模型和转换规则”；不证明任意法律或哲学文本能够无损、自动、可逆地转换为程序。

- **[R3] W3C，PROV-DM: The PROV Data Model**  
  <https://www.w3.org/TR/2013/REC-prov-dm-20130430/>  
  Abstract 将 PROV-DM 定义为描述生成数据或事物时涉及的实体、人员和过程的来源模型，并说明其具有领域无关性和扩展点。支持为文档、活动、责任和派生关系建立 provenance；不规定需求矩阵或 `STRUCTURE.md` 文件格式。

- **[R4] W3C，PROV-O: The PROV Ontology**  
  <https://www.w3.org/TR/prov-o/>  
  官方推荐规范用 OWL2 表达 PROV 数据模型，区分实体、活动、代理、使用、生成和派生，并支持不同语义关系的交换。支持把“同源”拆解为派生、修订和替代视图；不证明不同文本天然语义等价。

- **[R5] W3C，Constraints of the PROV Data Model**  
  <https://www.w3.org/TR/prov-constraints/>  
  说明来源模型中的实体、活动、时间、责任代理、派生、同一性和有效性约束。支持在映射中记录时间、责任、派生和有效性检查；不直接证明业务需求或代码正确。

- **[R8] AbsInt/CompCert，The CompCert verified compiler 文档**  
  <https://compcert.org/doc/>  
  官方文档说明 CompCert 涉及源语言、中间语言和目标语言的语法与语义，并为编译阶段提供正确性证明。支持“形式化编译的语义保持需要定义、映射和证明”的对照；不证明通用 LLM Agent 自动具备同等保证。

### 14.2 需求工程、迭代与变更治理

- **[R6] IEEE Standards Association，ISO/IEC/IEEE 29148-2018**  
  <https://standards.ieee.org/ieee/29148/6937/>  
  公开摘要说明该标准覆盖系统和软件全生命周期的需求工程过程与产品，定义良好需求的属性和特征，并讨论需求过程的迭代与递归。支持把 `mate` 的质量、状态和生命周期作为项目治理对象；公开页面不提供完整标准正文或本项目专用追踪格式。

- **[R7] NASA，SEH 6.0 Crosscutting Technical Management**  
  <https://www.nasa.gov/reference/6-0-crosscutting-technical-management/>  
  章节介绍技术规划、需求管理、接口、风险、配置、技术数据、评估和决策分析，并强调这些过程贯穿生命周期、相互协调和持续更新。支持补充追踪、配置、风险、决策和迭代要求；不证明 NASA 的目录或流程就是本项目必须复制的结构。

- **[R9] GitHub，About protected branches**  
  <https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches>  
  官方文档说明分支保护可以要求审查、状态检查、评论解决和 Code Owner 批准，并说明新提交可能使旧批准失效。支持把审批和变更门作为工程治理机制；不证明仅有 PR 就能保证语义正确。

- **[R10] GitHub，About code owners**  
  <https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners>  
  说明可以按路径指定负责审查的个人或团队，并结合分支保护要求批准。支持为 `STRUCTURE.md` 映射、敏感目录和工程文件设置责任人；不证明本项目已经配置这些规则。

### 14.3 AI 风险、隐私与安全开发

- **[R11] NIST，AI Risk Management Framework**  
  <https://www.nist.gov/itl/ai-risk-management-framework>  
  Overview 说明 AI RMF 用于管理影响个人、组织和社会的 AI 风险，并支持在 AI 产品、服务和系统的设计、开发、使用与评估中纳入可信性考虑。页面明确该框架是自愿使用的；它不证明采用框架后系统自动可信。

- **[R12] NIST，AI Risk Management Framework FAQs**  
  <https://www.nist.gov/itl/ai-risk-management-framework/ai-risk-management-framework-faqs>  
  “When do these characteristics apply...”说明可信性特征应覆盖预设计、设计开发、部署、使用和测试评估，并明确单独处理各项特征不能保证整体可信，目标之间可能存在权衡。支持全生命周期和“不把单一控制当作保证”的判断。

- **[R13] NIST，Privacy Framework**  
  <https://www.nist.gov/privacy-framework>  
  “About”说明该框架是帮助组织识别和管理隐私风险、同时保护个人隐私的自愿工具。支持把数据盘点、目的、控制、沟通和隐私风险纳入治理；不替代具体法律意见或项目数据清单。

- **[R14] NIST，Secure Software Development Framework (SSDF) Version 1.1**  
  <https://csrc.nist.gov/pubs/sp/800/218/final>  
  Abstract 说明安全开发实践应整合进不同 SDLC，以减少漏洞、降低未发现漏洞的影响并支持发布组件来源和漏洞响应。支持把安全需求、风险、设计决策、组件 provenance 和响应纳入工程闭环；不规定 `mate` 或 `STRUCTURE.md` 的格式。

- **[R32] OWASP GenAI Security Project，LLM01:2025 Prompt Injection**  
  <https://genai.owasp.org/llmrisk/llm01-prompt-injection/>  
  “Indirect Prompt Injections”说明网站、文件等外部内容可能改变模型行为，RAG 和微调不能完全消除提示注入风险；缓解建议包括隔离外部内容、限制权限、定义输出格式和对抗测试。支持把用户输入、检索内容和工具回传视为不可信边界。

- **[R33] OWASP GenAI Security Project，LLM05:2025 Improper Output Handling**  
  <https://genai.owasp.org/llmrisk/llm052025-improper-output-handling/>  
  “Prevention and Mitigation Strategies”要求模型输出进入下游前进行验证、净化和上下文编码，不能直接当作 shell、SQL、HTML、路径或工具参数。支持确定性输出校验和下游 sink 防护；不证明模型输出本身可信。

- **[R34] OWASP GenAI Security Project，LLM06:2025 Excessive Agency**  
  <https://genai.owasp.org/llmrisk/llm062025-excessive-agency/>  
  “Root Cause”和“Prevention and Mitigation Strategies”将过度功能、过度权限和过度自主性列为根因，并建议最小扩展、最小权限、服务端完整授权和高影响动作人工审批。支持工具注册、权限分级、HITL 和回滚要求。

- **[R35] OWASP GenAI Security Project，LLM07:2025 System Prompt Leakage**  
  <https://genai.owasp.org/llmrisk/llm072025-system-prompt-leakage/>  
  “Underlying Risk”和“Prevention and Mitigation Strategies”明确 system prompt 不应被当作秘密或安全控制，凭据和连接串不应放入其中，关键授权应在模型之外以确定性方式执行。支持把产品规则、提示词和真正的权限控制分层。

- **[R36] OWASP GenAI Security Project，LLM08:2025 Vector and Embedding Weaknesses**  
  <https://genai.owasp.org/llmrisk/llm082025-vector-and-embedding-weaknesses/>  
  “Unauthorized Access & Data Leakage”与“Permission and access control”说明向量库访问控制错配会导致跨用户或跨租户泄露，并建议权限感知存储、逻辑隔离、来源验证和检索监控。支持 RAG/记忆层的 ACL、provenance 和撤回传播要求。

- **[R37] OWASP GenAI Security Project，LLM10:2025 Unbounded Consumption**  
  <https://genai.owasp.org/llmrisk/llm102025-unbounded-consumption/>  
  “Prevention and Mitigation Strategies”建议输入限制、速率限制、用户配额、资源管理、超时、沙箱和监控。支持为 Agent 规定 token、时间、工具次数、并发和成本边界；不证明任何固定阈值适合本项目。

- **[R38] OWASP GenAI Security Project，LLM03:2025 Supply Chain**  
  <https://genai.owasp.org/llmrisk/llm032025-supply-chain/>  
  资料将第三方模型、数据、依赖和许可证列为影响完整性和法律风险的供应链因素，并提醒模型来源文件本身不是来源保证。支持模型、依赖、数据、许可证和 provenance 清单；不替代本项目的供应商审查。

- **[R39] OWASP GenAI Security Project，LLM04:2025 Data and Model Poisoning**  
  <https://genai.owasp.org/llmrisk/llm042025-data-and-model-poisoning/>  
  资料讨论训练、微调、嵌入和外部知识数据被篡改或投毒的风险，并建议来源追踪、合法性验证、沙箱和数据版本控制。支持知识库来源、数据版本和恶意内容隔离要求。

### 14.4 Agent 官方文档、源码与仓库

- **[R15] Anthropic，Building effective agents**  
  <https://www.anthropic.com/engineering/building-effective-agents>  
  “What are agents”“Agents”“Summary”区分预定义 workflow 与模型动态控制的 Agent，要求从环境获得 ground truth、在阻塞点接受人工反馈并设置停止条件，同时提醒自主性会带来成本和复合错误。支持受控 Agent 生命周期；不支持 Agent 可以无监督可靠完成任意开发任务。

- **[R16] OpenAI Agents SDK，Agents**  
  <https://openai.github.io/openai-agents-python/agents/>  
  官方文档将 Agent 描述为配置 instructions、tools、handoffs、guardrails、structured outputs 和运行行为的核心构件，并把模型、工具、运行、上下文和多 Agent 编排分开说明。支持 Agent 能力分层；不证明 SDK 提供完整产品部署和业务治理。

- **[R17] OpenAI Agents SDK，Agent 源码**  
  <https://raw.githubusercontent.com/openai/openai-agents-python/main/src/agents/agent.py>  
  官方源码提供 `tools`、`mcp_servers`、`mcp_config`、`handoffs`、`guardrails` 和 `context` 等入口。支持对 Agent 定义层的具体源码分析；不证明这些入口自动满足本项目的权限和语义要求。

- **[R18] OpenAI Agents SDK，Runner 源码**  
  <https://raw.githubusercontent.com/openai/openai-agents-python/main/src/agents/run.py>  
  `Runner` 运行逻辑围绕模型调用、最终输出、handoff、工具调用和继续循环展开，并包含最大轮次、状态、会话、guardrail、追踪和错误处理入口。支持对运行循环的具体源码分析；不证明外部模型行为可以被形式验证。

- **[R19] OpenAI Agents SDK，Tracing**  
  <https://openai.github.io/openai-agents-python/tracing/>  
  “Traces and spans”“Default tracing”“Sensitive data”说明可记录 runner、turn、agent、generation、function、guardrail 和 handoff spans，并可以控制敏感数据采集和导出。支持观测和隐私控制不可分离；不证明开启 tracing 就完成安全合规。

- **[R20] OpenAI Agents SDK，Sessions**  
  <https://openai.github.io/openai-agents-python/sessions/>  
  官方文档说明会话可以自动读写历史，并列出 SQLite、Redis、SQLAlchemy、Mongo 等实现和恢复行为。支持区分会话状态与长期存储；不规定本项目必须使用某种后端。

- **[R21] OpenAI Agents SDK，Guardrails**  
  <https://openai.github.io/openai-agents-python/guardrails/>  
  官方文档说明输入、输出和工具 guardrails 在工作流的不同边界执行并可以阻断。支持把规则校验绑定到具体输入、输出和工具边界；不表示所有模型输出已经被验证。

- **[R22] OpenAI Agents SDK，Human-in-the-loop**  
  <https://openai.github.io/openai-agents-python/human_in_the_loop/>  
  官方文档说明敏感工具调用可以暂停，审批以 interruption 暴露，运行状态可以序列化并在决定后恢复，待审批任务还涉及版本化。支持人工决策门、暂停/恢复和审计状态。

- **[R23] OpenAI Agents SDK，Testing**  
  <https://openai.github.io/openai-agents-python/testing/>  
  官方文档区分应用自身编排、工具、handoff、guardrail 和 retry 的确定性测试与外部模型、网络协议、sandbox provider 的真实集成测试。支持不能只用 mock 证明生产 Agent 行为。

- **[R24] LangChain，LangGraph overview**  
  <https://docs.langchain.com/oss/python/langgraph/overview>  
  官方文档将 LangGraph 定位为低层编排框架和运行时，强调持久执行、流式处理、人工介入、长期状态，以及确定性步骤与 Agent 步骤混合。支持把编排和运行时单独建模；不支持固定目录布局。

- **[R25] LangChain，LangGraph persistence**  
  <https://docs.langchain.com/oss/python/langgraph/persistence>  
  “Checkpointer vs. store”区分线程范围的短期图状态和跨线程的长期数据，并说明内存存储重启会丢失、生产环境需要持久后端和保留策略。支持会话状态、长期记忆、恢复和留存策略分层。

- **[R26] Model Context Protocol，Architecture**  
  <https://modelcontextprotocol.io/specification/2025-06-18/architecture>  
  官方架构定义 Host、Client、Server、JSON-RPC 会话、能力协商和服务器隔离，并强调 Host 管理权限、生命周期和同意。支持工具边界、能力协商、权限和隔离设计；不等于 MCP 自动保证工具安全。

- **[R27] Google ADK，Evaluation**  
  <https://adk.dev/evaluate/>  
  官方资料说明可评估 Agent 轨迹、工具使用和最终响应，并区分单会话测试与多会话评估。支持对轨迹和最终结果同时验收；不证明评估集本身没有偏差。

- **[R28] Google ADK，Deployment Options**  
  <https://adk.dev/deploy/>  
  官方资料列出 Agent Runtime、Cloud Run、GKE 和任意容器等部署选择。支持部署责任分布在应用、运行时和平台；反驳固定 `runtime` 目录是通用标准。

- **[R29] Google ADK，Safety**  
  <https://adk.dev/safety/>  
  官方资料涉及身份与授权、guardrails、沙箱、评估与追踪、网络控制等安全边界。支持把身份、权限、沙箱和网络控制列入 Agent 工程清单；不证明采用 ADK 就自动完成安全治理。

- **[R30] OpenAI Agents SDK，官方仓库源码树**  
  <https://github.com/openai/openai-agents-python/tree/main/src/agents>  
  官方仓库树显示 `src/agents` 下有 `extensions`、`handoffs`、`mcp`、`memory`、`models`、`sandbox`、`testing`、`tracing` 等目录，根部还有 `docs`、`examples`、`integration_tests`、`SECURITY.md`、`Makefile`、`mkdocs.yml` 和 `pyproject.toml`。支持“成熟 Agent 工程能力分散在多个目录”的具体对照；不证明本项目应照搬这些目录。

- **[R31] Microsoft AutoGen，官方 README 与 Core 源码树**  
  <https://raw.githubusercontent.com/microsoft/autogen/main/README.md>  
  <https://github.com/microsoft/autogen/tree/main/python/packages/autogen-core/src/autogen_core>  
  官方 README 将项目分为 Core、AgentChat、Extensions 和 Bench；源码树可见 telemetry、code execution、memory、model context、models、tools、runtime、serialization 和 config 等入口。支持“Agent 工程能力存在分层和运行时边界”的对照；不证明 AutoGen 的目录或当前状态适合本项目。

### 14.5 生物事实与类比边界

- **[R40] Smithsonian Institution Human Origins Program，Introduction to Human Evolution**  
  <https://humanorigins.si.edu/education/introduction-human-evolution>  
  “Human evolution”与“The process of evolution”说明人类属于灵长类，人类与非洲大猿拥有共同祖先，演化涉及遗传物质及种群基因比例跨世代变化。支持生物事实部分；不支持把生物共同祖先等同于文档源代码。

- **[R41] NHGRI，Comparative Genomics Fact Sheet**  
  <https://www.genome.gov/about-genomics/fact-sheets/Comparative-Genomics-Fact-Sheet>  
  说明比较完整基因组用于识别相似与差异，保守 DNA 只反映部分序列的长期保留。支持“相似不等于完全相同”的边界；不支持把物种比喻成可互换的软件版本。

- **[R42] UC Berkeley Understanding Evolution，Misconceptions about evolution**  
  <https://evolution.berkeley.edu/teach-evolution/misconceptions-about-evolution/>  
  资料反驳“演化等于进步”“个体一生进化”和“自然选择产生完美适配”等常见误解。支持限制生物类比中的目的性和线性进步暗示。
