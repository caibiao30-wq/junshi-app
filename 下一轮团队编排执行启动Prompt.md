# 下一轮 Claude agent teams · 编排执行启动 Prompt

> **⚠️ 严正声明（最高优先，高于本文件一切条款）**：本文件**仅作参考**，可能**覆盖不够全面**。执行必须**具体问题具体分析、随机应变、禁止死板不懂变通**。若本文件任何条目与实际任务冲突、或不足以达成目标，**以达成高质量、高专业、可靠、可信的交付物为最终依归**；一切为了**最终的交付结果**，最终交付物必须达到用户满意。**严禁把本文件当教条照搬。**

> **用法**：将本文件全文作为任务描述，交由下一轮团队 Lead 调用 `/team ralph` 启动执行。Lead 照抄本文件即可开工；本文件自包含，不依赖上一轮对话上下文。
> **本轮已完成**：前置对齐（三份访谈权威锚、商业战略定调、team+ralph 流程准入、高层会议门、代理数量与推荐、技术栈方向）。**下一轮交付**：交付层 + 承载层 5 个文件，其中交付层两件（工程开发总文件 + 派生专业文件）**必须达到可直接进入代码开发工程**。
> **状态**：READY TO LAUNCH。

---

## 一、给下一轮团队 Lead 的启动指令

以 `/team ralph` 组合启动。将二至十节作为执行宪章，组织 team 管线（team-plan → team-prd →【高层会议门】→ team-exec → team-verify → team-fix，外层套 Ralph）。高层会议门全部无异议通过后方可进入 team-exec。

**最高产出判据**：交付层两件文件交付后，团队 Lead **可以直接拿着它们 Scaffold 工程、初始化依赖、定义 schema、写出并启动第一个可运行的最小闭环**。任何停留在「治理原则/抽象边界」而没有落到「可写代码的具体规格」的内容，视为未完成，退回 team-fix。

## 二、本轮交付物（下一轮团队必须产出）

### 交付层（必须 code-ready）

**1. 工程开发总文件（母文件 / 总内容总和）** —— **整个学业军师产品的总内容总和**，全局主文件，统摄全部功能组件与部件，是全部派生文件的**母文件 / 唯一源**。含：
- **全产品总览与范围**：学业军师是什么、最高责任、范围边界、非目标。
- **总技术栈清单**：以终局访谈暂定栈为方向（`02_访谈记录.md:3606-3629`）——Flutter/Dart、SQLite+SQLCipher、JSON Schema/OpenAPI、Node/TS/Fastify、React/Vite/TS、版本化增量协议 + changelog；首版同步不引入 CRDT。**团队在 team-prd 确认具体版本号并标注「待验证」**，不得编造未列栈。
- **总工程目录结构**：顶层目录树（学生端 App / 后端代理 / 底层确定性设施 / 数据存储），每目录一句话职责。
- **总模块划分与依赖图**：全产品模块清单、每个模块的输入/输出/依赖、模块间调用方向。**至少列明「学业规划」「判断引擎」「客观证据模型」「唯一状态源与生命周期」「权限与确定性强制」「证据审计链」「业务回滚语义」为不可替换自持模块，不得外包**。
- **总数据模型（schema）概览**：核心实体的字段定义（可落为 JSON Schema/SQL DDL）；**各组件细则字段在对应派生文件展开，总文件只给概要**。
- **总 API 面**：学生端与后端代理、后端与底层设施的关键接口概要；**接口细则在对应派生文件展开**。
- **总配置与凭证接入**：模型 API 凭证如何注入、绝不明文入库。**以讯飞 iFlytek Cloud 的 HMAC-SHA256 鉴权签名流程作为凭证处理示例**（示例仅说明签名/注入机制，不锁定供应商）；最终云端模型机构/API 供应商属用户门（见 §六），团队不得擅自选定。
- **首个可运行最小闭环**：从「学生提交一份学校材料 → 证据登记 → 判断引擎产出候选 → 形成一条规划 → 转成任务」的可执行路径骨架，标注启动命令与验证点。
- **派生文件索引与交叉引用**：列出全部派生文件清单、各自的继承声明、相互引用关系；作为跨文件一致性核对基准。

**2. 派生专业文件（由总文件派生的细则）** —— **由总文件派生而来的更细细则，按功能组件 / 部件各一份独立文件**，覆盖该组件代码落地的方方面面，全部 `inherits_from` 总文件（母源）。功能组件细则清单（每组件一份独立派生文件）：
- **学业规划（最高责任层）**：规划对象链、位置/目标/差距/路径/任务/教与授/检验/反馈/修正的对象字段与因果链定义。
- **判断引擎（认知层）**：输入证据→理解组织→判断候选→证据校验→强度判定→降级/补证/停止→结构化输出→规划层裁决 的具体接口与契约。
- **客观证据模型（证据设施）**：外部证据/内部证据的字段表、来源登记、状态、审计字段（可落为 schema）。
- **唯一状态源与生命周期（状态设施）**：状态枚举、合法迁移表、迁移触发条件、候选/生效分离（可落为状态机定义与表结构）。
- **权限与确定性强制（确定性设施）**：确定性代码强制点清单（证据字段完整校验、状态迁移合法性、不可逆拦截），不靠 prompt。
- **证据审计链（审计设施）**：审计字段表、防篡改要求、写入链路。
- **业务回滚语义（回滚设施）**：回滚单元/版本语义/错误补偿的具体定义。
- **多代理编排（总军师/学科军师/专项 Agent）**：总军师=完整学业全局、学科军师=单学科、专项/责任域 Agent 的契约（输入/输出/交接/冲突裁决）。
- **教与授机制**：把规划目标转化为讲解/练习/检验/纠错的具体流程与判定（「看过讲解≠掌握」区分）。
- **学业资产（长期沉淀）**：从证据提炼资产候选、生命周期、复核修订撤回删除、跨阶段交接。
- **学生交互与前端**：统一入口、战场总框架（前线-后方）+ 分级学科窗口；不暴露内部 Agent 拓扑。
- **候选组件整合（0.5-1 复用落位）**：各复用候选如何落到对应模块的 How（可据以 Scaffold）。

**每个派生文件必须覆盖该组件代码落地的方方面面**：字段定义/schema、状态机与合法迁移、API/接口契约、模块边界与依赖、实现约束与确定性校验点、验证样例与验收入口，并在文件头写 `inherits_from: <总文件 baseline_id>`。
**派生文件之间关系**：可相互引用，但**以总文件为唯一母源**；派生文件间或与总文件不一致时，回总文件裁决，不静默合并。

**不可替换自持核心 = 派生文件组成部分**：「客观证据模型」「唯一状态源与生命周期」「权限与确定性强制」「证据审计链」「业务回滚语义」这五个**不可替换自持核心，同为派生专业文件的组成部分**，各有一份独立细则文件（见上方功能组件清单）。承载层「产品自持硬边界与门禁」是对这些核心细则的**汇总性硬门与准入核查**（进入工程代码前的门），**不重复承载细则内容**——细则在派生文件，门禁在承载层。

**3. 候选开源组件** —— 0.5-1 复用候选，**直接绑定到具体模块**：
- 事实取证 / HOW-WHAT 登记 / 准入评估 三子寄存器分离；
- 每个候选**落到它承载哪个模块的哪个 How**（如某编排库 → 多代理编排模块），不得做成「纯概念清单」；
- 逐案核验【许可证/依赖/范围/本地运行/迁移/替换】；**暂定，不写成已采纳**；未核验不得进 Scaffold。

### 承载层（以支撑代码开发为口径，非抽象治理）

**4. 可回溯交付总表** —— 结论—状态—来源—责任—影响—验收—用户门 总表；每条结论可归因到 `文件:行号` 或 schema/接口；用户裁决门矩阵。

**5. 产品自持硬边界与门禁** —— 五类硬边界的具体字段与确定性承载边界（可直接据此写校验代码）；进入工程代码前的准入硬门（架构符合性/V-07/G1 回滚/G2 审计防篡改）。

## 三、权威锚（下一轮团队取证唯一遵循）

| 领域 | 权威来源 | 处理 |
|---|---|---|
| 前端设计 | **第二次访谈报告**（`访谈资料/学业军师产品正式方案访谈/03_访谈报告.md`） | 战场总框架（前线-后方）+ 分级学科窗口；学生统一入口，不暴露内部 Agent 拓扑 |
| 产品本体/其余 | **第三次终局访谈定案**（`访谈资料/学业军师终局访谈/02_访谈记录.md`） | 学业规划最高责任；判断引擎；证据/状态/权限/审计/回滚；全学段/全学科统一结构；本地权威数据+云端仅模型API；CRDT 首版暂不引入 |
| 工程方法 | **开发指导文件**（`agent-framework.pdf` 主 > `22365_19_Agents_v8.pdf` 次 > `a-practical-guide-to-building-agents.pdf` 并入） | 落地/可行性/可靠性的顶层工程权威；不反向定义产品本体 |
| 复用输入 | **候选开源组件**（`工程开发文件/_候选组件名单.md`） | 0.5-1 暂定候选，逐案绑定模块；不自动采纳 |
| 历史材料 | `工程开发文件/` 下 P3 前置/共识/候选等 | 未装 OMC 前的错误尝试，不作产品定义权威；仅历史存档 |

## 四、商业战略与范围策略（最高约束）

- 学业军师是**裁缝产品**——0.5-1 拼接缝合复用成熟开源部件，避免 0-1 不稳定；信任 **AI 与 GitHub** 力量。用户是**商人**，追求利益与效率，唯一目标是把设想**打通、跑通、达成目的**。
- **首版把一切「不可替换」部分构建完毕**再优化/升级/替换。
- **不可替换（自持硬核）**：学业规划最高责任、判断引擎、客观证据模型、唯一状态源与生命周期、权限与确定性强制、证据审计链、业务回滚语义、全学段/全学科统一结构、学生主体与责任边界。
- **可替换（0.5-1 复用/拼接）**：学科内容、前端框架、模型供应商、检索/存储/编排组件等。

## 五、流程准入（team+ralph 标准管线 + 高层会议门）

```
Ralph 外层持续循环（iteration 1..N）
 └─ Team 管线：
    team-plan → team-prd →【高层会议门】→ team-exec → team-verify
        │                                        └─通过→ Ralph architect 验证
        │                                        │          └─通过→ 完成/取消
        │                                        └─失败→ team-fix → 回 team-exec→team-verify
        └─ fix 超 max_fix_loops → Ralph 递增 iteration 重跑
        └─ Ralph 超 max_iterations → 终态 failed
取消任一模式 → 双双取消（取消耦合）
```

### 高层会议门（team-prd 与 team-exec 之间 · 强治理闸门）

模拟公司内部对产品的高层决策会，**执行前最后一道闸门，全部无异议通过才放行**。

- **参与者**：`architect`（产品语义）+ `code-reviewer`（工程机制/可实现性）+ `security-reviewer`（证据/权限/合规）+ `tracer`（证据合规）+ 主控总协调。
- **审查对象**：team-prd 产出的范围 + 验收标准 + **技术可实现性方案**。重点查：
  - 交付物是否 **code-ready**（能否据此 Scaffold、定义 schema、启动最小闭环）；
  - 是否根据终局暂定栈落地具体技术，还是停留在抽象原则；
  - 是否违反权威锚、静默合并访谈冲突、把待确认写成已定、候选组件被误当已采纳、自持硬边界被外化。
- **程序**：各视角独立批判 → 往返互驳 → 收敛；每项争议记录【结论—状态—依据—影响—升级对象】；引用不替代记录。
- **通过条件**：全部无异议通过（每项争议关闭或显式升级到用户门）。
- **未通过**：回 team-prd/team-plan 修订，再开会（≤3 次）；超限 → 升级用户门，**不得带病进 team-exec**。

## 六、证据冲突处理（不得静默合并）

开工前建立「来源—状态—冲突」矩阵，以下须为用户裁决门或待确认/待验证：
- 首版学段/年龄/学科/典型任务（`02_访谈记录.md:926,1379-1387` 待确认）。
- 回滚单元/版本语义/错误补偿（G1）。
- CRDT/同步（终局：暂不引入，待验证，`02_访谈记录.md:3616-3619,3765-3774`）。
- 学校接入（方向 13 根级修正：学生间接承接客观材料，教师主观反馈不作输入/效果裁判，`02_访谈记录.md:4610-4666`）。
- 候选组件（DeepTutor 等均未采用，逐案能力级评估，`02_访谈记录.md:2277-2295,2504-2522`）。
- 云端模型机构/API 供应商、成本、稳定性与数据条款（终局 §2.3 用户门；iFlytek 仅为凭证处理示例，非已选供应商）。

## 七、验收标准（S1–S8 + code-readiness，含测量口径）

- **S0 code-readiness（新增，最高硬门）**：交付层两件文件产出后，团队 Lead 能直接据此执行「创建工程目录 → 安装依赖 → 初始化 schema → 定义 API → 启动最小闭环」。若任一环节因文档缺字段/schema/接口/命令而无法推进，即未通过。
- **S1 状态标签覆盖率**：分母=全部结论条目；判定=100%（原始用户判断/代拟/确认/待确认/待验证/已被修正）；证据=统计报告。
- **S2 关键结论定位覆盖率**：判定=100% 含 `文件:页码/行号`；证据=定位清单。
- **S3 字段完备率**：每份文件具 目的/范围/输入/输出/责任/完成定义/验收标准/实际证据/未验证边界；缺失=0；schema 字段必须有类型/约束/来源。
- **S4 边界无越界**：grep 为筛查非判定，逐命中语义分类（允许引用/历史/越界）；越界撤回清零。
- **S5 最小闭环可读通**：模板固定 输入→步骤→预期输出→记录；L1 机制 + L2 故障注入验收入口，作者不自审。
- **S6 规范一致性**：从冻结总文件生成规范条目 ID 集，每派生逐条映射 `covered/N-A/例外`；状态/权限/回滚/审计/范围高风险字段全量对照；差异未清零终版硬停。
- **S7 链接/只读质检 + 可执行性**：Markdown/链接/交叉引用/表格扫描错误=0；**schema 可通过校验、API 定义可通过 OpenAPI 校验、启动命令可执行**。
- **S8 审计轨迹**：门通过/硬停重开/争议关闭依据/用户确认/未决清单齐全。

## 八、候选组件暂定机制

候选组件绑定到具体模块后逐案核验【许可证/依赖/范围/本地运行/迁移/替换】，**不得写成已采纳**；处理建议限 复用/改造/受控整合/隔离/暂缓/放弃/自建，由下一轮团队逐案取证 + 用户准入门裁决；未核验不进入 Scaffold。

## 九、一致性协议与成本护栏（起草期闭环）

1. 冻结基线：总编签发 `baseline_id`。
2. 继承声明：每派生文件头 `inherits_from: baseline_id`。
3. 变更传播：上游变更签新 baseline_id，标记受影响派生 `STALE/PENDING`。
4. 单一写权：每主题独立责任人/写入者/冲突裁决者。
5. 冲突裁决：只回上游总文件或用户决策簇，禁总编静默择一。
6. 向上修订：派生发现缺口→变更请求→总编判定骨架（重冻结+STALE）或待决（挂用户门）。
7. 放行核对：终版前「基线版本—派生引用—差异/例外」三表核对。
8. 继承边界：组件 HOW/编排标 `indep`，不伪造继承来源。

**成本护栏**：总代理≤10=任一时刻并发实例数；口径=专题子任务+ralph 循环，不含总编/verifier/组件准入独立通道；重试计一次；ralph 单循环 ≤3×≤2。

## 十、代理数量与推荐（表）

### 表 1：代理数量（按工作量与关键程度；并发峰值 ≤10）

| 阶段 | 环节 | 代理类型 | 数量 | 依据 |
|---|---|---|---|---|
| team-plan | 分析与计划 | `explore`+`planner` | 2 | 盘点+拆解 |
| team-prd | 需求与验收 | `analyst`+`critic` | 2 | 定义+挑战 |
| **高层会议门** | 执行前决策会 | `architect`+`code-reviewer`+`security-reviewer`+`tracer` | 4 | 产品/工程/证据/合规 |
| team-exec | 并行执行 | `writer`×2+`executor`×2+`architect`+`test-engineer` | 5-7 | 总文件/派生/候选/总表/硬边界/规格可执行 |
| team-verify | 验证 | `verifier`+`security-reviewer`+`code-reviewer` | 2-3 | code-readiness+S1-S8+安全+架构 |
| team-fix | 修复 | `executor`/`debugger` | 1-2 | 按缺陷类型 |
| Ralph | 完成验证 | `architect` | 1 | 完成前核定 |

### 表 2：推荐代理

| 阶段 | 推荐代理 | 角色 | 何时启用 |
|---|---|---|---|
| team-plan | `explore` | 只读盘点 | 进入 plan 前 |
| team-plan | `planner` | 战略拆解 | 复杂任务必用 |
| team-prd | `analyst` | 需求→验收 | 需求不清必用 |
| team-prd | `critic` | 范围挑战 | 每份 PRD |
| 高层会议门 | `architect` | 产品语义辩论人 | 产品争议 |
| 高层会议门 | `code-reviewer` | 工程机制/可实现性 | 技术/工程争议 |
| 高层会议门 | `security-reviewer` | 证据合规辩论人 | 证据/权限/合规 |
| 高层会议门 | `tracer` | 证据合规辩论人 | 证据冲突/缺口 |
| team-exec | `writer` | 规格撰写 | 规范文件 |
| team-exec | `executor` | schema/接口/代码骨架 | 直接可落码的规格 |
| team-exec | `architect` | 硬边界语义 | 自持硬边界 |
| team-exec | `test-engineer` | 最小闭环可执行验证 | code-readiness |
| team-verify | `verifier` | 验证必跑 | 每阶段产物 |
| team-verify | `security-reviewer` | 安全审查 | 证据/权限/密钥 |
| team-verify | `code-reviewer` | 架构审查 | 大改动 |
| team-fix | `executor` | 缺陷修复 | 验证失败 |
| team-fix | `debugger` | 根因/编译 | schema/编译/回归 |
| Ralph | `architect` | 完成验证 | 每次循环完成 |

## 十一、启动命令（Lead 直接执行）

```bash
/team ralph "依据《下一轮团队编排执行启动Prompt》执行。**严正声明优先**：以下仅作参考、可能覆盖不全面，须具体问题具体分析、随机应变、禁止死板变通，一切以达成高质量/高专业/可靠/可信的交付物为依归，最终交付物必须让用户满意。交付 工程开发总文件（母文件/总内容总和）/ 派生专业文件（由总文件派生的逐功能组件细则，含不可替换自持核心）/ 候选开源组件（交付层，前两件必须 code-ready 可直接进入代码开发）+ 可回溯交付总表 / 产品自持硬边界与门禁（承载层）。严格遵循 team-plan→team-prd→【高层会议门】→team-exec→team-verify→team-fix 管线，外层套 Ralph；高层会议门全部无异议通过才进 team-exec；前端=第二次访谈，其余=第三次终局；候选暂定绑定模块逐案核验；按0.5-1划分不可替换自持核心 vs 可替换复用；以终局暂定栈(Flutter/Dart、SQLite+SQLCipher、Node/TS/Fastify、React/Vite/TS、OpenAPI/JSON Schema)为技术方向；S0 code-readiness+S1-S8验收；证据冲突登记不静默合并。工作目录 /Users/caibiao/orca/workspaces/junshi-app/docs"
```

## 十二、完成判定

交付层两件文件满足 **S0 code-readiness**（Lead 能直接据此 Scaffold、初始化 schema、启动最小闭环）+ S1-S8 + 审计轨迹 + 用户终版确认后，标记 READY 交付；否则保持 PENDING，登记差异与未决用户门。