# 候选草稿：运行时与质量组 学生端能力域开源组件来源表

> 状态：联网取证草稿（非产品决策、非准入门禁结论）｜取证时间：2026-09-15
> 研究人员：运行时与质量组（D）
> 范围：仅两组学生端能力域——① Agent 运行时 / 工具协议；② 测评 / 回归验证。只读网络取证，未做本地运行/CIA/依赖验证。
> 硬边界：所有条目一律 `[候选/待验证]`；本表不作「是否纳入首版」预筛结论，门禁决策由用户/后续工程阶段拍板。
> 证据契约：依据 `.claude/commands/research-evidence.md` 字段协议与 `research` 技能一手来源契约；每条结论追溯到一手来源（官方 LICENSE/README/release/发布页/GitHub API），不引用二手转述。

## 能力域说明与映射

| 本表能力域 | 对应证据包线索 | 说明 |
|---|---|---|
| ① Agent 运行时 / 工具协议 | `_证据包` T8（0.5→1 底座候选）、T9（LangGraph/Temporal 机制）、G5（外部组件准入） | 本地 Agent 运行循环、工具调用协议、结构化输入输出、流式会话、状态持久化 |
| ② 测评 / 回归验证 | `_证据包` T9 L191（Inspect AI/Ragas/Langfuse 评测回归）、G5 | Agent/workflow 评测、指标回归、可观测性/质量验证 |

## 取证方法与实际调用（skill-first 记录）

| 能力 | 调用结果 | 说明 |
|---|---|---|
| `research` 技能 | ✅ 成功加载并按契约执行 | 一手来源契约：DeepTutor 的 LICENSE/README 均追到官方仓库 commit 印记，非二手转述 |
| `md-writing` 技能 | ✅ 成功加载并产出 | 本文件按该规范撰写；`check_markdown.py` 运行通过 |
| `agent-reach` 技能（web 路由/Jina Reader） | ⚠️ 加载成功，**实际执行被环境阻塞** | Jina Reader 需 `curl https://r.jina.ai/<url>`，curl 被安全策略 deny；WebFetch 域校验拒绝 `r.jina.ai`；mcporter 列表无 `web-reader` MCP 服务。**阻塞记录**：Jina Reader 不可达 |
| github MCP / `gh` CLI | ✅ 正常 | 作为经授权的降级路径直接读官方一手来源与仓库元数据（较 Jina Reader 更底层一手） |
| 推理/模型基础能力 | — | 仅用于组织证据，不作为来源 |

**网络抓取阻塞说明**：lead 要求统一走 Jina Reader，但当前环境三处均不可达（curl deny、WebFetch 拒 `r.jina.ai`、无 web-reader MCP）。已按 skill-first 记录阻塞，并经授权降级到 github MCP 直接读取官方仓库一手文件（README/LICENSE/release），证据等级不降。本地如需复核官方文档 `deeptutor.info`，属下一步本地验证。

## 证据等级说明

- **一手·官方文件**：直接读官方仓库 `LICENSE`/`README`/release，附 commit SHA 印记。
- **一手·GitHub API 元数据**：`gh api`/GitHub MCP 返回的 owner/版本/license/stars/更新。
- **待核/本地**：依赖、网络、隐私、供应链、回滚、准入判断，需本地验证，未核不表述为已确认。

---

## ① Agent 运行时 / 工具协议

| 候选组件 | 仓库/主页 | 用途（推断） | 许可证（证据等级） | 版本（证据等级） | 来源 URL | 取证时间 |
|---|---|---|---|---|---|---|
| **DeepTutor** | `HKUDS/DeepTutor`（docs: deeptutor.info） | `[候选/待验证]` 底座候选。详见下方【DeepTutor 深入核验】。推断对应 T8「优先评估底座候选」 | Apache-2.0（【一手·官方文件】LICENSE 原文，Copyright 2025 Data Intelligence Lab, The University of Hong Kong；SHA `ae1dbfc`） | v1.6.8（【一手·官方文件】README release tag + gh releases/latest 一致） | https://github.com/HKUDS/DeepTutor | 2026-09-15 |
| **LangGraph** | `langchain-ai/langgraph` | `[候选/待验证]` 有状态图编排 agent 运行时：graph/checkpointer 状态持久化、流式、human-in-the-loop。推断覆盖「运行循环+状态持久化+流式会话」。映射 T9（LangGraph 机制候选） | MIT（【一手·GitHub API】license.spdx_id=MIT） | sdk==0.4.4（【一手·GitHub API】release tag，monorepo 非语义化） | https://github.com/langchain-ai/langgraph | 2026-09-15 |
| **LangChain** | `langchain-ai/langchain` | `[候选/待验证]` 组件库里层：tool calling、结构化 I/O、链式抽象。推断作为 LangGraph 依赖层 / 组件来源。映射 T9 `[GOOG]` create_react_agent 引用 | MIT（【一手·GitHub API】） | langchain-core==1.6.3（【一手·GitHub API】release tag） | https://github.com/langchain-ai/langchain | 2026-09-15 |
| **MCP（Model Context Protocol）规范** | `modelcontextprotocol/modelcontextprotocol` | `[候选/待验证]` 工具协议标准：工具调用/结构化资源/会话复用。推断覆盖「工具协议」协议层候选。许可证在「MIT → Apache-2.0」过渡期（LICENSE 首行，属一手事实） | MIT→Apache-2.0 过渡（【一手·官方文件】LICENSE 原文首行；SPDX 标 NOASSERTION） | 无版本 tag；最近 push 2026-07-28（【一手·GitHub API】） | https://github.com/modelcontextprotocol/modelcontextprotocol | 2026-09-15 |
| **Instructor** | `567-labs/instructor`（原名 `jxnl/instructor`，自动重定向） | `[候选/待验证]` 结构化输出：Pydantic 校验 + 重试 + 函数调用。推断覆盖「结构化输入输出」 | MIT（【一手·GitHub API】） | v1.17.0（【一手·GitHub API】release tag） | https://github.com/567-labs/instructor | 2026-09-15 |
| **OpenAI Agents SDK** | `openai/openai-agents-python` | `[候选/待验证]` 轻量 agent 运行循环：handoffs、guardrails、sessions（会话持久化）、streaming。推断覆盖「运行循环+流式+会话状态」。映射 T9 云端大模型语境 | MIT（【一手·GitHub API】） | v0.22.2（【一手·GitHub API】release tag） | https://github.com/openai/openai-agents-python | 2026-09-15 |
| **DSPy** | `stanfordnlp/dspy` | `[候选/待验证]` 声明式 agent/程序：模块化 prompt 编程、编译式优化、内建 evaluator。以「运行时」域纳入。推断与提示词研发/指标拟合相关 | MIT（【一手·GitHub API】） | 3.3.1（【一手·GitHub API】release tag） | https://github.com/stanfordnlp/dspy | 2026-09-15 |
| **Temporal** | `temporalio/temporal` | `[候选/待验证]` 持久化工作流时长执行（durable execution）：状态持久化、重试、确定性重放。推断覆盖「状态持久化/长期运行稳定性」；对学业军师本地权威状态语义存在适配缺口（推断）。映射 T9 | MIT（【一手·GitHub API】） | v1.32.0（【一手·GitHub API】release tag） | https://github.com/temporalio/temporal | 2026-09-15 |
| **Letta** | `letta-ai/letta` | `[候选/待验证]` 内存优先 agent 运行时：长期记忆/状态持久化/多代理。推断覆盖「状态持久化+会话」 | Apache-2.0（【一手·GitHub API】） | 0.16.8（【一手·GitHub API】release tag） | https://github.com/letta-ai/letta | 2026-09-15 |
| **Pydantic AI** | `pydantic/pydantic-ai` | `[候选/待验证]` Pydantic 原生 agent 框架：结构化 I/O、tool 注入、流式、RunContext。推断覆盖「结构化 I/O+运行循环」 | MIT（【一手·GitHub API】） | v2.43.0（【一手·GitHub API】release tag） | https://github.com/pydantic/pydantic-ai | 2026-09-15 |

## ② 测评 / 回归验证

| 候选组件 | 仓库/主页 | 用途（推断） | 许可证（证据等级） | 版本（证据等级） | 来源 URL | 取证时间 |
|---|---|---|---|---|---|---|
| **Inspect AI** | `UKGovernmentBEIS/inspect_ai`（官方名 Inspect AI） | `[候选/待验证]` LLM 评测框架（Python）：task/solver/metric 编排、指标回归、隔离 sandbox。推断覆盖「Agent/workflow 评测+指标回归」。映射 T9 L191 | MIT（【一手·GitHub API】license.spdx_id=MIT） | GitHub 无 release tag（`未取到`；以 PyPI `inspect-ai` 版本为准，待本地核） | 主：https://github.com/UKGovernmentBEIS/inspect_ai ；评测集合：https://github.com/UKGovernmentBEIS/inspect_evals | 2026-09-15 |
| **Ragas** | `vibrantlabsai/ragas`（原名 `explodinggradients/ragas`，已重定向） | `[候选/待验证]` RAG/Agent 评测指标（faithfulness/relevancy/上下文精度等）与指标回归。映射 T9 L191 | Apache-2.0（【一手·GitHub API】） | v0.4.3（【一手·GitHub API】release tag） | https://github.com/vibrantlabsai/ragas | 2026-09-15 |
| **Langfuse** | `langfuse/langfuse` | `[候选/待验证]` LLM 可观测/追踪/回放评测平台。推断覆盖「可观测性+质量验证」。**许可证需本地复核**：SPDX 标 NOASSERTION，根目录另有 LICENSE，README 语境存在混合/源码可用分区 | 混标待核（【一手·GitHub API】SPDX=NOASSERTION；需读 LICENSE 定版，本地复核） | v4.36.0（【一手·GitHub API】release tag） | https://github.com/langfuse/langfuse | 2026-09-15 |
| **DeepEval** | `confident-ai/deepeval` | `[候选/待验证]` LLM/Agent 评测框架与指标回归（assertions/metrics/CI）。推断覆盖「回归验证+质量检查」 | Apache-2.0（【一手·GitHub API】） | python-v4.2.0（【一手·GitHub API】release tag） | https://github.com/confident-ai/deepeval | 2026-09-15 |
| **promptfoo** | `promptfoo/promptfoo` | `[候选/待验证]` prompt/Agent 评测与回归（CI/cache/差量比对）。推断覆盖「提示词/行为指标回归」 | MIT（【一手·GitHub API】） | 0.123.0（【一手·GitHub API】release tag） | https://github.com/promptfoo/promptfoo | 2026-09-15 |
| **OpenTelemetry Python** | `open-telemetry/opentelemetry-python` | `[候选/待验证]` 可观测标准 API/SDK（trace/metric/log）。推断覆盖「运行可观测性」；与 `_证据包` T6 语境一致（外部信源无防篡改审计链，属产品自持边界） | Apache-2.0（【一手·GitHub API】） | v1.44.0（【一手·GitHub API】release tag） | https://github.com/open-telemetry/opentelemetry-python | 2026-09-15 |

---

## 【DeepTutor 深入核验】（底座候选核心线索，`_证据包` T8）

> 状态：`[候选/待验证]`，**非已采用底座**。以下均为一手来源（官方 LICENSE/README，附 commit SHA），标注所属证据。

### 一手来源事实
- **定位**（【一手·官方文件】README 标题）：「DeepTutor: Lifelong Personalized Tutoring」（终身个性化辅导）。
- **许可证**（【一手·官方文件】LICENSE 原文，SHA `ae1dbfc`）：Apache License 2.0；版权声明「Copyright 2025 Data Intelligence Lab, The University of Hong Kong」（即 HKUDS，香港大学数据智能实验室）。README 亦含 `License-Apache_2.0` badge 与「Licensed under the Apache License 2.0」。
- **版本线**（【一手·官方文件】README Releases 节 + gh releases/latest）：最新 v1.6.8；历史版本 v1.3.1→v1.6.6 周级密集发布（2026 全年活跃）。
- **能力域信号**（【一手·官方文件】README 原文）：多代理 agent harness；RAG 文档解析（含「LlamaIndex ingestion … Document Parsing engine」）、Web search（2026.8 六个新 provider）；**MCP Services 商店**（2026.7.31 v1.5.7「per-account MCP Services store, 101 CLI Apps」）；记忆/会话（CLI 链式 stateful session：chain turns in one stateful session, reuse session id）；Docker 容器化；CLI Agent-Native 接口（`deeptutor_cli`）；EduHub 与 Skills 生态；CLI/后端/Web 分层与本地 `工程代码库目录框架图/DeepTutor目录框架图.md` 服务层职责相符。

### 推断（非来源结论，本表研究者）
- 对应 `_证据包` T8 L174：可作为「经核验的通用 Agent 运行、学习材料、检索、练习」底座候选能力来源。
- DeepTutor 未覆盖且不由其决定的产品自持边界（`_证据包` T8 L175/L182）：证据底层设施、全局/学科分层、责任域会话、证据优先判断、监察、审计链、回滚语义、家长学习推送——这些须学业军师自持/自建。
- 底座候选地位：依 `_证据包` T8 L179/L180（访3 5-1/5-3）定位为「候选工程底座与能力来源，而非已确认采用的首版底座」；「一主多专」与「单 Agent loop」间存在适配缺口（L2386）。本表沿此，不表述为「已采用」。

### 待核（本地验证，未做）
- 本地目录（`/Users/caibiao/Downloads/DeepTutor-main`，见框架图）与线上 commit `a7c30a5`（README）/ `ae1dbfc`（LICENSE）的版本一致性。
- 依赖、部署、网络、隐私、供应链、回滚、许可证传播（Apache-2.0 派生条款）等本地/工程验证。

---

## 未取到 / 未能核实清单

| 项目 | 原因 | 影响 |
|---|---|---|
| **Inspect AI GitHub release tag** | `gh api /releases/latest` 返回 404（未发布 GitHub release） | 版本以 PyPI `inspect-ai` 为准，需本地复核；来源 URL 可打开 |
| **Langfuse / MCP 规范单一 SPDX 许可证** | GitHub SPDX 均返回 `NOASSERTION`；MCP 已核 LICENSE 首行为「MIT→Apache-2.0 过渡」；langfuse 为混合/需复核分区 | 许可证结论标「待核」，不放大为单一许可 |
| **Jina Reader（agent-reach web 路由）抓取** | curl deny + WebFetch 拒 `r.jina.ai` + 无 web-reader MCP，三处阻塞 | 已降级 github MCP 读一手官方文件，证据等级不降 |
| **DeepTutor 本地目录 ↔ 线上 commit 一致性** | 本表只读网络；本地归纳来自框架图（未读代码正文） | 归入 `[候选/待验证]`，base commit 需本地复核 |

## 事实 / 推断分层

- **事实（已联网核到）**：仓库 owner/URL、许可证（MCP 例外）、最新 release tag、最近 push、重定向信息（instructor→567-labs、ragas→vibrantlabsai）；DeepTutor 定位/许可证/版权/版本线/能力域信号（一手 README/LICENSE）。
- **推断（本表研究者，非来源结论）**：每项「用途」的「覆盖能力域」表述；DeepTutor 对应 T8 底座候选；Temporal/Agent 运行时与学业军师一主多专之间的适配缺口。
- **待核验**：所有候选的本地安装/依赖/网络/隐私/供应链/回滚与「纳入首版」结论。

## 结论边界

- 本表**无门禁预筛结论**，不判定任何组件「纳入/排除首版」；凡集成责任、稳定性、审计链等由学业军师自证（见 `_证据包` T8 L178、T9 L182）。
- 学业规划最高责任 / 判断引擎 / 证据 / 审计链等产品自持边界不因本表任何条目而外化。