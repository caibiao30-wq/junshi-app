# 底座候选仓库来源表草稿（学生端底座）

> 状态：**候选草稿 / 仅记录可核来源事实与情境推断**，全部条目 `[候选/待验证]`。
> 用途：为学业军师 0.5→1 工程开发前置工作提供「学生端底座候选仓库」的可核证据来源表，不构成任何采用/拒用/门禁预筛判断。
> 取证时间：2026-09-15（联网 + github MCP 实测）。时点后版本/许可可能变化，引用前需复核。
> 纪律：遵循 `_证据包.md` T8/C4 —— DeepTutor 等一律为「候选工程底座与能力来源」，**候选≠采用**，不表述为「已采用 DeepTutor」，不进入本草稿的门禁预筛（产品责任/安全评级/是否纳入首版均不由本文件承担）。

---

## 0. 本文件职责与边界

- **只读网络事实**：owner/repo、版本、许可证、仓库主页为本次实测核到的**事实**（附可核 URL）。
- **情境推断**：「对学业军师学生端底座能支撑什么」「0.5→1 覆盖面」为本文档标注的**推断**，统一 `[推断/候选]`，需后续 0.5-2 只读审查与运行验证，此处不作结论。
- **不越门禁**：不承担候选准入 12 项检查、五类门禁预筛、反事实本体测试、是否纳入首版判断（见 `_证据包` G5 外部组件准入）。
- 能力域对照基准：`_证据包.md` T8 候选来源能力域清单（Agent 运行时 / 多层协作 / 证据设施 / 监察 / 会话治理 / 知识检索 / 学业资产 / 前端 / 工具协议 / 工程质量）。

---

## 1. DeepTutor —— 学生端本体底座候选（核心线索，`_证据包` T8）

| 项 | 值 | 证据等级 |
|---|---|---|
| 仓库 | `HKUDS/DeepTutor` | [候选/事实-已核] |
| 仓库/主页 | https://github.com/HKUDS/DeepTutor ；官网 https://deeptutor.info/ | [候选/事实-已核] |
| 一句话定位 | DeepTutor: Lifelong Personalized Tutoring（终身个性化辅导 Agent） | [候选/事实-已核] |
| 语言/栈 | Python（后端 core + Next.js/TS 前端 web + deeptutor_cli + tests 镜像后端域） | [候选/推断] |
| 许可证 | **Apache-2.0**（LICENSE 全文实测） | [候选/事实-已核] |
| 版本 | **v1.6.8**（2026-09-14 发布） | [候选/事实-已核] |
| 来源 URL | 源码 https://github.com/HKUDS/DeepTutor ；Release https://github.com/HKUDS/DeepTutor/releases/tag/v1.6.8 ；LICENSE https://raw.githubusercontent.com/HKUDS/DeepTutor/main/LICENSE | [候选/事实-已核] |
| 取证时间 | 2026-09-15 | 事实 |

- 顶层结构（依据 `DeepTutor目录框架图.md`，为既有结构入口）：根目录 `pyproject.toml`/`requirements.txt`/`Dockerfile`/`compose.yaml`/`README`/`AGENTS.md`/`LICENSE`；核心子目录 `deeptutor/`（后端核心包，按职责分域）、`web/`（Next.js 前端）、`deeptutor_cli/`（命令行客户端）、`tests/`（按后端域镜像）、`scripts/`、`requirements/`、`assets/`、`docs/`、`packaging/`、`.github/`。
- **能力域覆盖（`[推断]`，对照 T8 清单）**：Agent 运行时（多 Agent 分级运行）、知识检索（GraphRAG/rag/kb_files）、学习材料与练习（视频学习、quiz、自适应问答、mastery 状态）、前端（web）、CLI、工程质量（测试/容器化/包管理、`REASONING_SAFETY_CHECKLIST.md`）。**未见明确覆盖**：证据底层设施、全局/学科战场分层、责任域会话、监察、学业资产生命周期（DeepTutor 的 mastery 状态非学业军师语义的资产生命周期）、学校现实反馈、家长侧推送 —— 依 `_证据包` T8[访1 L11755-11757]，这些须学业军师自行建设或深度重构。
- **0.5→1 覆盖部件（`[推断/候选]`）**：T8 明确列出「复用其经核验的通用 Agent 运行、学习材料、检索、练习和工程基础能力」。注意 `_证据包` C4：访谈3 已将 DeepTutor 收敛为「优先评估候选、未达产品集成验收、不进入首版底座」，单 Agent loop 与学业军师「一主多专」存在适配缺口 —— 本文不表述为已采用。

---

## 2. dify —— LLM 应用 / 工作流 / RAG 平台候选

| 项 | 值 | 证据等级 |
|---|---|---|
| 仓库 | `langgenius/dify` | [候选/事实-已核] |
| 仓库/主页 | https://github.com/langgenius/dify | [候选/事实-已核] |
| 一句话定位 | 开源 LLM App 开发平台：工作流编编排、RAG 知识库、Agent、多模型供应商接入 | [候选/推断] |
| 语言/栈 | `api/`（Python 后端）+ `web/`（Next.js/React 前端）+ `dify-agent`/`dify-agent-runtime`（Agent 子工程）+ `sdks/` 多语言 SDK + `docker/` + `e2e/` | [候选/推断] |
| 许可证 | **修改版 Apache-2.0（自定义，带额外条件）**：可商业使用；满足特定条件下须向生产者获取商业许可证（含 LOGO/版权信息不可删除、frontend 指 `web/` 目录） | [候选/事实-已核] |
| 版本 | **v1.17.1**（2026-09-10 发布，Bug Fixes and Improvements） | [候选/事实-已核] |
| 来源 URL | 源码 https://github.com/langgenius/dify ；Release https://github.com/langgenius/dify/releases/tag/1.17.1 ；LICENSE https://raw.githubusercontent.com/langgenius/dify/main/LICENSE | [候选/事实-已核] |
| 取证时间 | 2026-09-15 | 事实 |

- 顶层结构（依据 `dify目录框架图.md`）：`api/`（controllers/services/models/core/migrations/providers/events/tasks…）、`web/`（app/features/components…）、`packages/`、`cli/`、`dify-agent/`、`dify-agent-runtime/`、`sdks/`、`docker/`、`scripts/`、`dev/`、`docs/`、`e2e/`。
- **能力域覆盖（`[推断]`，对照 T8 清单）**：知识检索（RAG/知识库 dataset）、Agent 运行时（dify-agent/core）、会话治理、工具/MCP（core/mcp）、前端（web 全功能控制台）、工程质量（e2e/测试/多语言）。**未见明确覆盖**：证据底层设施、监察、学业资产生命周期、学校现实反馈。
- **0.5→1 覆盖部件（`[推断/候选]`）**：LLM 应用平台层能力（工作流编排、RAG 知识库、模型供应商、前端控制台）可作为 STEM/检索相关能力参考来源。**注意**：许可证为修改版 Apache-2.0，含商业授权前置条件 —— 此为已核事实，是否影响采用由后续工程阶段/用户拍板，本文件不作门禁预筛结论。

---

## 3. OpenHands —— 软件工程 Agent 平台候选

| 项 | 值 | 证据等级 |
|---|---|---|
| 仓库 | `OpenHands/OpenHands`（历史为 `All-Hands-AI/OpenHands`，org 已更名，最新 Release 指向 `OpenHands/OpenHands`） | [候选/事实-已核] |
| 仓库/主页 | https://github.com/OpenHands/OpenHands | [候选/事实-已核] |
| 一句话定位 | 通用软件工程 agent 平台：自主 coding agent、多 Agent 协作、Agent Canvas 桌面端 | [候选/推断] |
| 语言/栈 | TypeScript/React（`src/` 主应用、`electron/` 桌面、`docker/`、`helm/`、`tests/`、`e2e/`、Playwright mock-LLM） | [候选/推断] |
| 许可证 | **MIT**（LICENSE 全文实测，Copyright © 2025 OpenHands contributors） | [候选/事实-已核] |
| 版本 | **v1.18.0**（2026-09-11 发布） | [候选/事实-已核] |
| 来源 URL | 源码 https://github.com/OpenHands/OpenHands ；Release https://github.com/OpenHands/OpenHands/releases/tag/v1.18.0 ；LICENSE https://raw.githubusercontent.com/OpenHands/OpenHands/main/LICENSE | [候选/事实-已核] |
| 取证时间 | 2026-09-15 | 事实 |

- 顶层结构（依据 `OpenHands目录框架图.md`）：`src/`（Web 主源码）、`electron/`、`docker/`、`helm/`、`config/`、`docs/`、`examples/`、`tests/`、`e2e/`、`__tests__/`、`specs/`、`tools/`、`scripts/`、`.agents/`、`.openhands/`；根目录 `LICENSE`/`README`/`AGENTS.md`/`CHANGELOG`/`package.json`/Playwright 配置。
- **能力域覆盖（`[推断]`，对照 T8 清单）**：Agent 运行时、工具执行、会话治理、多 Agent 协作、前端/Agent Canvas、工程质量（mock-LLM Playwright 测试体系、变异测试）。**未见明确覆盖**：证据底层设施、监察（学业语义）、学业资产生命周期、学校现实反馈。coding-agent 能力与学业军师核心竞争力（辅导/证据判断）非同一域，属可借鉴而非核心复用。
- **0.5→1 覆盖部件（`[推断/候选]`）**：Agent 运行时与桌面端封装、mock-LLM 测试体系、会话与多 Agent 协作经验可作为工程参考。

---

## 4. openclaw —— 多 Agent 个人助手运行时候选

| 项 | 值 | 证据等级 |
|---|---|---|
| 仓库 | `openclaw/openclaw` | [候选/事实-已核] |
| 仓库/主页 | https://github.com/openclaw/openclaw ；文档 https://docs.openclaw.ai/ | [候选/事实-已核] |
| 一句话定位 | 多 Agent 个人助手运行时：「The AI that really does things. Any OS. Any Platform.」，skills/plugins 生态 + MCP 工具协议 | [候选/推断] |
| 语言/栈 | TypeScript monorepo（`src/` 主源码 + `packages/` + `apps/` + `extensions/` + `skills/`，依据 `openclaw目录框架图.md`） | [候选/推断] |
| 许可证 | **MIT**（LICENSE 全文实测，Copyright (c) 2026 OpenClaw Foundation） | [候选/事实-已核] |
| 版本 | **v2026.9.4**（2026-09-11 发布） | [候选/事实-已核] |
| 来源 URL | 源码 https://github.com/openclaw/openclaw ；Release https://github.com/openclaw/openclaw/releases/tag/v2026.9.4 ；LICENSE https://raw.githubusercontent.com/openclaw/openclaw/main/LICENSE | [候选/事实-已核] |
| 取证时间 | 2026-09-15 | 事实 |

- 顶层结构（依据 `openclaw目录框架图.md`，本地 `/Users/caibiao/Downloads/openclaw-main`）：`src/`（agents 为核心）、`packages/`、`apps/`、`extensions/`、`skills/`、tests。生态仓库另见 `openclaw/agent-skills`、`openclaw/clawhub`（技能/插件注册表）。
- **能力域覆盖（`[推断]`，对照 T8 清单）**：Agent 运行时、多层协作（多 Agent 编排）、工具协议（MCP、mcporter）、会话/记忆、监察（控制中心类生态工具）、前端/CLI、工程质量（release 验证体系、依赖证据）。**未见明确覆盖**：证据底层设施、学业资产生命周期、学校现实反馈。
- **0.5→1 覆盖部件（`[推断/候选]`）**：多 Agent 编排、MCP 工具协议接入、skills 生态与记忆体系可作为学生端多专/工具协议层次的能力参考来源。是否纳入首版尚需 `_证据包` T8[访3] 五类门禁与反事实本体测试，本文件不作决定。

---

## 5. OpenMAIC —— 多 Agent 互动课堂候选

| 项 | 值 | 证据等级 |
|---|---|---|
| 仓库 | `THU-MAIC/OpenMAIC` | [候选/事实-已核] |
| 仓库/主页 | https://github.com/THU-MAIC/OpenMAIC | [候选/事实-已核] |
| 一句话定位 | Open Multi-Agent Interactive Classroom（清华多 Agent 互动课堂）：一键沉浸式多智能体学习体验 | [候选/推断] |
| 语言/栈 | TypeScript / Next.js monorepo（`app/` 路由 + `components/` + `lib/` + `packages/` + `render-service/` + `skillls/` + `eval/` + `e2e/`，依据 `OpenMAIC目录框架图.md`） | [候选/推断] |
| 许可证 | **MIT**（LICENSE 全文实测，Copyright (c) 2026 THU-MAIC） | [候选/事实-已核] |
| 版本 | **v1.0.2**（2026-09-13 发布，Security release） | [候选/事实-已核] |
| 来源 URL | 源码 https://github.com/THU-MAIC/OpenMAIC ；Release https://github.com/THU-MAIC/OpenMAIC/releases/tag/v1.0.2 ；LICENSE https://raw.githubusercontent.com/THU-MAIC/OpenMAIC/main/LICENSE | [候选/事实-已核] |
| 取证时间 | 2026-09-15 | 事实 |

- 顶层结构（依据 `OpenMAIC目录框架图.md`）：`app/`、`components/`、`lib/`（应用核心库）、`packages/`（monorepo）、`render-service/`（独立渲染服务）、`configs/`、`skills/`、`tests/`、`eval/`、`e2e/`、`public/`、`assets/`、`community/`、`types/`、`.github/`。
- **能力域覆盖（`[推断]`，对照 T8 清单）**：多层协作（多 Agent 互动课堂：讲师/学生/助教等角色）、Agent 运行时（skill：agent-runtime / openmaic）、渲染服务（slide/教材渲染）、前端（Next.js）、教育内容生成（eval/quiz）。**未见明确覆盖**：证据底层设施、监察（学业语义）、学业资产生命周期、家长侧。OpenMAIC 为互动课堂（班级/场地）语义，与学业军师「一主多专 + 学科战场分层 + 学校现实反馈」的产品本体存在场景差异，属候选能力来源而非常见底座。
- **0.5→1 覆盖部件（`[推断/候选]`）**：多 Agent 教育协作编排、渲染服务、课堂式交互、quiz/示例生成可作为「学科因地制宜专家集群 / 课堂协同」类能力的参考来源。是否纳入首版由后续 0.5-2 验证与门禁判断。

---

## 6. langgraph —— 图编排 / 状态持久化候选（0.5-1 优先级最低，`_证据包` [上下] §7）

| 项 | 值 | 证据等级 |
|---|---|---|
| 仓库 | `langchain-ai/langgraph` | [候选/事实-已核] |
| 仓库/主页 | https://github.com/langchain-ai/langgraph | [候选/事实-已核] |
| 一句话定位 | 多 Agent/工作流图编排运行时 + 检查点/状态持久化（LangGraph core、checkpoint-sqlite/postgres、sdk、prebuilt、cli） | [候选/推断] |
| 语言/栈 | `libs/` monorepo：`langgraph/`（核心 Python 包）、`checkpoint/`、`checkpoint-postgres/`、`checkpoint-sqlite/`、`prebuilt/`、`sdk-py/`、`sdk-js/`、`cli/`（依据 `langgraph目录框架图.md`） | [候选/推断] |
| 许可证 | **MIT**（LICENSE 全文实测，Copyright (c) 2024 LangChain, Inc.） | [候选/事实-已核] |
| 版本 | 多包 monorepo，release-please 按包打 tag；本次实测最近 Release tag 为 `sdk==0.4.4`（2026-08-27）；核心 `langgraph` Python 包独立版本**未单独取到**（见未核实清单） | [候选/事实部分已核] |
| 来源 URL | 源码 https://github.com/langchain-ai/langgraph ；LICENSE https://raw.githubusercontent.com/langchain-ai/langgraph/main/LICENSE | [候选/事实-已核] |
| 取证时间 | 2026-09-15 | 事实 |

- 顶层结构（依据 `langgraph目录框架图.md`）：`libs/`（checkpoint / checkpoint-conformance / checkpoint-postgres / checkpoint-sqlite / cli / langgraph 核心 / prebuilt / sdk-js / sdk-py）、`docs/`、`examples/`（rewoo/self-discover/tutorials/usaco/web-navigation…）、`Makefile`、`LICENSE`。
- **能力域覆盖（`[推断]`，对照 T8 清单）**：Agent 运行时（图状态机、确定性的 flow/状态迁移）、唯一状态源与生命周期（checkpoint 持久化、sqlite/postgres 记忆/threads）、会话治理（graph/thread）、人机协同（interrupt/交互）。**未见明确覆盖**：证据底层设施、监察、学业资产生命周期、学校现实反馈、前端。
- **0.5→1 覆盖部件（`[推断/候选]`）**：图编排 + 确定性状态机 + 检查点持久化，与学业军师「唯一状态源/确定性强制」相关的工程能力可能存在参考价值；但 `_证据包` [上下] §7 明确其「是否纳入首版待研究验证」，优先级最低，本文件不将其作为既定选择。

---

## 7. 本次未取到 / 未能核实清单

| 项 | 说明 | 状态 |
|---|---|---|
| langgraph：核心 `langgraph`（Python）独立版本号 | repo 为多包 monorepo，release-please 按 package 打 tag，get_latest_release 返回的是 `sdk==0.4.4`（SDK 子包）。核心包版本需查 PyPI 或对应 tag 后补充 | [未取到/待核] |
| dify：官方 star 数等仓库元数据 | 本次版本/许可/主页已核，star 数未在本题取证范围内取到精确值 | [未取到/非关键] |
| 各 repo「对学业军师学生端底座的适用性」运行验证 | 本文件仅 0.5-1 网上取证（来源/版本/许可/结构），未做 0.5-2 只读审查与运行验证；所有适用性均为 [推断/候选]，需后续阶段核验 | [未执行/待验证] |
| OpenHands org 归属 | 最新 Release 证据指向 `OpenHands/OpenHands`（历史 `All-Hands-AI` org 更名）。已完成本体确认，无需复核 | [已核] |
| DeepTutor / 其余 repo 的许可证文本全文 | 已取到 LICENSE 首页确认许可类型；Apache-2.0 全文为通用模板、MIT 全文短且已核，未逐段复录 | [已核/足够] |

---

## 8. 研究技能调用记录（skill-first 执行证据）

- **`research` 技能**：**已调用**（skill 位于 `~/.claude/skills/research/SKILL.md`），按其一手来源契约执行：每条结论追回官方仓库 / 官方 Release 页 / 官方 LICENSE 原文，未引用二手转述。调用结果为「启动 background research 契约 + 证据追源」，本次由本底座深挖研究员按该契约实际执行取证。
- **`agent-reach` 技能**：**已调用**，采用其 web 路由（Jina Reader `r.jina.ai/URL`）；环境内 `curl` 被安全策略阻止，改为 `github MCP`（官方许可/版本精准落值）+ `context-mode ctx_fetch_and_index`（LICENSE 原始文件取回）完成网络取证。
- **`md-writing` 技能**：**已调用**，按研究/报告类骨架撰写本文件（方法→证据→分析→未取到→局限），事实/推断分离，结论先行。
- **github MCP**：用于把 owner/repo、版本、许可证落到精确值（search_repositories / get_latest_release / list_tags）。

---

## 9. 局限与下一动作（如实声明，非已确认能力）

- **局限**：本文件为 0.5-1 网上取证阶段草稿，所有「适用性 / 覆盖部件」均为推断，未做运行验证、未过准入门禁；许可证与版本的精确值以 2026-09-15 实测时点为准。
- **下一动作（供后续阶段，非本文件决定）**：进入 `_证据包` T8 0.5-2（候选只读审查与运行验证）时，逐仓库补充：代码/运行/许可证/依赖/迁移验证、12 项准入检查、反事实本体测试、是否纳入首版判断 —— 均由相应工程阶段与用户拍板。